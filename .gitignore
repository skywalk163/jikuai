# ===== 敏感文件（绝不提交）=====
.env
.env.local
.env.*.local
*.pem
*.key
*.p12
credentials.json
secrets.json

# .env.example 是模板，允许提交
!.env.example

# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ===== 测试与覆盖率 =====
.pytest_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover
.hypothesis/

# ===== 类型检查与 lint 缓存 =====
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/

# ===== 虚拟环境 =====
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# ===== 编辑器 / IDE =====
.vscode/
.idea/
*.swp
*.swo
*~
.comate/

# ===== 操作系统 =====
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# ===== 日志与临时文件 =====
*.log
*.tmp
*.bak
tmp/
temp/

# ===== 极快包管理（M8）=====
# 依赖安装目录：可由 `jk 包 装` + 包.锁 完整还原，不入库。
# 包.json 与 包.锁 应提交进版本库。
极快_包/

# ===== 编辑器插件构建产物 =====
# `vsce package` 的输出。可由源码重新打包，不入库；发行走 Release 附件。
*.vsix

# ===== 块自测产物兜底（W114 / v0.24.0）=====
# `数据.存文` / `数据.载入` / `数据.序出` 三个块的 `测试.jk` 会真写文件。
# runner 已 chdir 到 pytest 的 tmp_path，正常路径下不会落到仓库里；这条是
# 兜底——人手动 `jk <块>/测试.jk` 时产物会落在当前目录。
# 背景：v0.24.0 之前这些产物写死成 `stdlib/blocks/...` 相对路径，9 个真被
# 提交进了库，还跟着进了 wheel 发给用户。
临时_测试*.txt

# ===== 误建目录兜底 =====
# `python scripts/generate_block_index.py --check` 一旦被当成位置参数解析，
# 就会在仓库根建出 `--check/` 这种目录（v0.15.0 真实踩过）。
--*/

