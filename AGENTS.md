# AGENTS.md · 给 AI Agent 的极快调用手册

本文件写给在本仓库里工作的编码 agent。人读的总览在 [README.md](README.md)，
协议正本在 [docs/协议-三通道.md](docs/协议-三通道.md)。

## 一、这个工具替你做什么

极快（`.jk`）不是让你去学的语言，是一个**确定性代码合成层**：从 139 个块的库里
挑几个块，用模板 + 类型图推导拼出粘合代码，**整条链路不调用任何 LLM**。

对你的意义：把「生成几百行 + 反复调试」换成「选 3 个块 + 填参数」，输出 token 从
O(代码行数) 降到 O(块数)。但它**不是** 0 token——你仍要花 token 读需求、读候选、
挑块、拼方案 JSON。

块库覆盖的域只有七个：**财务 / 数据 / 中文 / 历法 / 网络 / 工具 / 制造**，且是中国式
业务（人民币、个税、增值税、农历、干支、身份证、报销单、工资册、客户对账、产量、
达成率、缺陷率、单车电耗）。这七个域之外的需求，不要用它。

## 二、三连（机读接口）

三条命令，退出码约定 `0` 成功 / `1` 输入错误 / `2` 执行期报错。

```bash
jk 块 选 "月薪两万个税多少" --json    # → 选响应
jk 块 组 方案.json                    # → {"源码": "..."}
jk 块 跑 方案.json --json             # → 跑响应
```

`选响应`：

```json
{
  "需求": "月薪两万个税多少",
  "候选": [
    {"名称": "个税", "领域": "财务", "层级": 0, "导出名": "缴税",
     "描述": "中国大陆个人所得税月度速算，起征点5000，七级超额累进（2019年起适用）",
     "分数": 3.5901, "路径": "[启发式]"}
  ]
}
```

- `名称` 是块目录名，`导出名` 是调用名，**两者可以不同**（`个税` 块导出 `缴税`）。
  写导入语句一律用 `导出名`：`从 blocks.财务.个税 导入 缴税。`
- `分数` 是未归一化的 TF-IDF 词面相关度，**跨查询不可比**，也不衡量覆盖度（见第四节）。
- `路径` 是 `[启发式]` 或 `[神经]`。可选字段 `降级说明` 只在请求神经检索却拿不到
  查询向量时出现。可选字段 `命名空间` 只在第三方块包里出现。

`方案.json`（你要构造的输入）：

```json
{
  "需求": "月薪两万个税多少",
  "共享": [{"名": "赵月收", "值": "20000"}],
  "步骤": [
    {"块": "个税", "领域": "财务", "导出名": "缴税", "参数": ["赵月收"]}
  ]
}
```

`步骤.参数` 省略时，粘合器按类型图 + 槽名字面匹配自动推链。**协议禁止多余字段，多一个
键就报错**——字段名照 [`src/jikuai/service/schema.py`](src/jikuai/service/schema.py) 取。

`跑响应`：`{"源码": "...", "执行结果": {"stdout", "stderr", "返回值", "耗时毫秒",
"错误"?, "诊断"?}}`。

### 另两条：`规划` 与 `问`（v0.27.0 起，制造域问答用）

```bash
jk 块 规划 "2026年6月各车型总产量" [--top 8] [--json]   # → 规划上下文包
jk 块 问   "2026年6月各车型总产量" --模型 <端点> [--严格] [--json]
```

`规划` **纯离线、一个模型都不碰**：它把「问句 + 检索候选 + 语义层 + 块元数据」组装成
`{需求, 语义命中, 候选, 回填契约, 拒答建议, 分歧告警?}`。与 `选响应` 的关键差别是
**候选带 `输入槽[{名,类型}]` 与 `输出类型`**——v0.26.0 实测「候选不告诉你块吃几个参数」
是 LLM 写不出实参的直接根因。读到这段的 agent 就是那个「LLM」：`规划` 出包 → 你回填
`{需求, 方案, 模型}` → `jk 块 组`/`跑`。回填**多一个键就拒**。

`问` 是端到端（包 → 你给的端点 → 校验 → 组 → 跑），要显式 `--模型`，不给就退 1 而
**不静默降级**到某个内置端点。两条路径回填后走**同一个校验器**，`问` 不因为「模型是
自己调的」就旁路它。

回填会被 `tools/ai-bridge/planner.py` 的五条硬规则挡：`参数` 每步必填且长度 = 该候选
`输入槽` 个数 / `块`+`领域`+`导出名` 必须逐字取自本包 `候选` / 命中口径分歧点必须显式
选一条（两侧同时出现也拒）/ 信封与方案形状合法 / `--严格` 下无声明类型的共享常量不许
喂精确形参。拒绝理由是中文可操作文本，照着补就行。

**`拒答建议.覆盖=False` 时 `规划` 的退出码仍是 0**：包是合法产出，判定写在里面供你
决策，不是命令失败了。


## 三、别用 MCP，也别起 HTTP 服务

上面三条是进程级 CLI，用你自带的 shell 工具直接调即可。本仓库**没有 MCP server**，
也不需要——套一层 MCP 只会多一份配置和常驻的工具描述开销。

`tools/web/server.py` 有一套 HTTP 接口（`POST /api/选|组|跑`），**没有任何鉴权，
且 `/api/跑` 会在本机进程内执行调用方提交的极快代码**（极快能通过 `蟒:` 桥调用
Python，等于任意代码执行）。它只适合人在本机交互式使用，不要拿它做 agent 通道，
更不要暴露到网络。

要执行代码，用 `jk 块 组` 拿源码，然后在**你自己的沙箱**里跑 `jk <文件.jk>`。

## 四、检索层不会拒答，判断权在你

`retrieval.py` 的检索**没有分数阈值、没有拒答路径**，永远返回 top-K。库里根本没有
对应能力时，它照样返回几个像样的中文块名和一个高分。实测
（`tools/ai-bridge/bench_retrieval_reject.py`，块库 112）：18 条明确无覆盖的需求，
返回空候选 **0 条**，正确拒答率 **0.0%**。

注意这个数字量的是**检索器**，不是你。它不代表你会被骗——见本节末尾。

四轮实测已证伪所有基于分数的自动拒答方案：近边缘需求上各置信信号的可分性 AUC 只有
0.52–0.67（0.5 = 随机），唯一能拒掉 94% 无覆盖需求的阈值会同时误杀 64% 的真命中。
所以这一层**不会替你把关**。

要你判断的典型情形是**兄弟能力缺位**：库里有同族的块，但口径差一档——方向相反
（正向转换有、反向没有）、粒度不同、算法不同、适用对象不同。这种候选照着组出来的
代码**会跑通，结果是错的**，比报错糟得多。

### 规划器层有个拒答判据，但它拦的不是你以为的那件事

v0.27.0 起 `jk 块 规划` 的上下文包带 `拒答建议.覆盖`。它是**词表覆盖判定**（语义层
业务词或其同义词字面命中 + 候选非空），**没有任何分数参与**。实测
（`tools/ai-bridge/bench_planner.py`，块库 139）：

- **远离档**（10 条整类能力不在库里）：规划器层拒下 **50.0%**。
- **近边缘档**（8 条口径差一档）：规划器层拒下 **0.0%**；兄弟块诱骗率 **62.5%**，
  与检索层**同一个数**。

原因不神秘：`中位达成率` 里有 `达成率`、`单车水耗` 里有 `水耗`，字面覆盖判定必然判
「覆盖」。所以——

- **它拦「整类能力不在库里」，拦不住「口径差一档」**。后者只有 `分歧告警` 逼你显式选
  一条，加上下面那些规则。别把它读成「规划器会拒答」。
- **规划器层拒下的那部分不改变检索层仍是 0.0%**。两层量的是不同的东西，`jk 块 选`
  一行没改，它照旧永远返回 top-K、永不拒答。
- 覆盖判定**只登记制造域**业务词（42 条），因此财务/数据/中文/历法/网络/工具六个域的
  问句在那里一律判「未覆盖」——那是规划器的作用域边界，**不是**「块库没这能力」。

### 因此你必须遵守


1. **每次拿到候选，先自己判断它是否真的做这件事**。判据是 `描述` 里的口径声明。
   `分数` 是 TF-IDF 词面相关度，不衡量覆盖度：高不代表命中，低也不代表没命中。
2. **块描述会自报口径**——「算术平均」「SHA256」「把公历年月日转成」「同 os.listdir
   语义」「不发起连接」。需求在那个维度上超出了自报口径，就当没命中。
3. **描述没在你关心的那个维度上自报口径时，别赌**。要么按未命中自己写，要么在回复里
   点明「块描述没说清这一点」让人复核——照着用可能对也可能错。
4. **没命中就自己写代码**，别硬凑。
5. **涉及钱、税、社保、工资的需求，宁可自己写 + 补测试**。这类错误在运行期不报错，
   只在审计时暴露。
6. `jk 块 组` 的源码里出现 `?` 占位或 `需人工填参` 记号，表示粘合器推不出实参并
   **拒绝硬塞**（它把拒绝理由写进源码注释）。这是设计如此，不是 bug——照注释补参数，
   或者判断这条路走不通就换自己写。

### 陷阱：块库 ≠ stdlib 模块，`选块` 只搜块（本节最要紧的一段）

第六轮实测（`tools/ai-bridge/agents-md-ab/`，三臂 × 35 条真实 agent 对照）给出的结论是：
上面那些「自己判断」的规则，你大体做得到——只给协议、不给任何拒答提示的对照臂，在 19 条
近边缘负例上判对 18 条，12 条正例一条没误弃。**但下面这张表你推不出来**，对照臂在这三条
（银行卡、社会信用代码、车牌）上全部选择自己重写 Luhn/加权校验，而正确答案是标准库里已有。
所以本节真正不可替代的就是这张表。

`jk 块 选` 检索的是 `stdlib/blocks/` 下的 139 个**块**。极快还有另一套能力面——
`stdlib/*.jk` **模块**，`选块` **永远不会返回它们**：

| 模块 | 导出 |
| --- | --- |
| 校验 | 校验身份证、校验手机号、**校验银行卡**、**校验车牌**、**校验社会信用代码** |
| 正则 | 匹配、搜索、替代、编译 |
| 简繁 | 转繁体、转简体 |
| 排版 | 规范化文本、插入间距、规范标点 |
| 成语 | 是成语、成语释义 |
| 分词 | 分词 |
| 工具 | 平方、立方 |

所以「选块没命中」**不等于**「极快没有这个能力」。上表里的东西直接 `从 校验 导入
校验银行卡。` 用，别自己重写。判断顺序应该是：先查上表 → 再 `jk 块 选` → 都没有才自己写。

## 五、其它入口

Python 库（`import jikuai` 无副作用、无全局可变状态）：

```python
import jikuai
mod = jikuai.load("脚本.jk")       # 模块对象
jikuai.run_source('打印 加 3 5。')
# 异常 jikuai.JiKuaiError，e.info 是 ErrorInfo（保留中文文案）
```

LSP（`python -m jikuai_lsp`）有 `workspace/executeCommand` 命令 `极快.选块`，
入参 `{需求, top?}`，出参与 `jk 块 选 --json` 同构。第四节的限制同样适用。

包管理：`jk 包 <初始化|添加|装|列表|运行|发布|密钥|搜索|注册表>`。

## 六、改本仓库代码时

- 协议字段**只能**从 [`src/jikuai/service/schema.py`](src/jikuai/service/schema.py)
  的常量取，通道里出现手写字段名字面量视为违约（W20 起 grep 把关）。
- 任何声称提升检索指标的改动，**必须同时报调优集和留出集两套数字**
  （`tools/ai-bridge/评测集.json` / `评测集-留出.json`）。只有留出集也涨才算真涨；
  留出集只当裁判，绝不能看它的 miss 明细去调参。
- 拒答相关改动另有两档负例集：`评测集-无覆盖*.json`（远离档）、
  `评测集-近边缘*.json`（近边缘档）。**必须分档报数**，只报远离档等于自欺。
- 门禁 G10–G23 在 [`scripts/check_stdlib_contract.py`](scripts/check_stdlib_contract.py)
  （G21 三包发行元数据在 `check_dist_metadata.py`、G22 制造域口径契约在
  `check_manufacturing_contract.py`、G23 规划器契约在 `check_planner_contract.py`，
  都由主脚本串起来）。G23 比 G10-G19 慢（子进程回放 15 份录像、约 8.5 秒），
  但仍在主流程里跑——录像回放不进 CI 就等于没有。
- 改块元数据后要重跑 embeddings 才能过 G12；本机离线要设
  `HF_HUB_OFFLINE=1` `TRANSFORMERS_OFFLINE=1`。
- **改本文件第四节要重跑对照实验**：`tools/ai-bridge/agents-md-ab/` 是三臂 A/B 台架
  （裸协议 / 全文 / 只给规则），`构建判分.py 构建|判分`。它需要人在仓外空目录开三个
  独立会话跑，做不成 CI 门禁——但改了拒答相关文字就该重跑，否则等于凭感觉写文档。
  已知结论：黑名单式清单证明不了收益，模块陷阱表是唯一不可替代的部分。
