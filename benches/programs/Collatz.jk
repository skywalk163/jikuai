-- Collatz 猜想：while 循环 + 整数分支（AOT 子集内）
-- 对 1..1000 每个初值算 Collatz 步数，累加输出总步数

定义 钱总步 = 0。
定义 赵起 = 1。
当 小于等于 赵起 1000：
    定义 孙x = 赵起。
    定义 孙步 = 0。
    当 大于 孙x 1：
        如果 等于 (取余 孙x 2) 0：
            赋值 孙x = 整除 孙x 2。
        否则：
            赋值 孙x = 加 (乘 3 孙x) 1。
        。
        赋值 孙步 = 加 孙步 1。
    。
    赋值 钱总步 = 加 钱总步 孙步。
    赋值 赵起 = 加 赵起 1。
。
打印 钱总步。
