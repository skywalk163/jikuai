-- 斐波那契：递归压力（AOT 子集内）
-- 规模刻意控制在单次约 1-2 秒：解释器约 35k 次调用/秒，fib(22) ≈ 57k 次调用。
-- 要加压请改 22 -> 26（约 7 倍），但 CI 里不建议。

函数 斐 接收 赵n：
    如果 小于 赵n 2：
        返回 赵n。
    。
    返回 加 斐(减 赵n 1) 斐(减 赵n 2)。
。

打印 斐(22)。
