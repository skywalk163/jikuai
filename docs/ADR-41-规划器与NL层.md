# ADR-41 · 规划器与 NL 层

- 状态：已接受（2026-08-18，v0.27.0 W147）
- 关联：[`v0.27.0-WBS.md`](v0.27.0-WBS.md)、[`ADR-40-制造域数据模型.md`](ADR-40-制造域数据模型.md)（口径正本）、
  [`ADR-30-槽绑定歧义消解.md`](ADR-30-槽绑定歧义消解.md)、[`ADR-26-类型词表规范.md`](ADR-26-类型词表规范.md)、
  [`路线图-v0.26-v0.28.md`](路线图-v0.26-v0.28.md) §六
- 取代/补充：无。本 ADR 定义 v0.27.0 新增的规划器层，不改既有口径正本。

## §1 主张与边界

**核心主张：LLM 只选口径，不算数。** 自然语言理解（问句 → 选哪些块、填哪些实参）交给
LLM；口径计算（达成率怎么加权、缺陷率先汇总后相除）由确定性块算；`组`/`跑` 的代码
合成与执行链路**整条不调用 LLM**。这是本项目区别于「LLM 直接写 SQL / 直接算数」的
落点——LLM 错选口径会被 §4 校验器和 §5 拒答拦下，LLM 不经手任何一个数字。

**不做真 Text-to-SQL。** 理由：一旦让 LLM 直接产出可执行查询，口径正确性就回到「祈祷
模型没算错」，本项目的确定性合成层卖点被架空。等价 SQL 导出（v0.28.0 W159）只作
「展示查询过程」，不进执行路径。

**边界**：规划器不进 wheel（§7）；不碰检索层的拒答（§5）；不新增 Web 路由
（演示端点安全化是 v0.28.0 W161）。

## §2 双模式，共用一个校验器

两条命令，职责切分明确：

- `jk 块 规划 "问句" [--json]` → 出**规划上下文包**（§3）。纯离线、确定性、CI 真跑。
  它不接触任何模型——只是把「问句 + 检索候选 + 语义层 + 块元数据」组装成 LLM 能照着
  回填的受限结构。**正在读这段的 agent 自己就是那个 LLM**：`规划` 出包，你回填。
- `jk 块 问 "问句" --模型 <端点> [--json]` → 端到端：上下文包 → 外部端点 → 回填 JSON
  → 校验器 → 组 → 跑 → 答案 + 溯源。给 demo 用。

**硬约束：两条路径回填后走同一个 `validate_filled`（§4）。** `问` 不因为「模型是自己
调的、应该更可信」就旁路校验。外部端点的响应一律当**不可信输入**。这条是为了让
`规划`（人/agent 手填）和 `问`（模型自填）的正确性保证完全一致——校验器是唯一真源。

不给 `--模型` 调 `问` → 退 1 并指向 `规划`，**不静默降级**为某个内置默认端点。

**W158 落地口径（2026-08-19）**：`--模型 <端点>` 的契约是 **POST 上下文包 JSON →
回填响应 JSON**。端点鉴权 token 只从环境变量 `JIKUAI_PLANNER_TOKEN` 取（取到就发
`Authorization: Bearer`），**命令行不收 key**——命令行会进 shell history、进 `ps` 输出、
进 CI 日志，是三条各自独立的泄露路径。端点 scheme 走白名单，只收 `http`/`https`：
`file:`/`data:` 交给 `urlopen` 会去读本机文件，那不是「调模型」。

拒答判定为库外时，两条命令的退出码**刻意不同**：`规划` 退 0（判定是包里的一个字段，
不是命令失败；把它变成非零会让 CI 与管道把一次正常的「劝你别用」当成工具坏了），
`问` 退 1 且在调端点**之前**就停（省掉那次推理，也不给模型一个注定要编的上下文）。


## §3 规划上下文包：为什么「选响应」不够

现有「选响应」的候选字段是 `名称/领域/层级/导出名/描述/分数/路径`
（`schema.CANDIDATE_REQUIRED`）——**它不带块的输入槽**。这正是 v0.26.0 W145 实测里
LLM 写不出实参、只能靠人手写 124 步实参的直接根因：候选告诉你「有个块叫 `产量汇总`」，
但不告诉你它吃几个参数、每个参数什么类型。

上下文包在候选基础上补齐（字段常量在 `schema.py`，W148）：

```
需求
语义命中[业务词, 表, 字段, 口径说明]        # 从 制造/语义层.json 的 42 条业务词命中
候选[
  …选响应候选全部字段,
  输入槽[名, 类型],                          # 新增：LLM 照它填 参数
  输出类型                                    # 新增：LLM 据它接链
]
分歧告警[分歧点, 两侧块名, 实测差值, 须显式选一条]   # §5
回填契约                                      # 给 LLM 的回填格式说明（字段名、必填项）
拒答建议[覆盖, 理由]                          # §5，含"制造域无领域先验"这类已知缺口
```

`输入槽` 是把「实参写死」从事后校验前移到**信息供给**：LLM 知道每个槽的名和类型，
才有可能一次填对。校验器（§4）是兜底，上下文包是使能。

**回填响应**（LLM 回填后、过校验器前的形状，W148 定）：

```
需求
方案                                          # PLAN_* 结构，LLM 的产出
模型                                          # 回填来源标识：人手填 '人工'，端到端填端点名
溯源?                                         # 可选，端到端时带上每个数字的来源块名
```

`模型` **不可省**：ADR-41 §8 的录像回放要靠它标明这份回填出自谁，缺了就无法判断
「换模型后结果变了」还是「链路本身变了」。空串同样拒。

**拒答走 `拒答建议` 而不是回填响应里的拒答分支**：判「库外能力」是规划器在出上下文包
时就该做完的事（§5 的词表覆盖判定不需要 LLM 参与）。让 LLM 再回一个「我拒答」的分支，
等于把已经确定的判断交回给不确定的一方。所以回填响应里 `方案` 是必需字段。


## §4 回填校验器：五条硬规则

`planner.validate_filled(填充方案, 上下文包)` → `(通过?, 拒绝理由列表)`。任一不过即拒。
每条都要有反例测试（W157，≥5 类反例）：

1. **每步 `参数` 必填，长度 = 该块输入槽数。** 缺一个就拒。比粘合器的 `?` 占位更早
   一道闸——`?` 占位是「组出来了但推不出实参」，这条是「根本没让你省略实参」。
   *根因*：v0.26.0 W145 对照实验证明，省略 `参数` 让粘合器自动推链会**静默错绑**
   （`读表(赵产量列)`），既不落 `?` 也不写拒绝理由，运行期才死。制造域尤其致命。
2. **块/领域/导出名必须来自上下文包 `候选`。** 白名单，防 LLM 幻觉块名。
3. **分歧点两侧同时出现 → 拒；问句命中该口径而一侧都没选 → 拒。** 逼 LLM 显式选一条
   口径（§5）。
4. **产物必过 `schema.validate_plan`。** 协议多余字段、类型错误在这里兜底。
5. **`--严格` 下，无声明类型的共享常量不许喂精确形参。** 配合 §6 的粘合器治根。

拒绝理由必须**可操作**：说清缺哪个槽、该填什么类型，不是一句「校验失败」。

**W157 落地口径（2026-08-19）**：实现签名是
`planner.validate_filled(回填, 上下文包, 严格=False) -> list[str]`——返回**中文拒绝理由
列表，空列表 = 通过**，不是本节初稿写的 `(通过?, 理由)` 二元组。改的理由：全仓校验器
（`schema.validate_*`、粘合器的拒绝理由）都是这个约定，`通过 = not validate_filled(...)`
一行可得，多带一个布尔只是多一处可以写反的地方。另配 `ensure_filled`（不通过抛
`SchemaError`），与 `schema.ensure_*` 同构。第 4 条实际调 `validate_filled_envelope`
（内含 `validate_plan`）且**先跑**：形状不对时后四条读到的 `步骤` 可能压根不是数组，
先回形状错误，不叠一堆派生噪音。


## §5 拒答放规划器层，不放检索层

**检索层本轮一行不改。** `retrieval.py` 无分数阈值、永远返回 top-K，这是四轮实测后的
既定设计：近边缘需求上各置信信号的可分性 AUC 仅 0.52–0.67（0.5=随机），唯一能拒掉
94% 无覆盖需求的阈值会同时误杀 64% 真命中（`bench_retrieval_reject.py`）。
**任何想在检索层复活分数阈值的改动，先去读那四轮结论。**

规划器层的拒答判据是**词表覆盖判定**，与分数无关：
- 语义层业务词未登记 **且** 候选块无一自报对应口径 → 拒（远离档）。
- 近边缘档**不自动拒**：出 `分歧告警`（两侧块名 + 实测差值）逼 LLM 显式选一条，
  选不出才拒。

分档报数（远离档 / 近边缘档），与检索层的 0.0% 并列——**它们量的是不同层，规划器的
拒答数字不许拿来掩盖检索器仍是 0.0% 这个事实**。

**W160 实测（2026-08-19，块库 139，`bench_planner.py`）把这条边界钉死了**：

- 远离档 10 条：规划器层拒答率 **50.0%**（检索层 0.0%）。词表覆盖判定确实能拦下一半。
- 近边缘档 8 条：规划器层拒答率 **0.0%**、兄弟块诱骗率 **62.5%**，**与检索层逐位相同**。

这不是实现没做好——是**判据本身的边界**。覆盖判定按字面子串匹配业务词，而近边缘档的
问句里那个业务词就在词表里（`中位达成率` 有 `达成率`、`单车水耗` 有 `水耗`），覆盖判定
**必然**判 True。也就是说本节原来写的「近边缘档不自动拒、出 `分歧告警` 逼选一条」只在
**问句恰好命中某个已登记分歧点**时生效（近边缘 8 条里仅 2 条带告警）；其余「口径差一档
但不在已登记分歧点上」的近边缘需求，规划器层**拦不住**，最终防线仍是 AGENTS.md §四的
「判断权在你」。**W161 改 AGENTS.md 时必须把这条如实写进去，不许含糊成「规划器会拒答」。**

> **2026-08-20 补（v0.28.0 W169）**：上面那句原文写的是「四处分歧点」，已改为「已登记
> 分歧点」——分歧点数量会长（W169 加了 ADR-40 §5.5 绝对量 vs 比率排名，现为**五处**），
> 但**这一段的结论不随数量变化**：多一处只是把「恰好命中」的问句多盖一类，盖不到的近边缘
> 需求照旧由 agent 自己判。括号里「近边缘 8 条里仅 2 条带告警」是 W161 对四处的实测，
> **不改它**；第五处用「与」形触发（只在排名语境响），在近边缘 8 条上新增命中 **0 条**。


讲口径分歧要**拿缺陷率那 57% 去讲**（2026-06 先汇总后相除 0.050550 vs 行级比率平均
0.032218）。达成率那处在本数据集只差万分之几，讲会讲得很弱。

## §6 `共享` 携带声明类型：改动面与预期代价

**根因链**（v0.26.0 W145 + Explore 实测）：`glue.py:376-380` 把 `共享` 常量一律入池为
`('任意', None)` → `type_feeds`（`glue.py:236-238`）里 `任意` 双向放行 → 每个字符串常量
对每个形参都「类型兼容」→ 多候选消解退回「最近产出优先」随便挑 → 静默错绑。

**改动**（W149-W152）：
- `schema.py` 的 `共享` 键集从硬编码 `('名','值')` 加可选 `('类型',)`，取值域落 ADR-26
  类型词表。
- `glue.py` 入池读声明类型，无声明才退 `任意`。
- 多个 `任意` 候选竞争同一槽 → 落拒绝理由，不再「最近产出优先」。
- `type_feeds` 的类型格语义**不动**（`任意` 仍是顶）；改的是「不再无脑把常量标成
  `任意`」。

**预期代价（写死在此，不许到时候当回归回退）**：这会让 `bench_glue.py` 的自动链式
命中率**下降**——因为一批原本靠 `任意` 放行「蒙对」的绑定，现在会变成显式拒绝。
这是**正确的代价**：把静默错绑换成显式拒绝。v0.22 主教训「守卫绿 ≠ 守卫在守」的反面
就是这条——**数字丑但真，优于数字漂亮但错绑**。W152 必须并列报改前/改后两套数字，
下降项逐项写明原因。语义荒谬率仍须 = 0（硬底线）。

**W151-W152 落地实测（2026-08-18，纠正了本节初稿的一处判断错误）**：

初稿以为「消解判据 = 有没有声明类型」——有声明类型的候选走 recency，无声明的拒。
**实测把这条证伪了**：Q_PUB_001 的 5 个共享常量（CSV 路径、列名、两个日期、产量列名）
全都**合法且正确地**是 `字符串`，声明类型一个都挡不住 `表载入.路径` 按书写顺序错绑到
`赵产量列`。病根不是「没声明类型」，是**拿共享常量的书写先后当消解证据**。

修正后的判据（已落 `glue.py`）：多候选且槽名字面消解失败时——
- 候选里有**步骤产出**（非共享常量）→ 取最近产出。产出之间 recency 有语义（数据前向
  流动），L13 等既有链式走这一支。
- 候选**全是共享常量** → 拒绝（落 None + 可操作理由）。常量书写先后不是证据。

bench_glue.py 三轮数字：
- 改前：自动链式命中率 **100.0%** / 语义荒谬率 0.0% / 拒绝质量 100.0% / 边界 100.0%
- W151+W152 初版（判据错用「声明类型」）：命中率跌到 **92.3%**，误伤 L13——L13 的两个
  常量 `赵文`/`赵分` 都无声明，被新拒答逻辑挡下。
- 定版（判据=共享常量 + L13 的 `赵分`→`赵分隔符` 改名）：命中率回 **100.0%**。

**L13 的 fixture 改名不是「把测试改绿」**：`赵分` 剥前缀=`分`，与形参名 `分隔符` 既不
精确也不后缀也不包含匹配，改前它是**靠 recency 蒙对**的（两个无类型常量，恰好挑中对的）。
改名 `赵分隔符` 后剥前缀=`分隔符`，与形参名**精确匹配**（置信度 1.0）——L13 从「靠书写
顺序蒙对」升级成「靠槽名字面真对」。这正是本轮要的方向：**能力靠证据，不靠巧合**。
语义荒谬率全程 0.0%（硬底线未破）。


## §7 规划器为什么不进 wheel

落 `tools/ai-bridge/planner.py`，由 `blocks_cli._planner()` 仿 `_glue()` 动态加载。
不是偷懒：`glue.py` 本来就在 `tools/ai-bridge/`、不进 wheel（`MANIFEST.in` 只
`recursive-include src/jikuai/stdlib`），`blocks_cli._glue()`（`blocks_cli.py:190-213`）
找不到就抛 `BlockError('组/跑 需要仓库内的 tools/ai-bridge/')`。规划器依赖 `组`/`跑`，
**不可能比它的依赖可用性更强**——装了 wheel 的用户本来就跑不了 `组`/`跑`。

副作用（都是好的）：不动 `MANIFEST.in`、不动 `块背衬PY数`（当前 35）、不碰 G20。

## §8 录像回放：端到端评测的唯一可行解

LLM 输出不可复现，而 CI 在 FreeBSD、无外网、无 API key。若在 CI 里真调模型，评测不可
复现且无法离线；若 mock 掉模型，等于 mock 掉被测对象。

解法：`tools/ai-bridge/规划录像/` 录制每条问句的**回填 JSON**（含模型标识 + 时间戳）。
- CI 只回放录像，跑「校验器 → 组 → 跑」——确定性、离线、可进门禁（G23）。
- 真调模型只在本机人工跑，结果单独归档；与录像的差异如实记，不掩盖。

关键：**回放不 mock 校验器**。被测的是「校验器 + 组 + 跑」这条确定性链路对一份给定
回填的处理，模型的角色由录像固定下来。模型换了、prompt 改了，重录即可，被测链路不变。

**W160 落地口径（2026-08-19）** —— 台架 `tools/ai-bridge/bench_planner.py`、录像
`tools/ai-bridge/规划录像/`（15 份 + `清单.json`）：

1. **录像文件是纯回填响应信封**（`{需求,方案,模型,溯源?}`）。`id`/`录制时间`/`来源`
   一律落 `清单.json`——信封键集是白名单，多塞一个元信息键就过不了
   `validate_filled_envelope`，回放第一步就废。
2. **绿的判据是「判定与登记相符」，不是「全部通过」**。清单给每条录像登记
   `期望判定`（`通过`/`拒答`）+ `期望拒因`（关键词逐个都要出现在拒因里）。当前 15 份
   里 11 通过、**4 份该被拒且真被拒**：W145 手写方案把 §5.1/§5.2 两侧口径并排跑当人工
   交叉校验（§4 规则 3 判「两个不同的答案」），且命中 §5.4 参照系分歧点时 `窗间对比`/
   `基线偏离` 尚不存在。**不改录像去凑绿**——登记下来它才会继续咬人：补了环比块或改了
   规则 3，台架当场红。
3. **回放用全库当白名单**。§4 规则 2 要求块名来自上下文包 `候选`，实测正确方案用到的块
   在 TF-IDF 排名里最深到**第 89 名**，默认 `top=8` 下白名单装不下**任何一份**正确方案。
   故「装不装得下」单独量成 `白名单可承载率`（K=8/20/40 分档），回放本身用
   `--回放top`（缺省 = 块库全量）把规则 2 从瓶颈位置挪开。两个数字同表并列，不藏。
   （**v0.28.0 W174 已把 K=8 从 0.0% 抬到 60.0%**，做法与代价见 §9；`--回放top` 缺省
   仍是全量，那是台架口径，不随旁路改。）

4. **`期望结果` 只自动判机读实体**（`M003`/`L002`/`C005` 这类 ID），口径散文
   （「先汇总后相除」）不做自动判定、明确标注要人读。假装量过了比不量更糟。
5. **`--门禁` 是 G23（W161）的接口**：`--只回放 --门禁` 下判定不符即退 1；缺省报数模式
   恒退 0（与 `bench_chatbi.py` 同约定，报数脚本不当 gate 用）。

**W161 落地口径（2026-08-19）** —— G23 `scripts/check_planner_contract.py` 四条断言：
字段来源（AST 扫 `planner.py` 的键位字面量 + 九组字段集冻结）/ 五条规则长在原处 /
**行为断言**（六个合成场景，证明「缺 `参数` 即拒」这类守卫真在守）/ 录像回放
（子进程 `bench_planner.py --只回放 --门禁`）。已串进 `check_stdlib_contract.py`。
三点判断记在这里，免得下一轮重新纠结：

1. **静态与行为两套断言都要**。只静态时把规则循环体注释掉照样绿；只行为时把校验器改成
   「凡不认识的键就拒」也能六场景全过，而规则 1 的「长度 = 输入槽数」已经没了。
   v0.22.0 的主教训是「守卫绿≠守卫在守」，这里按那条教训双向锁。
2. **第 1 条扫键位、不扫全部字符串**。`planner.py` 的 `main()` 里 `add_argument('需求')`
   是 argparse 位置参数名，与协议字段撞名却不是协议使用；按全字符串扫会假红，而假红会逼
   人给门禁开豁免注释——那是门禁死亡的开始。`_I*`/`_Y*`/`_S*`/`分歧点表` 及其循环变量在
   豁免名单里（它们的键与协议字段撞名是事实，混成一套才是病）。
3. **G23 慢在第 4 条（≈8.5 秒）但仍进主流程**，不学 G20 的「只打提示」：录像回放不进 CI
   就等于没有（v0.25.0 W129 / v0.26.0 W144 两次实证）。录像或 `赛题/` 数据集不在场时显式
   打「跳过第 4 条」，绝不静默当通过。



## §9 规则 2 与 `top=8` 的耦合：语义层直取旁路（W174 拍板）

**问题**：§4 规则 2 要求块名逐字取自上下文包 `候选`，而 §8.3 实测正确方案用到的块在
TF-IDF 排名里最深到**第 89 名**——`top=8` 下 `白名单可承载率` 是 **0.0%**。规则 2 与
默认 top 值就这么焊在了一起：不动 top，合法方案一份也回填不出来。

**拍板：走语义层直取旁路，不靠调大 top。** 旁路只在规划器层做，`retrieval.py`
**一行未改**（§5 的边界不动，`jk 块 选` 照旧永远 top-K、永不拒答）；旁路注入的候选
带 `路径 = [语义层]`，与 `[启发式]`/`[神经]` 一眼可分，`分数` 恒 `0.0` 且**明确不是**
TF-IDF 相关度、不可与之比较。

**WBS 原文那条被实测证伪。** 原文写的是「按语义层业务词命中直接把对应块注入候选」，
即「业务词 → 表 → `描述` 里提到该表的制造域块」（下称一跳）。K=8 漏掉的块按频次是
`表载入` 20/20、`窗口` 17、`选取` 12、`连接` 5、`取前N` 4、`排序` 3——**全是通用管道
块**，`描述` 里没有任何表名，这条映射**结构上永远碰不到它们**。缺的不是口径块，是链路
骨架。

有效的是再走**两跳**，且两跳都读块作者**已经写下**的字段，不新建任何手工清单：
一跳拿口径块；二跳拿该口径块 `块.json` 的 `示例` 里 `从 blocks.X.Y 导入` 点名的块，
以及 `描述` 里以 `制造/块名` 形式点名的兄弟块。

四档实测（块库 145，`bench_planner.py` 自己的判据 = 录像方案的块**全部**进候选才算装得
下；分母 = 有录像的用例，调优 10 条 / 留出 5 条。下列每格都是「调优 / 留出」）：

- 基线 K=8（旁路关）：承载 **0.0% / 0.0%**，期望块完整命中 0.0% / 0.0%，平均候选 8.0
- 一跳只做：承载 **0.0% / 0.0%**（候选 11.6 / 11.2）← WBS 原意，**证伪**
- 两跳全做 K=8：承载 **60.0% / 60.0%**，期望块完整命中 **53.3% / 60.0%**，
  平均候选 **16.5 / 15.6** ← **本实现**
- 对照：旁路关 + K=40，承载 **20.0% / 20.0%**，候选 40（旁路开 + K=40 是 80% / 80%）

即：默认 K 不动、候选只从 8 涨到 ~16，就比把 K 调到 40（候选 ×5）高三倍，**留出档同涨**。

> BACKLOG §12.4 原文记的是「K=40 才 30.0%」，那是块库 139 时的数；块库长到 145 后同一
> 判据实测 **20.0%**——**新块进来会摊薄排名**，这个数只会随库增长继续跌。这也正是
> 「靠调大 top」不成立的另一半理由：它的收益随块库反向缩水，旁路不随。

**被排除的一档：无条件注入手工挑的 6 块骨架**（调优 **80%** / 留出仍 **60%**）。多出的

20 点全在调优档，而那 6 块名单本身就是从调优档诊断明细里挑出来的——按 AGENTS.md §六
「只有留出集也涨才算真涨、留出集只当裁判」判为**过拟合，不取**。数字更漂亮不是理由。

**代价，写在明面上**：近边缘档 8 条的兄弟块诱骗率 **62.5% → 75.0%**（+1 条，
`停线汇总` 被二跳带进候选）。这是旁路必然的副作用——多给候选就多给一次被骗机会。
两点限定：(1) 那 1 条**不在** ADR-40 §5.5 已登记的任何分歧点上，五处分歧点的对侧块
旁路**一条都没带进来**（有测试钉死），所以立项时预备的「剔除分歧对侧块」缓解手段
**没有东西可剔**，不做；(2) 远离档拒答率 **50.0% 不变**——旁路只在语义层命中时才动，
远离档压根没有命中。**不拿 60% 的承载率去盖 75% 的诱骗率**，两个数同表并列。

规则 2 本身**不放松**：旁路是把块**送进白名单**，不是给白名单开后门。幻觉块名照旧拒。

## §10 被本 ADR 明确排除的选项

- **让 LLM 直接写 `.jk` 或 SQL**：见 §1，架空确定性合成层。
- **在检索层加分数阈值拒答**：见 §5，四轮实测已证伪。
- **规划器进 wheel 做成独立可分发组件**：见 §7，依赖 `组`/`跑` 决定了它进不了 wheel。
- **`问` 对自调模型旁路校验**：见 §2，破坏「校验器是唯一真源」。
- **为把「完整命中率恒 0」做上去而从评测集期望里删 `表载入`**：那个 0 是真缺口
  （问句从不提「载入」，TF-IDF 抓不到），删期望是自欺。
- **靠调大 `top` 解决规则 2 装不下方案**：见 §9，K=40 只到 20.0%、候选翻五倍。
- **无条件注入一份手工维护的骨架块名单**：见 §9，调优 80% / 留出 60%，多出的 20 点全在
  调优档且名单是从调优档诊断里挑的 —— 过拟合。旁路只读块作者已写下的 `示例`/`描述`。

