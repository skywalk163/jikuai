# PyPI 分发打通 Implementation Plan（v0.24.0）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `pip install jikuai` 在干净 venv、非 editable wheel 下真的能用——stdlib 随包发行，三包同步发 PyPI 0.24.0，yank 线上坏版。

**Architecture:** `stdlib/` 物理搬进 `src/jikuai/stdlib/` 作为包内资源，靠 `[tool.setuptools.package-data]` 进 wheel/sdist；新增 `src/jikuai/resources.py` 作为**唯一**定位入口（含 `JIKUAI_STDLIB` 覆盖口），替掉 6 处各写各的 `__file__` 相对回溯；新增 G20 构建后门禁断言 wheel 内容。推翻 ADR-16 §3.4 的 data-files 裁决，另立 ADR-39。

**Tech Stack:** Python 3.10+、setuptools>=68、pytest、twine、PowerShell 5.1（本机 shell，**不支持 `&&` 与 heredoc**，命令用 `;` 分隔）

> **PowerShell 写文本文件的坑（Task 1 实测踩到）**：PS 5.1 的 `Set-Content -Encoding UTF8`
> **会写 BOM**，setuptools 解析带 BOM 的 `pyproject.toml` 直接失败
> （`ERROR Failed to parse pyproject.toml: Invalid statement (at line 1, column 1)`）。
> 本计划里凡用 shell 写文本文件，一律用：
> `[IO.File]::WriteAllText($路径, $文本, (New-Object System.Text.UTF8Encoding($false)))`
> 仓库内的文件用编辑器工具写，不走 shell。

**Spec:** `docs/superpowers/specs/2026-08-15-pypi分发打通-design.md`

---

## 关键事实与两处对 spec 的修正

写计划时读代码发现两点，实施时按本节口径，不按 BACKLOG §10 原文：

**修正 1 · 是 6 处不是 7 处。** BACKLOG §10 把 `src/jikuai/pkg/blocks_cli.py:186` 的 `_repo_root()` 列为 stdlib 定位点，但 grep 确认它**只被 `blocks_cli.py:202` 用于定位 `tools/ai-bridge/glue.py`**：

```python
path = os.path.join(_repo_root(), 'tools', 'ai-bridge', 'glue.py')
```

它与 `ai/embed_client.py:57` 同类——tools/ 定位器，tools/ 明确不入包，**本轮不改**。真正要改的 6 处：

- `src/jikuai/stdlib_contract.py:16-22` — `default_stdlib_dir()`
- `src/jikuai/module_loader.py:147-149` — `_search_paths()` 的 stdlib 兜底
- `src/jikuai/evaluator.py:1644-1646` — `_load_stdlib_module()`
- `src/jikuai/pkg/blocks.py:301-309` — `blocks_root()`
- `src/jikuai/ai/retrieval.py:131-135` — `vector_index_path()`
- `src/jikuai/ai/retrieval.py:673-677` — `_load_builtin_blocks()`

**修正 2 · 用 `os.path.dirname(__file__)` 而不是 `importlib.resources`。** spec 第三节写的是 `importlib.resources`。实施改用包内 `__file__` 拼路径，理由：这 6 个消费方全都需要**真实文件系统目录路径**——`module_loader` 要往搜索路径列表里塞目录、`evaluator` 要 `spec_from_file_location`、`blocks.py` 要 `os.walk` 扫目录、`retrieval` 要 `open()`。`importlib.resources.files()` 返回 `Traversable`，只有在 zipimport 场景才有额外价值，而本项目**做不到 zip-safe**（它本质上要扫目录），引入它只增加抽象不增加能力。ADR-39 要把这个取舍写清。

---

## File Structure

**新建：**
- `src/jikuai/resources.py` — stdlib 资源定位唯一入口。只 import 标准库、无副作用、无全局可变状态。
- `tests/test_resources.py` — 定位入口的单元测试（含 `JIKUAI_STDLIB` 覆盖）。
- `scripts/check_wheel_contents.py` — G20 构建后门禁：构建 wheel 并断言内容。
- `tests/test_wheel_contents.py` — G20 的 pytest 包装（可被全量回归带起来）。
- `docs/ADR-39-stdlib包内资源.md` — 推翻 ADR-16 §3.4 的新裁决。
- `docs/路线图-v0.24.md` — 本版路线图（沿用仓库既有 `路线图-vX.md` 惯例）。
- `MANIFEST.in` — 保证 sdist 也带 stdlib。

**移动：**
- `stdlib/` → `src/jikuai/stdlib/`（`git mv`，381 文件 / 136 子目录）

**修改：**
- `pyproject.toml` — 加 `[tool.setuptools.package-data]`
- 上述 6 处定位点
- `lsp/pyproject.toml` / `dap/pyproject.toml` — 版本接单一真源 + 依赖钉下界
- `scripts/check_stdlib_contract.py` — 注册 G20；G15 投影从 4 处扩到 6 处
- `README.md` — 安装段与项目结构树
- `docs/BACKLOG.md` — §10 结账
- `CHANGELOG.md` — v0.24.0 段

---

## Task 1: 中文文件名 zip 冒烟（最高风险先做，一次性实验）

stdlib 从上到下全是中文名（`分词词典.txt`、`blocks/财务/个税/*.jk`），而线上 0.4.1 的 wheel 里一个非 ASCII 文件都没有——**这条风险从未被暴露过**。先用 10 个文件试通，再动 381 个。

**Files:**
- 全部在 `$env:TEMP\jk_zh_probe`，**不进仓库**

- [ ] **Step 1: 造一个最小包，含中文文件名与中文子目录**

```powershell
$p = "$env:TEMP\jk_zh_probe"
Remove-Item -Recurse -Force $p -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force "$p\src\zhprobe\stdlib\blocks\财务\个税" | Out-Null
Set-Content "$p\src\zhprobe\__init__.py" -Value "" -Encoding UTF8
Set-Content "$p\src\zhprobe\stdlib\分词词典.txt" -Value "个人所得税`n增值税" -Encoding UTF8
Set-Content "$p\src\zhprobe\stdlib\blocks\财务\个税\个税.jk" -Value "导出 缴税。" -Encoding UTF8
[IO.File]::WriteAllBytes("$p\src\zhprobe\stdlib\blocks\向量索引.bin", [byte[]](1,2,3,4))
```

- [ ] **Step 2: 写 pyproject，用与真包同构的 package-data 声明**

```powershell
$toml = @"
[project]
name = "zhprobe"
version = "0.0.1"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"zhprobe" = ["stdlib/*.txt", "stdlib/blocks/**/*.jk", "stdlib/blocks/**/*.bin"]
"@
Set-Content "$p\pyproject.toml" -Value $toml -Encoding UTF8
```

- [ ] **Step 3: 构建 wheel 并列出内含条目**

```powershell
python -m venv "$p\bv"
& "$p\bv\Scripts\pip.exe" install -q build
& "$p\bv\Scripts\python.exe" -m build --wheel --outdir "$p\dist" "$p"
$w = (Get-ChildItem "$p\dist\*.whl").FullName
Add-Type -AssemblyName System.IO.Compression.FileSystem
$z = [IO.Compression.ZipFile]::OpenRead($w); $z.Entries | ForEach-Object { $_.FullName }; $z.Dispose()
```

Expected：条目里出现 `zhprobe/stdlib/分词词典.txt`、`zhprobe/stdlib/blocks/财务/个税/个税.jk`、`zhprobe/stdlib/blocks/向量索引.bin`，**中文不乱码**。

- [ ] **Step 4: 装进干净 venv，从安装位置按中文路径真读一次**

```powershell
python -m venv "$p\iv"
& "$p\iv\Scripts\pip.exe" install -q --no-deps $w
& "$p\iv\Scripts\python.exe" -c "import os,zhprobe; d=os.path.join(os.path.dirname(zhprobe.__file__),'stdlib'); print(sorted(os.listdir(d))); print(open(os.path.join(d,'分词词典.txt'),encoding='utf-8').read()); print(os.path.isfile(os.path.join(d,'blocks','财务','个税','个税.jk')))"
```

Expected：列目录中文正常、词典内容读出、块 `.jk` 判定为 `True`。

- [ ] **Step 5: 判定与清理**

四步全绿 → 方案 A 可行，继续 Task 2。
任一步中文乱码或文件缺失 → **停止执行本计划**，把失败输出带回来重新评估（可能要退回方案 B 或考虑 sdist-only 分发）。

```powershell
Remove-Item -Recurse -Force "$env:TEMP\jk_zh_probe"
```

- [ ] **Step 6: 不提交任何东西**（本任务零仓库改动，确认 `git status` 干净）

```powershell
git status --short
```

---

## Task 2: `resources.py` 定位入口（TDD，先于搬目录）

先建入口并让它在**旧布局**下也能工作（此时 `src/jikuai/stdlib/` 还不存在，靠 `JIKUAI_STDLIB` 覆盖测），Task 3 搬完目录后默认分支自然生效。

**Files:**
- Create: `src/jikuai/resources.py`
- Test: `tests/test_resources.py`

- [ ] **Step 1: 写失败测试**

```python
# -*- coding: utf-8 -*-
"""stdlib 资源定位唯一入口的测试（ADR-39 / v0.24.0 W113）。"""

import os

from jikuai import resources


def test_stdlib_dir_在包内(tmp_path, monkeypatch):
    """默认分支：stdlib 目录是包内 `jikuai/stdlib`。"""
    monkeypatch.delenv(resources.ENV_STDLIB, raising=False)
    pkg_dir = os.path.dirname(os.path.abspath(resources.__file__))
    assert resources.stdlib_dir() == os.path.join(pkg_dir, 'stdlib')


def test_环境变量可覆盖(tmp_path, monkeypatch):
    """`JIKUAI_STDLIB` 指向已存在目录时优先生效。"""
    monkeypatch.setenv(resources.ENV_STDLIB, str(tmp_path))
    assert resources.stdlib_dir() == os.path.abspath(str(tmp_path))


def test_环境变量指向不存在目录时忽略(monkeypatch):
    """配错路径不该让整个运行时崩——回落到包内默认值。"""
    monkeypatch.setenv(resources.ENV_STDLIB, os.path.join('不存在', '的路径'))
    pkg_dir = os.path.dirname(os.path.abspath(resources.__file__))
    assert resources.stdlib_dir() == os.path.join(pkg_dir, 'stdlib')


def test_blocks_dir_是stdlib下的blocks(monkeypatch, tmp_path):
    monkeypatch.setenv(resources.ENV_STDLIB, str(tmp_path))
    assert resources.blocks_dir() == os.path.join(os.path.abspath(str(tmp_path)), 'blocks')


def test_stdlib_path_拼多级(monkeypatch, tmp_path):
    monkeypatch.setenv(resources.ENV_STDLIB, str(tmp_path))
    got = resources.stdlib_path('blocks', '索引.json')
    assert got == os.path.join(os.path.abspath(str(tmp_path)), 'blocks', '索引.json')


def test_模块只依赖标准库():
    """ADR-25 风格的静态约束：本模块不得 import 第三方或本包其它模块。"""
    src = open(resources.__file__, encoding='utf-8').read()
    for 行 in src.splitlines():
        剥 = 行.strip()
        if 剥.startswith('import ') or 剥.startswith('from '):
            assert 剥 in ('import os',), f'意外的导入：{剥}'
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_resources.py -v`
Expected: FAIL — `ImportError: cannot import name 'resources' from 'jikuai'`

- [ ] **Step 3: 写实现**

Create `src/jikuai/resources.py`：

```python
# -*- coding: utf-8 -*-
"""极快语言 · stdlib 资源定位单一入口（ADR-39 · v0.24.0 W113）。

v0.24.0 之前，stdlib 目录靠 6 处各写各的 `__file__` 相对回溯定位（上溯 2 级或
3 级到仓库根），只在 `pip install -e .` 下成立——wheel 里根本没有 stdlib
（`docs/BACKLOG.md` §10：PyPI 上 0.4.1 的 wheel 零个 stdlib 文件，装完
`导入 数学` 直接报「找不到模块」）。

ADR-39 把 `stdlib/` 收进包内 `jikuai/stdlib/`，**本模块是唯一定位入口**。
新增任何需要定位 stdlib 的代码都必须调这里，不许再写 `__file__` 回溯——
留一处旧回溯，就等于留一个「本机 editable 全绿、wheel 里坏掉」的假绿缺口。

**刻意不用 `importlib.resources`**：6 个消费方全都需要真实文件系统目录路径
（往搜索路径塞目录、`spec_from_file_location`、`os.walk` 扫块、`open()`）。
`Traversable` 只在 zipimport 场景有额外价值，而本项目本质上要扫目录、做不到
zip-safe，引入它只增加抽象不增加能力。见 ADR-39 §3。

只 import 标准库；无副作用、无全局可变状态（同 `jikuai` 包既有约定）。
"""

import os

__all__ = ['ENV_STDLIB', 'stdlib_dir', 'blocks_dir', 'stdlib_path']

#: stdlib 根目录覆盖口。开发、装包、测试三种场景都从这里接管。
#: 值必须是已存在的目录，否则忽略并回落到包内默认值——配错路径不该让
#: 整个运行时崩（同 `blocks.extra_roots()` 对坏路径的处置）。
ENV_STDLIB = 'JIKUAI_STDLIB'


def stdlib_dir() -> str:
    """stdlib 根目录的绝对路径。"""
    override = os.environ.get(ENV_STDLIB, '')
    if override and os.path.isdir(override):
        return os.path.abspath(override)
    here = os.path.dirname(os.path.abspath(__file__))
    return os.path.join(here, 'stdlib')


def blocks_dir() -> str:
    """`stdlib/blocks/` 的绝对路径（块生态根，其下第一级是领域）。"""
    return os.path.join(stdlib_dir(), 'blocks')


def stdlib_path(*parts: str) -> str:
    """拼 stdlib 下的资源路径，如 `stdlib_path('blocks', '索引.json')`。"""
    return os.path.join(stdlib_dir(), *parts)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_resources.py -v`
Expected: PASS 6 项。注意 `test_stdlib_dir_在包内` 此刻断言的是**路径值**而非目录存在性，故搬目录前也应通过。

- [ ] **Step 5: 提交**

```powershell
git add src/jikuai/resources.py tests/test_resources.py
git commit -m "feat(v0.24): stdlib 资源定位单一入口 resources.py · W113"
```

---

## Task 3: 搬 `stdlib/` 进包 + 打包声明

**Files:**
- Move: `stdlib/` → `src/jikuai/stdlib/`
- Modify: `pyproject.toml`
- Create: `MANIFEST.in`

- [ ] **Step 1: 确认工作树干净，记录搬前文件数作为基线**

```powershell
git status --short
"文件数(不含 __pycache__)：" + (Get-ChildItem stdlib -Recurse -File | Where-Object { $_.FullName -notmatch '__pycache__' }).Count
```

- [ ] **Step 2: 先删掉 `stdlib/__pycache__`（不该随搬迁进包）**

```powershell
Get-ChildItem stdlib -Recurse -Directory -Filter __pycache__ | Remove-Item -Recurse -Force
```

- [ ] **Step 3: `git mv` 整个目录**

```powershell
git mv stdlib src/jikuai/stdlib
git status --short | Measure-Object -Line
```

Expected：全部是 `R`（rename）条目，数量等于 Step 1 的文件数。

- [ ] **Step 4: 检查空目录（zip 不保留空目录）**

```powershell
Get-ChildItem src/jikuai/stdlib -Recurse -Directory | Where-Object { (Get-ChildItem $_.FullName -File -Recurse).Count -eq 0 } | Select-Object FullName
Get-ChildItem src/jikuai/stdlib -Recurse -File -Filter ".gitkeep" | Select-Object FullName
```

若有 `.gitkeep` 占位的空目录：该目录内既然有 `.gitkeep` 这个文件，zip 就会保留该目录，**不需额外处理**；但要把 `.gitkeep` 写进 Step 5 的 package-data 白名单，否则目录会丢。若发现**没有任何文件的**空目录，记录下来在 Step 8 前搞清是否有代码依赖它存在。

- [ ] **Step 5: 加 package-data 声明**

Modify `pyproject.toml`，在 `[tool.setuptools.packages.find]` 之后插入：

```toml
# W114（v0.24.0 · ADR-39）：stdlib 是包内资源，必须显式列进 package-data。
# 这里不用 `include-package-data = true` + MANIFEST.in 的组合，是因为那条
# 路径把「wheel 里有什么」的真源分散到两个文件；显式白名单更好审。
# 扩展名白名单而非 `**/*`：明确排除 .pyc（stdlib 下有 .py，会生成 __pycache__）。
[tool.setuptools.package-data]
"jikuai" = [
    "stdlib/*.jk",
    "stdlib/*.py",
    "stdlib/*.txt",
    "stdlib/*.json",
    "stdlib/*.md",
    "stdlib/blocks/*.json",
    "stdlib/blocks/*.bin",
    "stdlib/blocks/.gitkeep",
    "stdlib/blocks/**/*.jk",
    "stdlib/blocks/**/*.json",
    "stdlib/blocks/**/*.py",
]
```

> **W114 白名单两处订正（Task 3 执行期实测）**：
> 1. **必须有 `stdlib/blocks/**/*.py`** —— `blocks/` 下有 14 个 ADR-16 §3.3 混合模块的
>    背衬 `.py`（如 `财务/保留分/保留分.py`，导出 `圆分`，被 13 个财务块引用，含 `个税`/
>    `增值税`）。漏了它们，装出来的 wheel 里旗舰块**运行期炸**，而原 G20 那 5 项验收
>    （3 个具名文件 + 块 json 数 + 无 pyc）**全会绿**——「守卫绿≠守卫在守」的同构复现。
> 2. **只需 `stdlib/blocks/*.bin` 一条**，不要 `**/*.bin`：全库唯一的 `向量索引.bin`
>    在 `blocks/` 直属层（`**` 不匹配零层目录，`**/*.bin` 今天匹配 0 个）。`.gitkeep`
>    同理修正为 `stdlib/blocks/.gitkeep`。

- [ ] **Step 6: 加 `MANIFEST.in`（保 sdist）**

Create `MANIFEST.in`：

```
# W114（v0.24.0 · ADR-39）：sdist 也必须带 stdlib。
# package-data 管 wheel；sdist 的收录规则不同，显式再声明一次。
recursive-include src/jikuai/stdlib *.jk *.py *.txt *.json *.md *.bin
recursive-include src/jikuai/stdlib .gitkeep
global-exclude *.pyc
global-exclude __pycache__/*
```

- [ ] **Step 7: 构建 wheel，核对内容**

```powershell
Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue
python -m build --wheel
$w = (Get-ChildItem dist\*.whl).FullName
Add-Type -AssemblyName System.IO.Compression.FileSystem
$z = [IO.Compression.ZipFile]::OpenRead($w); $names = $z.Entries | ForEach-Object { $_.FullName }; $z.Dispose()
"总条目=" + $names.Count
"stdlib 条目=" + ($names | Where-Object { $_ -like 'jikuai/stdlib/*' }).Count
"词典=" + ($names -contains 'jikuai/stdlib/分词词典.txt')
"向量索引=" + ($names -contains 'jikuai/stdlib/blocks/向量索引.bin')
"块索引=" + ($names -contains 'jikuai/stdlib/blocks/索引.json')
"块 json 数=" + ($names | Where-Object { $_ -like 'jikuai/stdlib/blocks/*/*/*.json' }).Count
"pyc 泄漏=" + ($names | Where-Object { $_ -like '*.pyc' }).Count
```

Expected：词典/向量索引/块索引三项为 `True`；块 json 数 ≥112；pyc 泄漏 = 0。若 `python -m build` 不可用先 `pip install build`。

- [ ] **Step 8: 提交**

```powershell
git add -A
git commit -m "refactor(v0.24)!: stdlib 搬进 src/jikuai/stdlib 并纳入 package-data · W114"
```

---

## Task 4: 6 处定位点改调 `resources`

一次改完再跑回归——这 6 处互相独立，但拆成 6 个提交会让中间态跑不起来（搬完目录后旧回溯已全部失效）。

**Files:**
- Modify: `src/jikuai/stdlib_contract.py:16-22`
- Modify: `src/jikuai/module_loader.py:147-149`
- Modify: `src/jikuai/evaluator.py:1638-1653`
- Modify: `src/jikuai/pkg/blocks.py:301-309`
- Modify: `src/jikuai/ai/retrieval.py:131-135` 与 `:673-677`

- [ ] **Step 1: 改 `stdlib_contract.py`**

把：

```python
def default_stdlib_dir() -> str:
    """返回仓库根 stdlib/ 绝对路径。

    与 module_loader._search_paths 同一算法：从 src/jikuai/ 上溯两级。
    """
    here = os.path.dirname(os.path.abspath(__file__))
    return os.path.normpath(os.path.join(here, '..', '..', 'stdlib'))
```

改为：

```python
def default_stdlib_dir() -> str:
    """返回包内 stdlib/ 绝对路径。

    W115（v0.24.0 · ADR-39）：改由 `resources.stdlib_dir()` 统一定位，
    不再自己回溯 `__file__`。
    """
    from . import resources
    return resources.stdlib_dir()
```

- [ ] **Step 2: 改 `module_loader.py`**

把 `_search_paths` 里的：

```python
        here = os.path.dirname(os.path.abspath(__file__))
        stdlib_dir = os.path.normpath(os.path.join(here, '..', '..', 'stdlib'))
        paths.append(stdlib_dir)
```

改为：

```python
        # W115（v0.24.0 · ADR-39）：stdlib 是包内资源，定位收敛到 resources。
        from . import resources
        paths.append(resources.stdlib_dir())
```

- [ ] **Step 3: 改 `evaluator.py` 的 `_load_stdlib_module`**

把：

```python
    import importlib.util
    # stdlib 目录：src/jikuai/../../stdlib
    here = os.path.dirname(os.path.abspath(__file__))
    stdlib_dir = os.path.join(here, '..', '..', 'stdlib')
    path = os.path.normpath(os.path.join(stdlib_dir, f'{name}.py'))
```

改为：

```python
    import importlib.util
    # W115（v0.24.0 · ADR-39）：stdlib 是包内资源，定位收敛到 resources。
    from . import resources
    path = resources.stdlib_path(f'{name}.py')
```

- [ ] **Step 4: 改 `pkg/blocks.py` 的 `blocks_root`**

把：

```python
def blocks_root() -> str:
    """返回内置 `stdlib/blocks/` 的绝对路径。

    定位方式与 `module_loader._search_paths()` 找 stdlib 的方式保持一致：
    从本文件位置上溯到仓库根（`src/jikuai/pkg` → 上三级）。
    """
    here = os.path.dirname(os.path.abspath(__file__))
    repo_root = os.path.normpath(os.path.join(here, '..', '..', '..'))
    return os.path.join(repo_root, 'stdlib', 'blocks')
```

改为：

```python
def blocks_root() -> str:
    """返回内置 `stdlib/blocks/` 的绝对路径。

    W115（v0.24.0 · ADR-39）：定位收敛到 `resources.blocks_dir()`。
    """
    from .. import resources
    return resources.blocks_dir()
```

- [ ] **Step 5: 改 `ai/retrieval.py` 两处**

`vector_index_path`：

```python
def vector_index_path() -> str:
    """返回 `stdlib/blocks/向量索引.bin` 的绝对路径。

    W115（v0.24.0 · ADR-39）：定位收敛到 resources。
    """
    from .. import resources
    return resources.stdlib_path('blocks', '向量索引.bin')
```

`_load_builtin_blocks` 的头三行：

```python
def _load_builtin_blocks() -> List[dict]:
    """加载 `stdlib/blocks/索引.json` 的内置块列表。"""
    from .. import resources
    idx_path = resources.stdlib_path('blocks', '索引.json')
    if not os.path.isfile(idx_path):
```

同时把该函数的告警文案里过时的建议改掉——`JIKUAI_PATH` 改不了内置根，`JIKUAI_STDLIB` 才行：

```python
        _log.warning(
            '块索引未找到：%s——内置块检索将为空。'
            '若 stdlib 不在包内默认位置，请设 JIKUAI_STDLIB 或显式传入 blocks 列表。',
            idx_path)
```

- [ ] **Step 6: 断言仓库里再无 stdlib 相关的 `__file__` 回溯**

```powershell
python -m pytest tests/test_resources.py -v
Select-String -Path src\jikuai\*.py,src\jikuai\**\*.py -Pattern "'\.\.',\s*'\.\.',\s*'stdlib'","'stdlib',\s*'blocks'" | Select-Object Path,LineNumber,Line
```

Expected：pytest 全绿；Select-String **零命中**（`retrieval.py` 里改后是 `stdlib_path('blocks', ...)`，不匹配上面两个模式；若仍有命中说明有漏改的点）。

- [ ] **Step 7: 确认 tools/ 侧对 retrieval 的导入方式没被相对导入打破**

`from .. import resources` 要求包上下文。`tools/ai-bridge/*.py` 若是以文件路径加载 `retrieval.py` 而非 `from jikuai.ai import retrieval`，这一步会炸。

```powershell
Select-String -Path tools\ai-bridge\*.py -Pattern "retrieval" | Select-Object Path,LineNumber,Line
python tools\ai-bridge\bench_retrieval.py
```

Expected：bench 能跑起来（不必关心指标数值，只看不 ImportError）。若报相对导入错误，把该处改成 `from jikuai.ai import retrieval` 形式的包内导入。

- [ ] **Step 8: 提交**

```powershell
git add -A
git commit -m "refactor(v0.24): 6 处 stdlib 定位点收敛到 resources · W115"
```

---

## Task 5: 全量回归 + G10–G19

**Files:** 无新增，只跑与修

- [ ] **Step 1: 跑全量回归，输出落临时文件（量大，会被截断）**

```powershell
$env:PYTHONIOENCODING = "utf-8"
New-Item -ItemType Directory -Force .comate | Out-Null
python -m pytest -q 2>&1 | Tee-Object -FilePath .comate\v024_regress.txt | Select-Object -Last 20
```

- [ ] **Step 2: 逐条修失败**

预期高发区（都因路径字面量）：`tests/test_v0_5_0_stdlib_contract.py`（24 处 stdlib 引用）、`tests/test_blocks_cli.py`（11 处）、`tests/test_v0_6_0_segmenter.py`（5 处）、`tests/test_pkg_block_install.py`（4 处）。修法一律是把硬编码的 `<repo>/stdlib` 改成 `jikuai.resources.stdlib_dir()`，**不是**改成新的字面路径——测试里再埋一份路径推法就是把双态搬进测试。

- [ ] **Step 3: 跑门禁**

```powershell
python scripts\check_stdlib_contract.py 2>&1 | Tee-Object -FilePath .comate\v024_gates.txt | Select-Object -Last 30
```

Expected：G10–G19 全绿。`scripts/` 下自己的 `__file__` 回溯按 spec 不改，但它们若回溯到旧 `stdlib/` 位置会红——那属于「门禁脚本自身定位 stdlib」，改成 `from jikuai import resources` 调用（脚本已把 `src/` 塞进 `sys.path`）。

- [ ] **Step 4: 复跑回归确认全绿，清理临时文件**

```powershell
python -m pytest -q 2>&1 | Select-Object -Last 5
Remove-Item .comate\v024_regress.txt,.comate\v024_gates.txt -ErrorAction SilentlyContinue
```

Expected：用例数不低于 v0.23.0 基线，0 failed。

- [ ] **Step 5: 提交**

```powershell
git add -A
git commit -m "test(v0.24): 回归与门禁适配包内 stdlib 布局 · W115"
```

---

## Task 6: G20 构建后门禁

**Files:**
- Create: `scripts/check_wheel_contents.py`
- Create: `tests/test_wheel_contents.py`
- Modify: `scripts/check_stdlib_contract.py`（注册 G20 到门禁清单输出）

- [ ] **Step 1: 写失败测试**

Create `tests/test_wheel_contents.py`：

```python
# -*- coding: utf-8 -*-
"""G20：wheel 内容断言（v0.24.0 W116）。

守的是 BACKLOG §10 那次**已经发生过**的事故：PyPI 0.4.1 的 wheel 里零个
stdlib 文件，装完 `导入 数学` 直接失败，而本机 editable 一切正常。
这条门禁必须真去构建 wheel 再解包看，不能只看 pyproject 声明——
「声明写了」和「文件真在包里」是两件事。
"""

import subprocess
import sys

import pytest

from scripts.check_wheel_contents import 必需条目, 校验wheel条目


def test_缺词典即判失败():
    条目 = ['jikuai/__init__.py', 'jikuai/stdlib/blocks/索引.json']
    问题 = 校验wheel条目(条目)
    assert any('分词词典.txt' in p for p in 问题)


def test_pyc泄漏即判失败():
    条目 = list(必需条目()) + ['jikuai/stdlib/__pycache__/分词.cpython-311.pyc']
    问题 = 校验wheel条目(条目)
    assert any('.pyc' in p for p in 问题)


def test_块json不足112即判失败():
    条目 = list(必需条目())
    问题 = 校验wheel条目(条目)
    assert any('112' in p for p in 问题)


def test_块背衬py缺失即判失败():
    """W114 那次差点漏掉的洞：14 个混合模块背衬 .py 全没进 wheel 时，
    原来的三项具名断言 + 块 json 计数**全会绿**。这条就是补那个洞的。"""
    条目 = [f'jikuai/stdlib/blocks/领域{i}/块{i}/块{i}.json' for i in range(120)]
    条目 += ['jikuai/stdlib/分词词典.txt',
             'jikuai/stdlib/blocks/向量索引.bin',
             'jikuai/stdlib/blocks/索引.json',
             'jikuai/stdlib/blocks/财务/保留分/保留分.py']
    问题 = 校验wheel条目(条目)
    # 只有 1 个背衬 .py（那个具名样本），期望 14
    assert any('块背衬 .py 有 1 个' in p for p in 问题)
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_wheel_contents.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'scripts.check_wheel_contents'`

- [ ] **Step 3: 写实现**

Create `scripts/check_wheel_contents.py`：

```python
# -*- coding: utf-8 -*-
"""G20 · wheel 内容门禁（v0.24.0 W116 · ADR-39 §5）。

用法：
    python scripts/check_wheel_contents.py            # 构建 wheel 再校验
    python scripts/check_wheel_contents.py <路径.whl>  # 校验已有 wheel

退出码 0 全绿 / 1 有问题。
"""

import os
import subprocess
import sys
import zipfile

#: 块元数据 json 的下界。112 是 v0.23.0 的块数；只设下界不设上界会重演
#: 「守卫绿≠守卫在守」，故同时设一个宽松上界哨。
块JSON下界 = 112
块JSON上界 = 500

#: 块背衬 `.py` 的精确条数（ADR-16 §3.3 混合模块）。W114 执行期实测 14 个。
#: **这条是本门禁存在的核心理由之一**：原验收线（3 个具名文件 + 块 json 数 +
#: 无 pyc）在漏掉全部 14 个背衬 .py 时会全绿，而 `圆分`（`财务/保留分/保留分.py`）
#: 被 13 个财务块引用（含 `个税`/`增值税`），漏了就是旗舰块运行期炸。
#: 用等值而非下界：块背衬数变了就该有人来改这个数并解释一句。
块背衬PY数 = 14


def 必需条目():
    """wheel 里必须出现的具名条目。"""
    return (
        'jikuai/stdlib/分词词典.txt',
        'jikuai/stdlib/blocks/向量索引.bin',
        'jikuai/stdlib/blocks/索引.json',
        # 混合模块背衬的代表样本，见 块背衬PY数 的注释
        'jikuai/stdlib/blocks/财务/保留分/保留分.py',
    )


def 校验wheel条目(条目):
    """对条目名列表做断言，返回问题描述列表（空 = 全绿）。"""
    问题 = []
    有 = set(条目)
    for 必需 in 必需条目():
        if 必需 not in 有:
            问题.append(f'wheel 里缺必需资源：{必需}')
    泄漏 = [n for n in 条目 if n.endswith('.pyc') or '__pycache__' in n]
    if 泄漏:
        问题.append(f'wheel 里泄漏了 .pyc / __pycache__（{len(泄漏)} 个），'
                    f'首个：{泄漏[0]}')
    块json = [n for n in 条目
              if n.startswith('jikuai/stdlib/blocks/')
              and n.endswith('.json')
              and n.count('/') >= 5]
    if len(块json) < 块JSON下界:
        问题.append(f'块元数据 json 只有 {len(块json)} 个，低于下界 {块JSON下界}')
    if len(块json) > 块JSON上界:
        问题.append(f'块元数据 json 有 {len(块json)} 个，超过上界哨 {块JSON上界}——'
                    f'块库真的长这么多了就上调这个数，别删这条哨')
    块py = [n for n in 条目
            if n.startswith('jikuai/stdlib/blocks/')
            and n.endswith('.py')
            and n.count('/') >= 5]
    if len(块py) != 块背衬PY数:
        问题.append(f'块背衬 .py 有 {len(块py)} 个，期望 {块背衬PY数} 个——'
                    f'漏了它们旗舰块会在运行期炸；块背衬真变了就改这个常量')
    return 问题


def 构建wheel(仓库根, 输出目录):
    """构建 wheel，返回文件路径。"""
    subprocess.run([sys.executable, '-m', 'build', '--wheel',
                    '--outdir', 输出目录, 仓库根], check=True)
    whls = [f for f in os.listdir(输出目录) if f.endswith('.whl')]
    if not whls:
        raise RuntimeError(f'构建后 {输出目录} 里没有 .whl')
    whls.sort()
    return os.path.join(输出目录, whls[-1])


def main(argv):
    if len(argv) > 1:
        wheel = argv[1]
    else:
        仓库根 = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
        wheel = 构建wheel(仓库根, os.path.join(仓库根, 'dist'))
    with zipfile.ZipFile(wheel) as z:
        条目 = z.namelist()
    问题 = 校验wheel条目(条目)
    if 问题:
        print(f'G20 wheel 内容门禁：失败（{wheel}）')
        for p in 问题:
            print(f'  - {p}')
        return 1
    print(f'G20 wheel 内容门禁：通过（{wheel}，{len(条目)} 个条目）')
    return 0


if __name__ == '__main__':
    sys.exit(main(sys.argv))
```

- [ ] **Step 4: 让 `scripts` 可被测试导入**

若 `python -m pytest tests/test_wheel_contents.py` 报找不到 `scripts` 包，在 `tests/conftest.py` 里把仓库根加入 `sys.path`（照该文件已有写法追加，不要新建机制）。

- [ ] **Step 5: 运行测试确认通过**

Run: `python -m pytest tests/test_wheel_contents.py -v`
Expected: PASS 3 项

- [ ] **Step 6: 真跑一次 G20**

Run: `python scripts\check_wheel_contents.py`
Expected: `G20 wheel 内容门禁：通过（...）`

- [ ] **Step 7: 注册进门禁清单**

Modify `scripts/check_stdlib_contract.py`：照该文件既有 G 编号的注册写法，加一条 G20，实现体委托给 `check_wheel_contents.main([])`。若该文件的门禁都是纯静态检查、跑 `build` 会显著拖慢，则**只在清单输出里列出 G20 并注明「由 `scripts/check_wheel_contents.py` 单独跑」**，不要把构建塞进静态门禁主流程。

- [ ] **Step 8: 提交**

```powershell
git add -A
git commit -m "feat(v0.24): G20 wheel 内容门禁 · W116"
```

---

## Task 7: 干净 venv 真机验收（本地 wheel）

这是唯一能抓住「本机 editable 恰好还能回溯到旧位置」这种假绿的一步。

**Files:** 无仓库改动，产物在 `$env:TEMP\jk_e2e`

- [ ] **Step 1: 构建并装进干净 venv**

```powershell
$e = "$env:TEMP\jk_e2e"
Remove-Item -Recurse -Force $e -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force $e | Out-Null
Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue
python -m build --wheel
python -m venv "$e\venv"
& "$e\venv\Scripts\pip.exe" install -q --no-deps (Get-ChildItem dist\*.whl).FullName
```

- [ ] **Step 2: 四条验收命令**

```powershell
$py = "$e\venv\Scripts\python.exe"
$jk = "$e\venv\Scripts\jk.exe"
Set-Content "$e\hello.jk" -Value '打印 "你好，极快"。' -Encoding UTF8
& $jk "$e\hello.jk"; "EXIT1=$LASTEXITCODE"
Set-Content "$e\seg.jk" -Value "从 分词 导入 分词。`n打印 分词 ""个人所得税起征点""。" -Encoding UTF8
& $jk "$e\seg.jk"; "EXIT2=$LASTEXITCODE"
& $jk 块 选 "月薪两万个税多少" --json; "EXIT3=$LASTEXITCODE"
& $jk 包 列表; "EXIT4=$LASTEXITCODE"
```

Expected：EXIT1–EXIT4 全为 0；第二条输出真的分词结果（不是逐字）；第三条 JSON 的 `候选` 非空且含 `个税`。

- [ ] **Step 3: 确认 stdlib 真在 site-packages 里、且没有回落到源码树**

```powershell
& $py -c "import jikuai, os; from jikuai import resources; print(resources.stdlib_dir()); print(os.path.isfile(resources.stdlib_path('分词词典.txt')))"
```

Expected：路径在 `...\site-packages\jikuai\stdlib`（**不含 `g:\jikuai`**）；`True`。

- [ ] **Step 4: 验证 `JIKUAI_STDLIB` 覆盖口真的生效**

```powershell
$env:JIKUAI_STDLIB = "g:\jikuai\src\jikuai\stdlib"
& $py -c "from jikuai import resources; print(resources.stdlib_dir())"
Remove-Item Env:\JIKUAI_STDLIB
```

Expected：打印仓库内路径。

- [ ] **Step 5: 清理**

```powershell
Remove-Item -Recurse -Force "$env:TEMP\jk_e2e"
```

- [ ] **Step 6: 把验收步骤落成可复跑脚本并提交**

Create `scripts/verify_wheel_e2e.ps1`，内容即 Step 1–4（参数化 wheel 路径），供 M31/M32 反复用。

```powershell
git add scripts/verify_wheel_e2e.ps1
git commit -m "chore(v0.24): 干净 venv wheel e2e 验收脚本 · W117"
```

---

## Task 8: lsp / dap 版本单一真源 + 依赖下界

**Files:**
- Modify: `lsp/pyproject.toml`
- Modify: `dap/pyproject.toml`
- Modify: `src/jikuai/_version.py:20`
- Modify: `scripts/check_stdlib_contract.py`（G15 投影 4 → 6）

- [ ] **Step 1: 主包版本号升到 0.24.0**

Modify `src/jikuai/_version.py`：`__version__ = "0.23.0"` → `__version__ = "0.24.0"`

- [ ] **Step 2: 同步 G15 已管的四处投影**

按 `_version.py` docstring 列的四处改：`pyproject.toml`（dynamic 引用，无需改字面量）、`CHANGELOG.md`（加 v0.24.0 段）、`editors/vscode/package.json`、`editors/vscode/CHANGELOG.md`。

```powershell
python scripts\check_stdlib_contract.py 2>&1 | Select-String "G15"
```

Expected：G15 绿。

- [ ] **Step 3: lsp/dap 改用动态版本 + 钉依赖下界**

Modify `lsp/pyproject.toml`：

```toml
[project]
name = "jikuai-lsp"
dynamic = ["version"]
description = "极快语言 LSP 服务器 - Language Server Protocol 实现（completion / hover / definition / executeCommand）"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    {name = "skywalk163"}
]
keywords = ["jikuai", "lsp", "language-server", "中文编程"]
# W118（v0.24.0）：**必须钉下界**。PyPI 上 0.4.1 及更早的 jikuai 是坏包
# （wheel 里零个 stdlib，装完 `导入 数学` 即失败，见 BACKLOG §10）。
# 无下界时解析器可能把它拽回来，与 yank 构成双保险。
dependencies = [
    "jikuai>=0.24.0",
]

[project.optional-dependencies]
pygls = ["pygls>=2.0"]
dev = ["pytest>=7.0"]

[project.scripts]
jikuai-lsp = "jikuai_lsp.server:main"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["jikuai_lsp*"]

# W118：版本接主包单一真源，不再手写字面量（此前写死 0.15.0，落后主包八个版本）。
[tool.setuptools.dynamic]
version = {attr = "jikuai_lsp._version.__version__"}
```

Create `lsp/jikuai_lsp/_version.py`：

```python
# -*- coding: utf-8 -*-
"""jikuai-lsp 版本号。

W118（v0.24.0）：与主包同号发布。此前这里写死在 `pyproject.toml` 里，
停在 0.15.0 而主包已 0.23.0——三包同仓同步发，版本各走各的只会让用户
猜「哪个 lsp 配哪个 jikuai」。G15 门禁把这里纳入投影校验。

刻意不 `from jikuai._version import __version__`：构建 lsp 的 wheel 时
主包不一定可导入，构建期不该有跨包运行时依赖。改由 G15 静态校验两处一致。
"""

__version__ = "0.24.0"

__all__ = ["__version__"]
```

`dap/pyproject.toml` 与 `dap/jikuai_dap/_version.py` 同构处理（name `jikuai-dap`、脚本 `jikuai-dap`、include `jikuai_dap*`）。

- [ ] **Step 4: G15 投影从 4 处扩到 6 处**

Modify `scripts/check_stdlib_contract.py` 的 G15 实现，把 `lsp/jikuai_lsp/_version.py` 与 `dap/jikuai_dap/_version.py` 加入投影清单，要求与 `src/jikuai/_version.py` 字面一致。同步更新 `src/jikuai/_version.py` 的 docstring（「四处投影」→「六处投影」）。

- [ ] **Step 5: 写投影一致性测试**

Modify `tests/test_version_consistency.py`，追加：

```python
def test_lsp与dap版本与主包一致():
    """W118（v0.24.0）：三包同号发布，G15 之外再留一个 pytest 兜底
    （G15 的历史教训是「跑门禁的人当场看不到红」）。"""
    import os
    根 = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

    def 读版本(相对路径):
        命名空间 = {}
        with open(os.path.join(根, 相对路径), encoding='utf-8') as f:
            exec(compile(f.read(), 相对路径, 'exec'), 命名空间)
        return 命名空间['__version__']

    主 = 读版本(os.path.join('src', 'jikuai', '_version.py'))
    assert 读版本(os.path.join('lsp', 'jikuai_lsp', '_version.py')) == 主
    assert 读版本(os.path.join('dap', 'jikuai_dap', '_version.py')) == 主
```

- [ ] **Step 6: 跑测试与门禁**

```powershell
python -m pytest tests/test_version_consistency.py -v
python scripts\check_stdlib_contract.py 2>&1 | Select-Object -Last 30
```

Expected：全绿，G15 报 6 处投影一致。

- [ ] **Step 7: 三包都能构建，且 lsp 装得起来**

```powershell
$e = "$env:TEMP\jk_three"
Remove-Item -Recurse -Force $e -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force dist,lsp\dist,dap\dist -ErrorAction SilentlyContinue
python -m build --wheel
python -m build --wheel --outdir lsp\dist lsp
python -m build --wheel --outdir dap\dist dap
python -m venv "$e\venv"
& "$e\venv\Scripts\pip.exe" install -q --no-deps (Get-ChildItem dist\*.whl).FullName (Get-ChildItem lsp\dist\*.whl).FullName
& "$e\venv\Scripts\python.exe" -c "import jikuai_lsp, jikuai; print(jikuai.__version__, jikuai_lsp._version.__version__)"
```

Expected：打印 `0.24.0 0.24.0`。

- [ ] **Step 8: LSP `initialize` 冒烟**

```powershell
$body = '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"processId":null,"rootUri":null,"capabilities":{}}}'
$msg = "Content-Length: $($body.Length)`r`n`r`n$body"
$msg | & "$e\venv\Scripts\python.exe" -m jikuai_lsp 2>&1 | Select-Object -First 5
Remove-Item -Recurse -Force $e
```

Expected：输出含 `"capabilities"` 的 JSON-RPC 响应。若 LSP 因等后续请求而挂住，加 `-TimeoutSec` 或改用仓库里已有的 LSP 测试（`tests/test_lsp_*.py`）当作这一步的替代证据，并在计划里记下替代原因。

- [ ] **Step 9: 提交**

```powershell
git add -A
git commit -m "feat(v0.24): 三包版号对齐 0.24.0 + lsp/dap 接单一真源与依赖下界 · W118"
```

---

## Task 9: 文档结账（ADR-39 / BACKLOG §10 / README / 路线图）

**Files:**
- Create: `docs/ADR-39-stdlib包内资源.md`
- Create: `docs/路线图-v0.24.md`
- Modify: `docs/BACKLOG.md`（§10 结账）
- Modify: `docs/ADR-16-标准库契约.md`（§3.4 标注被推翻）
- Modify: `README.md`

- [ ] **Step 1: 写 ADR-39**

Create `docs/ADR-39-stdlib包内资源.md`，必须覆盖：

- §1 背景：BACKLOG §10 + 实测证据（PyPI 0.4.1 wheel 19 条目零 stdlib；干净 venv `导入 数学` 报「找不到模块：数学」退出码 1）
- §2 裁决：stdlib 是**包内资源**，位于 `src/jikuai/stdlib/`；**推翻 ADR-16 §3.4 的「固定仓库根不可移动 + data-files」与 D-05**，并写明 data-files 为何不成立（装到 `sys.prefix`，与 `site-packages/jikuai/` 上溯 2–3 级对不上）
- §3 为何不用 `importlib.resources`（照本计划「修正 2」的理由）
- §4 单一入口纪律：`resources.py` 是唯一定位口，`JIKUAI_STDLIB` 是唯一覆盖口；新增 `__file__` 回溯视为违约
- §5 G20 门禁定义与两侧阈值（下界 112 / 上界哨 500）
- §6 不做的：`tools/` 不入包（`tools/web/server.py` 无鉴权 + 进程内执行提交代码 = RCE，入包等于给每个 pip 用户默认装一个；`tools/ai-bridge` sidecar 依赖隔离且神经收益在 v0.16 链式任务被 TF-IDF 反超 38pp 未证实）；`scripts/` 回溯不改；`blocks_cli.py:186` 属 tools/ 定位器不改
- §7 已知残留：`向量索引.bin` 跨平台性（见 Task 10 Step 1 的核实结果）

- [ ] **Step 2: BACKLOG §10 结账**

Modify `docs/BACKLOG.md` §10：整节改为「**已在 v0.24.0 清账**」，保留原因分析（供回溯），并**订正「7 处」为「6 处」**，注明 `blocks_cli.py:186` 是 tools/ 定位器的误归类。删掉「建议的修法与验收（真做时再细化，别现在动）」——它已经落地成 ADR-39。

这一步是 v0.19 复盘那条教训的直接应用：「实现完了忘了回头改 BACKLOG，下一轮开发者读 BACKLOG 建立错误前提」——**不许留陈账**。

- [ ] **Step 3: ADR-16 §3.4 标注被推翻**

Modify `docs/ADR-16-标准库契约.md`，在 §3.4 开头加一个醒目块：`> **本节已于 v0.24.0 被 ADR-39 推翻**`，并说明原方案不成立的原因与替代裁决位置。不要删除原文——决策史要留痕。

- [ ] **Step 4: 修 README**

Modify `README.md`：

- 「安装与使用」段把 `cd G:\jikuai` / `pip install -e .` 换成：

```bash
pip install jikuai
jk examples/hello.jk
jk              # 进入 REPL
```

  并在其后另起「从源码开发」小节保留 editable 装法。
- 「项目结构」树里 `stdlib/` 从顶层移到 `src/jikuai/stdlib/`，并把「块生态（108 块 ...）」的 108 订正为实际块数（以 `stdlib/blocks/索引.json` 为准）。
- v0.15.0 quickstart 段的 `$env:PYTHONPATH = "src;lsp"` 之外补一句 `pip install jikuai-lsp` 的装法。

- [ ] **Step 5: 写路线图 v0.24**

Create `docs/路线图-v0.24.md`，照 `docs/路线图-v0.23.md` 的结构（一、为什么是这个主轴 / 二、目标 / 三、里程碑 M30-M32 与 W113-W120 / 四、风险与边界 / 五、WBS 一览 / 六、实施结果）。主题一句话：**「把 `pip install jikuai` 从坏的修成能用的」**。

- [ ] **Step 6: 跑文档相关门禁与测试**

```powershell
python -m pytest tests/test_check_protocol_doc.py tests/test_check_pkg_doc.py -v
python scripts\check_stdlib_contract.py 2>&1 | Select-Object -Last 30
```

Expected：全绿。

- [ ] **Step 7: 提交**

```powershell
git add -A
git commit -m "docs(v0.24): ADR-39 立 + BACKLOG §10 结账 + README 安装段修正 · W119"
```

---

## Task 10: TestPyPI 预演

**不可逆边界提醒**：PyPI 文件名一旦用过永久不可复用。本任务必须全绿才允许进 Task 11。

**Files:** 无仓库改动（除 Step 1 的核实结论写进 ADR-39 §7）

- [ ] **Step 1: 核实 `向量索引.bin` 跨平台性**

```powershell
Select-String -Path src\jikuai\ai\retrieval.py -Pattern "struct|MAGIC|dtype|float32|byteorder|pack|unpack" | Select-Object LineNumber,Line
```

判据：写/读用显式字节序（如 `struct.pack('<f', ...)` 或 `'<%df'`）即平台无关，把结论写进 ADR-39 §7。若发现用了原生字节序（`'@'` / 无前缀）或 `array.tobytes()` 无字节序声明，**这是一个真实的跨平台缺陷**——记进 BACKLOG 并在 CHANGELOG 里声明「当前 wheel 的向量索引仅在小端平台可用」，不要默默发出去。

- [ ] **Step 2: 重建三个包的 wheel + sdist，跑 G20**

```powershell
Remove-Item -Recurse -Force dist,lsp\dist,dap\dist -ErrorAction SilentlyContinue
python -m build
python -m build --outdir lsp\dist lsp
python -m build --outdir dap\dist dap
python scripts\check_wheel_contents.py (Get-ChildItem dist\*.whl).FullName
```

Expected：G20 通过。

- [ ] **Step 3: 核对 sdist 也带 stdlib**

```powershell
$sd = (Get-ChildItem dist\*.tar.gz).FullName
tar -tzf $sd | Select-String "stdlib" | Measure-Object -Line
```

Expected：行数 ≥ 380。若为 0 说明 `MANIFEST.in` 没生效，回 Task 3 Step 6 修。

- [ ] **Step 4: `twine check`**

```powershell
pip install -q twine
python -m twine check dist\* lsp\dist\* dap\dist\*
```

Expected：全部 `PASSED`。

- [ ] **Step 5: 上传 TestPyPI**

```powershell
python -m twine upload --repository testpypi dist\* lsp\dist\* dap\dist\*
```

凭据用 TestPyPI 的独立 token（与正式 PyPI 不同账户体系，需单独注册）。**不要**在命令里内联 token；用 `.pypirc` 或 `TWINE_PASSWORD` 环境变量。

- [ ] **Step 6: 从 TestPyPI 装三个包并复跑 Task 7 的验收**

```powershell
$t = "$env:TEMP\jk_testpypi"
Remove-Item -Recurse -Force $t -ErrorAction SilentlyContinue
python -m venv "$t\venv"
& "$t\venv\Scripts\pip.exe" install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ jikuai==0.24.0 jikuai-lsp==0.24.0 jikuai-dap==0.24.0
powershell -File scripts\verify_wheel_e2e.ps1
```

`--extra-index-url` 是必需的：TestPyPI 上没有 `pytest` 之类的依赖。

Expected：Task 7 的四条命令 + LSP `initialize` 全绿。

- [ ] **Step 7: 清理，不提交（本任务无代码改动，除 Step 1 的 ADR 补写）**

```powershell
Remove-Item -Recurse -Force "$env:TEMP\jk_testpypi"
git status --short
```

若 Step 1 补写了 ADR-39 §7：

```powershell
git add docs/ADR-39-stdlib包内资源.md
git commit -m "docs(v0.24): ADR-39 §7 补向量索引跨平台核实结论 · W120"
```

---

## Task 11: 正式发布 + yank + 复验

**这一步不可逆。** 前提：Task 10 全绿。

- [ ] **Step 1: 打 tag**

```powershell
git status --short
python -m pytest -q 2>&1 | Select-Object -Last 3
git tag -a v0.24.0 -m "v0.24.0 · pip install jikuai 真的能用了"
```

- [ ] **Step 2: 上传正式 PyPI**

```powershell
python -m twine upload dist\* lsp\dist\* dap\dist\*
```

- [ ] **Step 3: 全新 venv 从正式 PyPI 复验**

```powershell
$f = "$env:TEMP\jk_prod"
Remove-Item -Recurse -Force $f -ErrorAction SilentlyContinue
python -m venv "$f\venv"
& "$f\venv\Scripts\pip.exe" install jikuai jikuai-lsp
& "$f\venv\Scripts\python.exe" -c "import jikuai; print(jikuai.__version__)"
powershell -File scripts\verify_wheel_e2e.ps1
```

Expected：版本为 `0.24.0`；四条命令全绿。**注意此处是不带版本号的 `pip install jikuai`——这正是 spec 的目标句。**

- [ ] **Step 4: yank 0.4.1 及更早所有版本**

先列出所有历史版本：

```powershell
(Invoke-WebRequest "https://pypi.org/pypi/jikuai/json" -UseBasicParsing).Content | ConvertFrom-Json | ForEach-Object { $_.releases.PSObject.Properties.Name } | Sort-Object
```

对每个 ≤0.4.1 的版本执行 yank。PyPI 的 yank 无 CLI，在 web 界面 `Manage project → Releases → <version> → Options → Yank` 操作，理由填：

```
wheel 不含 stdlib，装后标准库无法导入（见 GitHub docs/BACKLOG.md §10），请用 >=0.24.0
```

**yank 不删包**：已 pin 该版本的人不受影响；只是新的 `pip install jikuai` 不再解析到它们。可 unyank。

- [ ] **Step 5: 验证 yank 生效**

```powershell
$f2 = "$env:TEMP\jk_yank_check"
Remove-Item -Recurse -Force $f2 -ErrorAction SilentlyContinue
python -m venv "$f2\venv"
& "$f2\venv\Scripts\pip.exe" index versions jikuai
Remove-Item -Recurse -Force $f2,"$env:TEMP\jk_prod"
```

Expected：可用版本列表里不再出现 0.4.1 及更早。

- [ ] **Step 6: 推送与收尾**

```powershell
git push origin main
git push origin v0.24.0
```

（记忆里有一条：github 推送连续抖动要重试。失败就重跑，不要改成 force。）

- [ ] **Step 7: 回填路线图 §六 实施结果**

Modify `docs/路线图-v0.24.md` §六，记录：wheel 实际条目数与体积、回归用例数、与计划的偏离（至少两处已知：BACKLOG「7 处」订正为 6 处；`importlib.resources` 改用包内 `__file__`）、以及本轮暴露的新缺口。

```powershell
git add docs/路线图-v0.24.md
git commit -m "docs(v0.24): 回填 M30-M32 实施结果与偏离 · W120"
git push origin main
```

---

## 自审记录

**Spec 覆盖**：spec 范围内 7 条 → Task 3（stdlib 随包）、Task 2+4（单一入口）、Task 6（G20）、Task 8（三包版号/依赖）、Task 10+11（TestPyPI→正式）、Task 9 Step 4（README）、Task 11 Step 4（yank）。spec 六道硬闸 → Task 5（闸 1）、Task 7（闸 2）、Task 8 Step 8（闸 3）、Task 10 Step 6（闸 4）、Task 11 Step 3（闸 5）。spec 五条风险 → Task 1（中文名）、Task 3 Step 2/4（pycache 与空目录）、Task 4 Step 6 + Task 7 Step 3（漏改）、Task 10 Step 1（向量索引跨平台）、Task 11 Step 7（体积记录）。

**两处对 spec 的修正**已在开头单列，未静默偏离。

**留给执行者判断的两处**（不是占位符，是真实分支）：Task 6 Step 7（G20 进不进静态门禁主流程，取决于 `check_stdlib_contract.py` 是否纯静态）、Task 8 Step 8（LSP 冒烟若挂住则以既有 `tests/test_lsp_*.py` 替代并记录原因）。
