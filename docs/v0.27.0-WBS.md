# 极快 JiKuai v0.27.0 WBS · 规划器与 NL 层（M28）

- 起草：2026-08-18
- 周期：W146-W162（约 3 个月）
- 主题：自然语言问句 → `方案.json` 的规划器 + 回填校验器 + 两个缺口块
- 前置：v0.26.0（tag `v0.26.0`/`2371584`，gitea run 51/52 双绿，G10-G22 全绿，块库 137）
- 起点：main HEAD = `2371584`
- 关联：[`路线图-v0.26-v0.28.md`](路线图-v0.26-v0.28.md) §六 v0.27.0 段、
  [`ADR-41-规划器与NL层.md`](ADR-41-规划器与NL层.md)（**本轮架构正本**）、
  [`ADR-40-制造域数据模型.md`](ADR-40-制造域数据模型.md)（口径正本）、
  [`BACKLOG.md`](BACKLOG.md) §12.3（八条能力缺口）/§12.4（挂账）

**周次分配**：**清账与架构 3 周（W146-W148）/ 协议与粘合器治根 4 周（W149-W152）/
缺口块 3 周（W153-W155）/ 规划器 4 周（W156-W159）/ 评测门禁与收尾 3 周（W160-W162）**。

粘合器治根给 4 周的理由：`共享` 携带声明类型这一改动会穿透 `schema.py` → 协议正本 →
G16 → `glue.py` 消解逻辑 → `bench_glue.py` 指标，且**预期把指标做丑**（把静默错绑
换成显式拒绝）。这里要留足时间把「改前/改后两套数字」跑完并写清楚，而不是赶工回退。

## 本轮范围（已拍板，五个岔路）

1. **LLM 只选口径，不算数**。NL 理解交给 LLM，`组`/`跑` 仍是确定性的。
2. **双模式**：默认出**规划上下文包**（离线、确定性、CI 真跑）；`--模型` 可选接外部
   端点做端到端 demo。两条路径共用同一个回填校验器。
3. **范围 = 内核 + §12.3 高优两条**。图表块 / 运营日报 / 溯源面板 / 等价 SQL 导出 /
   数据质量检查 **推 v0.28.0**；§12.3 中优六条顺延。
4. **「实参写死」双管齐下**：规划器侧强制（校验器）+ 粘合器侧治根（`共享` 带类型）。
5. **缺口块出双块**：`窗间对比` + `基线偏离`，互相点名，沿用制造域双块范式。

## 本轮明确不做

- 语言级「表」一等类型（ADR-40 §3.3，留 v0.29.0 后另立 ADR）
- 把引擎层纳入类型图（`值类型: 任意` 的退化是已知代价，不假装它被守着）
- 真 Text-to-SQL（等价 SQL 导出在 v0.28.0，且只作「展示查询过程」）
- 在检索层做分数阈值拒答（**四轮实测已证伪，本轮 `retrieval.py` 一行不改**）
- 把规划器塞进 wheel（见执行原则 3）

**执行原则**

1. **拒答只做「词表覆盖判定」，不做分数阈值**。四轮实测已证伪基于分数的自动拒答
   （近边缘档各置信信号 AUC 仅 0.52-0.67，能拒掉 94% 无覆盖的阈值会误杀 64% 真命中）。
   规划器层的拒答判据是「语义层业务词是否登记 + 候选块是否自报对应口径」，与分数无关。
   **任何想复活分数阈值的改动，先去读 `bench_retrieval_reject.py` 的四轮结论。**
2. **指标变丑不许回退**。W152 预期让 `bench_glue.py` 的自动链式命中率下降——那是把
   静默错绑变成显式拒绝的**正确代价**。必须如实报改前/改后两套数字。
   v0.22 的主教训是「守卫绿 ≠ 守卫在守」，这一条是它的反面：**数字丑但真**，
   优于数字漂亮但错绑。
3. **规划器落 `tools/ai-bridge/planner.py`，不进 wheel**。理由不是偷懒：`glue.py`
   本来就在 `tools/ai-bridge/`、不进 wheel（`MANIFEST.in` 只
   `recursive-include src/jikuai/stdlib`），`blocks_cli._glue()` 找不到就抛
   `BlockError('组/跑 需要仓库内的 tools/ai-bridge/')`。规划器依赖 `组`/`跑`，
   **不可能比它的依赖可用性更强**。副作用：不动 `MANIFEST.in`、不动
   `块背衬PY数`、不碰 G20。
4. **LLM 不可复现 ⇒ 录像回放**。CI 在 FreeBSD、无外网、无 API key。真调模型只在本机
   人工跑并归档；CI 只回放 `tools/ai-bridge/规划录像/` 里录制的回填 JSON，跑
   「校验器 → 组 → 跑」。**端到端评测的确定性靠录像，不靠 mock 掉校验器。**
5. **新门禁必造反例**（沿用 v0.18.0 W55 / v0.19.0 W62 / v0.26.0 W144 规矩）：G23 上线
   必用 `tmp_path` 造反例逐一证明抓得到，不少于 3 类。只测正例等于没有门禁。
6. **协议字段只从 `schema.py` 常量取**。上下文包是新协议结构，字段名一律进常量，
   通道里出现手写字面量视为违约（W20 起 grep 把关）。新常量要同步
   `docs/协议-三通道.md`，否则 G16 双向一致会红。
7. **分配块名必须「目录名 + 导出名」两个都验**（v0.26.0 W140-W142 教训：三个块
   **全栽在目录名上**）。目录名进 `从 blocks.制造.<目录名> 导入 <导出名>。` 的
   点分路径段，必须过 `check_module_segment_atomicity`；导出名另过 G13 全局唯一 +
   `check_export_atomicity`。**验完再动手写文件。**
8. **加块必刷索引 + embeddings，加/减 sidecar `.py` 必同步 `块背衬PY数`**。后半句是
   v0.26.0 首推双红的直接原因（14 → 35 没同步，本机 G10-G22 全绿抓不到，因为 G20 要
   `python -m build`）。当前常量值 **35**，在 `scripts/check_wheel_contents.py:46`。
9. **留出档只跑一次、不看 miss 明细**。`评测集-chatbi-留出.json` 5 条与
   `评测集-留出.json` 25 条同一条纪律。规划器的端到端数字同样分档报，
   **只报调优集等于自欺**。

---

## 清账与架构（W146-W148）

### W146 · BACKLOG 订正 + 本轮立段 · **准入前置**

**目标**：把 §12.4 里已被 v0.26.0 收尾结清、却仍写成「高优待办」的条目订正，
避免照旧前提排期。

**动作**
- §12 导语「两处必须先看清的实况」→ 改为「已结清」，保留记录并写明结清依据
  （gitea run 51/52 双绿、`赛题/chatbi/` 已随 `e23bb33` 进仓）。
- §12.4 三条划掉并标已清：G22 从未在 CI 真跑 / `赛题/` 未跟踪 / W130-W145 未提交。
- 把「首推双红 = `块背衬PY数` 等值断言没同步」的教训从复盘搬进 §12 导语（它是
  长期纪律，不该只活在复盘里）。
- §12.4 保留**唯一未清的那条**：AGENTS.md 第四节被改动但未重跑三臂对照 → 到期 W161。

**DoD**
- BACKLOG §12 里不再有与 `git log` / gitea run 记录矛盾的陈述
- 未清挂账数 = 1，且带明确到期 W 号

### W147 · ADR-41 规划器与 NL 层架构定稿

**目标**：把五个已拍板岔路写成正本，后续 W 一律照它执行，不许边写边改架构。

**动作**
- §1 主张与边界：LLM 只选口径不算数；为什么不做真 Text-to-SQL。
- §2 双模式：`jk 块 规划`（上下文包）与 `jk 块 问 --模型`（端到端）的职责切分，
  以及**两条路径共用同一个校验器**这条硬约束。
- §3 规划上下文包结构（字段逐条定义 + 为什么现有「选响应」不够：**候选不带输入槽**，
  这是 LLM 写不出实参的直接根因）。
- §4 回填校验器五条硬规则 + 每条的反例形态。
- §5 拒答定位：为什么放规划器层而不是检索层（引四轮实测数字）。
- §6 `共享` 携带声明类型的改动面与**预期指标代价**（写明「指标变丑不回退」）。
- §7 规划器为什么不进 wheel（引 `_glue()` 的既有边界）。
- §8 录像回放为什么是端到端评测的唯一可行解。

**DoD**
- ADR-41 定稿，五个岔路各有「为什么这样定 / 另一条路为什么不走」
- §6 明确写出「若 `bench_glue.py` 数字下降，这是预期结果，不是回归」

### W148 · 规划上下文包字段常量入 `schema.py`

**目标**：新协议结构的字段名全部收进常量，成为唯一真源。

**动作**
- 新增常量组：上下文包信封、语义命中项、候选扩展项（`输入槽`/`输出类型`）、
  分歧告警项、回填契约、拒答建议、以及回填响应。
- 每组配 `make_*` 构造器 + `validate_*` 校验器，沿用既有 `_check_keys` 严格键集白名单
  （`schema.py:393-406`）。
- 追加进 `__all__`。

**DoD**
- 新常量与构造器全部有单测；严格键集反例（多一个键就报错）有断言
- `python -c "import jikuai.service.schema"` 零副作用

---

## 协议与粘合器治根（W149-W152）

### W149 · `共享` 增加可选键 `类型`

**目标**：让方案能声明共享常量的类型，为 W150 的治根提供输入。

**动作**
- `schema.py:450-457` 的 `共享` 键集当前**硬编码 `('名','值')` 且无 optional**——
  这是第一处拦路点，加 `类型` 会当场被判未知字段。改为
  required `('名','值')` + optional `('类型',)`。
- `类型` 的取值域必须落在 ADR-26 类型词表内，非法值报错而不是静默降级为 `任意`。
- **向后兼容**：不带 `类型` 的旧方案照旧可用（退 `任意`），已有 15 份赛题方案
  一份都不用改。

**DoD**
- 带 `类型` / 不带 `类型` / `类型` 非法 三种输入各有断言
- `赛题/chatbi/产出/` 那 15 份方案 `组`/`跑` 退出码仍全 0（**回归证据，不是口头保证**）

### W150 · 协议正本同步 + G16 双向一致

**目标**：新字段进 `docs/协议-三通道.md`，G16 不红。

**动作**
- 协议文档补 `共享[].类型` 与上下文包全套字段。
- 上下文包与回填响应属于新通道，确认 G16（`check_protocol_doc.py`，协议文档 ↔ Web
  路由双向一致）的检查范围是否需要扩到规划通道；若扩，反例测试同步补。
- `tools/web/server.py` 本轮**不新增路由**（演示端点安全化是 v0.28.0 W161 的事，
  现有那套无鉴权、`/api/跑` 等于任意代码执行，不许顺手挂新口）。

**DoD**
- `check_protocol_doc.py` 退出码 0；文档漏一个字段就红，有反例
- 确认 `tools/web/server.py` 零改动（`git diff` 为空）

### W151 · `glue.py` 用声明类型入池

**目标**：拆掉 `共享` 常量一律 `任意` 这个静默错绑的源头。

**动作**
- `glue.py:376-380`（`池.append((c['名'], ('任意', None)))`）改为读 `c.get('类型')`，
  有声明用声明、无声明才退 `任意`。
- `type_feeds`（`glue.py:225-265`，`236-238` 是 `任意` 双向放行那两行）**不动语义**——
  `任意` 仍是顶。改的是「不再无脑把常量标成 `任意`」，不是改类型格。

**DoD**
- 带 `类型` 的共享常量喂错形参 → 类型图拒绝，落 `?` 占位 + 拒绝理由
- Q_PUB_001 去掉全部 `参数` 的对照实验：改前静默错绑成 `读表(赵产量列)`，
  改后**必须落拒绝理由而不是错绑**（这条是本 W 的验收核心）

### W152 · 多候选竞争同一槽 → 落拒绝理由 + 指标如实报

**目标**：把「多个 `任意` 候选竞争同一槽时退回『最近产出优先』随便挑」这个行为
换成显式拒绝。

**动作**
- `TypeGraph.plan`（`glue.py:362-461`）的消解分支：当同一槽有 ≥2 个类型兼容候选、
  且**候选是靠 `任意` 放行进来的**，不再按「最近产出优先」挑，改写拒绝理由。
  槽名字面匹配唯一命中的情形照旧放行（那是有依据的，不是瞎猜）。
- 跑 `bench_glue.py` + `bench_glue_l3.py` **改前/改后各一遍**，两套数字并列写进
  ADR-41 §6 与本 W 的记录。
- 语义荒谬率仍须 = 0（这条是硬底线，不能因为拒绝变多就松）。

**DoD**
- 改前/改后两套数字齐，且**下降的项逐项写明原因**
- 新增反例测试：构造两个 `任意` 候选竞争同一槽，断言输出含拒绝理由而非任一候选
- 15 份赛题方案（实参全手写）**不受影响**，`组`/`跑` 退出码仍全 0

---

## 缺口块（W153-W155）

### W153 · 块名分配（目录名 + 导出名两道都验）· **动手写文件之前**

**目标**：先把名字验通过，再写实现。W140-W142 三个块全栽在目录名上，一个都没栽在
导出名上——这道必须前置。

**动作**
- 候选目录名 `窗间对比` / `基线偏离`，候选导出名 `窗比` / `基偏`。
- 逐字过 `check_module_segment_atomicity`（目录名）与 `check_export_atomicity` +
  G13 全局唯一（导出名）。**任一不过就换名，不许改门禁。**
- 特别小心的字：`间`、`对`、`比`、`线`、`偏`、`离` 是否落在 VERB / KEYWORD /
  ADVERB 词表里（`加`/`次`/`列`/`归` 都是这么栽的）。

**DoD**
- 两个目录名 + 两个导出名各有一条断言证明原子且唯一
- 若换名，换名理由与被否名字一并记档（下一轮不再踩）

### W154 · 块 `窗间对比`（导出 `窗比`）

**目标**：两个已切好窗口的表 → 逐组差值与变化率。**4 题踩到的缺口的一半。**

**动作**
- 输入：基期表、比期表、维度列、度量列。输出：逐组 `(维度键, 基期值, 比期值,
  差值, 变化率)`。
- 描述自报口径：「只比两窗差值，**不含基线偏离**」，并**点名 `基线偏离`**
  （G22 断言 3 的双块判法要求互相点名）。
- 描述含 G22 断言 2 关键词（`分母`/`汇总`/`加权`/`现成列`/`重算`/`窗口` 至少一项）。
- **空 ≠ 0**：基期缺某维度键时输出 `空` 而不是 0（沿用 `fact_orders.delay_days`
  57 空 vs 977 个 0 的教训）；分母为 0 时变化率出 `空`，不出 `inf`。
- 零第三方依赖，sidecar 走标准库。

**DoD**
- 单测覆盖：正常两窗、基期缺键、分母 0、维度列不存在、两表列名不一致
- 最小例上差值可手算核对（不是「跑通就算对」）

### W155 · 块 `基线偏离`（导出 `基偏`）+ 索引与 embeddings 重生成

**目标**：相对**自身**基线的升幅。**这是 Q_PUB_004 的正确答案。**

**动作**
- 输入：观察窗表、基线窗表、维度列、度量列。输出逐组升幅 %。
- 描述自报口径：「**相对自身基线**，不是横向比较」，并点名 `窗间对比`；另点名
  「绝对量榜与比率榜结论不同」这个坑（§12.3 第 5 条的最低成本缓解）。
- 用 Q_PUB_004 真数据立断言：**L002 从 151.801 → 191.095 = +25.9%**，
  其余七条在 −3.8%~+3.2%；横向水平降序榜首是 L006（256.505）而 L002 排第 4
  ——**两个榜不同不是 bug，立断言防后人「修正」它**。
- 重跑 `generate_block_index.py`（G11）与 `generate_embeddings.py`（G12，本机离线
  设 `HF_HUB_OFFLINE=1` `TRANSFORMERS_OFFLINE=1`）。
- 块库 137 → **139**；若新增 sidecar `.py`，同步 `块背衬PY数`（35 → 37）。

**DoD**
- G11/G12 绿；`check_stdlib_contract.py` 退出码 0
- `块背衬PY数` 与源码树实数一致（v0.26.0 补的 `test_块背衬PY数与源码树一致` 绿）
- Q_PUB_004 的 +25.9% 与「两个榜不同」各有断言

---

## 规划器（W156-W159）

### W156 · 上下文包构造器 `planner.build_context()`

**目标**：把「问句 + 检索 + 语义层 + 块元数据」组装成 LLM 能照着回填的受限结构。

**动作**
- 新建 `tools/ai-bridge/planner.py`；`blocks_cli._planner()` 仿 `_glue()`
  （`blocks_cli.py:190-213`）动态加载，同样在缺文件时抛可读的 `BlockError`。
- 候选**补输入槽名 + 槽类型 + 输出类型**（现有「选响应」候选没有，见 ADR-41 §3）。
- 语义命中：读 `src/jikuai/stdlib/blocks/制造/语义层.json` 的 42 条业务词。
- 已知缺口如实标注：`_DOMAIN_KEYWORDS`（`retrieval.py:355-391`）**没有「制造」域**，
  制造块拿不到领域先验加分。本轮**不改检索器**，但上下文包要把这条写进
  `拒答建议.理由` 供人复核，不许假装召回是干净的。

**DoD**
- 15 条调优 + 5 条留出问句各能出一份合法上下文包
- 上下文包字段全部来自 `schema.py` 常量（grep 无手写字面量）

### W157 · 回填校验器 `planner.validate_filled()`

**目标**：把「实参必须写死」变成协议级强制，比 `?` 占位更早一道闸。

**动作** —— 五条硬规则，任一不过就拒并给可读理由：
1. 每步 `参数` **必填**，且长度等于该块输入槽数。缺一个就拒。
2. 块 / 领域 / 导出名必须来自上下文包 `候选`（白名单，防幻觉块名）。
3. 分歧点两侧同时出现 → 拒；问句命中该口径而一侧都没选 → 拒。
4. 产物必过 `schema.validate_plan`。
5. `--严格` 下：无声明类型的共享常量不许喂精确形参（配合 W151）。

**DoD**
- **五条各有独立反例测试**（缺参数 / 幻觉块名 / 双侧口径 / 协议多余字段 /
  裸 `任意` 喂精确槽），共 ≥5 类反例
- 拒绝理由是**可操作的**（说清缺哪个槽、该填什么类型），不是「校验失败」

### W158 · CLI `jk 块 规划` 与 `jk 块 问`

**目标**：两条命令落地，退出码守既有约定（`0` 成功 / `1` 输入错误 / `2` 执行期报错）。

**动作**
- `_ALIASES`（`blocks_cli.py:78-90`）加 `规划`/`问` + 英文别名；`_DISPATCH`
  （`1181-1191`）加两个 `_cmd_*`；新函数签名 `List[str] -> int`。
- `jk 块 规划 "问句" --json` → 上下文包，纯离线。
- `jk 块 问 "问句" --模型 <端点>` → 上下文包 → 端点 → 回填 → 校验器 → 组 → 跑。
  **不给 `--模型` 退 1 并指向 `规划`**，不静默降级。
- `--模型` 的端点从环境变量取 key，**不接受命令行传 key**（会进 shell history）。
  端点响应一律当**不可信输入**过校验器，不许旁路。
- 同步 `_USAGE`（`43-75`）与 `main.py:104-105` 的顶层 usage——后者已经漏了
  `组`/`跑`/`新建`，本轮顺手补齐。

**DoD**
- 两条命令的正常路径 + 未知选项 + 缺 `--模型` 各有断言
- `jk 块 问` 在端点返回垃圾 JSON 时退 1 并给可读理由，不抛裸异常

**W158 落地口径（2026-08-19）**
- `_glue()` 与 `_planner()` 抽出共用的 `_按路径加载(文件名, 模块名, 用途)`——importlib
  那套 dance 抄第二遍就该提取，两处的错误文案也因此同源。
- 端点契约：**POST 上下文包 JSON → 回填响应 JSON**。scheme 走白名单（只收
  `http`/`https`）；`file:`/`data:` 交给 `urlopen` 会去读本机文件，那不是「调模型」。
- 拒答判定为库外时，`规划` 退 0（判定是包里的一个字段，不是命令失败），
  `问` 退 1 且**在调端点之前就停**——省掉那次推理，也不给模型一个注定要编的上下文。
- 真踩到的一处：URL 里带中文路径 / token 里带中文，`http.client` 抛的是
  `UnicodeEncodeError`（请求行与头值必须 ASCII，CVE-2019-9740 的防线），它**不是**
  `OSError`。只捕 `OSError` 会让用户吃一个裸 traceback，故一并捕 `ValueError`。
- 端点用**真的**本机 HTTP server 测（`127.0.0.1:0`），不 mock `_请求回填`：协议白名单、
  鉴权头、垃圾响应处理都在那个函数里，mock 掉等于把要测的东西测没了。

### W159 · 拒答与分歧告警

**目标**：规划器层的拒答落地，分档报数。

**动作**
- 拒答判据 = **词表覆盖判定**：语义层业务词未登记 **且** 候选块无一自报对应口径 → 拒。
  **不引入任何分数阈值**（执行原则 1）。
- 近边缘档**不自动拒**：出 `分歧告警`（两侧块名 + 实测差值）逼 LLM 显式选一条，
  选不出才拒。**要拿缺陷率那 57% 去讲**（2026-06 先汇总后相除 0.050550 vs 行级比率
  平均 0.032218）——达成率那处只差万分之几，讲口径分歧会讲得很弱。
- 远离档 10 条 / 近边缘档 8 条**分档报数**，与词面路线的拒答率 0.0% 并列对照。

**DoD**
- 两档数字齐，且与 `bench_chatbi.py` 的检索层 0.0% 并列（**不许拿规划器的数字去
  掩盖检索器的 0.0%**——它们量的是不同层）
- 近边缘档的兄弟块诱骗率 0.625 有对照数字（规划器层能拦下多少）

**W159 落地口径（2026-08-19）**
- 判据本体（词表覆盖 → `拒答建议`、触发词 → `分歧告警`）在 W156 的 `build_context`
  里就落了；本轮补的是**CLI 这一层的呈现**：四段分级报告（拒答判定 → 口径分歧 →
  语义命中 → 候选），按「拿到包之后该先看什么」排序，拒答排第一段。
- 分级报告刻意**不打 `回填契约`**：那段是给 LLM 读的长文，人读模式打出来会把前三段
  冲掉。要看它就 `--json`。
- **两档分档数字尚未产出**：它要一支能批量跑负例集的 bench，那正是 W160 的
  `bench_planner.py`。数字随 W160 一起报，不在本轮拍脑袋填——「守卫绿≠守卫在守」
  同理，报告能打印不等于拒答率被量过。

### W159 遗留 → W160 · **已在 W160 结清（2026-08-19）**

- 远离档 10 条 / 近边缘档 8 条的分档拒答率，与检索层 0.0% 并列对照。
- 近边缘档兄弟块诱骗率 0.625 的规划器层对照数字。

数字见下面 W160 段的「分档拒答率与兄弟块诱骗率」。**结论一句话**：规划器层的词表
覆盖判定把远离档从 0.0% 拉到 **50.0%**，对近边缘档**一点用都没有**（0.0%，兄弟块
诱骗率 62.5% 与检索层逐位相同）。这不是实现没做好，是判据本身的边界——见下。



---

## 评测、门禁与收尾（W160-W162）

### W160 · 规划录像 + `bench_planner.py`

**目标**：给端到端评测一个确定性、离线、可进 CI 的形态。

**动作**
- `tools/ai-bridge/规划录像/`：录制每条问句的回填 JSON（含模型标识与时间戳）。
- `bench_planner.py`：量「上下文包是否含全部期望块」「校验器拒答率」
  「端到端结果 vs `期望结果`」。`期望结果` 字段此前闲置，本轮启用。
- `bench_chatbi.py` 的四档检索指标**原样保留不动**（保跨版本可比性）。
  已知缺口照旧如实报：**完整命中率恒 0** 的根因是每条 `期望` 都含 `表载入` 而问句
  从不提「载入」，TF-IDF 抓不到——**不许为了把这个数做上去而从期望里删掉 `表载入`**。
- CI 只跑回放（离线确定性）；真调模型只在本机人工跑，结果单独归档。

**DoD**
- 回放路径零网络、零 API key，在 FreeBSD CI 上真跑
- 录像与真调模型的结果差异（若有）如实记，不掩盖

**W160 落地口径（2026-08-19）**

*录像来源*：`tools/ai-bridge/规划录像/` 15 份，转录自 `赛题/chatbi/产出/` 的 W145
手写方案，`模型` 字段如实写 **`人工·v0.26.0 W145`**——本仓库至今**没有一份真调模型
的回填**，别把它当 LLM 产出读。录像文件是**纯回填响应信封**（`{需求,方案,模型,溯源?}`，
多一个键就过不了 `validate_filled_envelope`），`id`/`录制时间`/`来源` 这些元信息落
`清单.json`。`赛题/` 下的原件**一个字没动**，那是 W145 的归档件。

*回放绿的判据不是「全部通过」*：清单给每条录像登记 `期望判定`（`通过`/`拒答`）与
`期望拒因`，绿 = **实际判定与登记逐条相符**。15 份里 **4 份该被拒且真被拒**
（`Q_PUB_004` / `Q_PUB_005` / `Q_HID_003` / `Q_HID_005`），拒得对：

- W145 刻意把 `单车电耗现成`/`单车电耗重算`（`达成率权重`/`达成率均值`）两侧口径
  **并排跑**当人工交叉校验——对人是好报表，对协议是「两个不同的答案」（W157 规则 3）。
- `窗间对比`/`基线偏离` 是 **W154/W155 才有的块**，W145 无从选起，只能在 `说明` 里
  写「这一步的相除要人来做」。三条命中 §5.4 参照系分歧点的录像因此全被拒。

**这 4 条是真口径债，不是台架噪声**：`赛题/chatbi/产出/README.md` 早就记了
`Q_PUB_004`/`Q_PUB_007` 在「水平榜 vs 基线偏离」上与 reference 判读不一致，校验器
现在把同一件事抓成了协议级拒绝。把判定登记进清单而不是改录像去凑绿，是为了让它
**继续咬人**：哪天补了环比块或改了规则 3，`bench_planner.py` 当场红。

*回放为什么用全库当白名单*：规则 2 要求块名来自上下文包 `候选`，而实测正确方案用到
的块在 TF-IDF 排名里**最深落到第 89 名**（`Q_HID_004` 的 `表载入`）——默认 `top=8`
下白名单**装不下任何一份**正确方案。所以「白名单装不装得下」单独量成
`白名单可承载率` 按 K 分档报，回放本身用 `--回放top`（缺省 = 块库全量 139）把规则 2
从瓶颈位置挪开。两个数字在同一张表上，没有藏起来的那一半。

*上下文包候选能不能装下正确方案*（块库 139 · TF-IDF 启发式）：

- 调优档 15 条：K=8 → 期望覆盖 **20.7%** / 期望全中 **0.0%** / 白名单可承载 **0.0%**；
  K=20 → 50.6% / 0.0% / 0.0%；K=40 → **80.6% / 53.3% / 30.0%**
- 留出档 5 条：K=8 → 25.4% / 0.0% / 0.0%；K=20 → 41.8% / 0.0% / 0.0%；
  K=40 → 72.8% / 20.0% / 20.0%

根因照旧是 `表载入`（每条链路都要它，问句从不提「载入」）——**不许为了把数字做上去
从期望里删掉它**（W160 动作里已写明）。新发现的是 `窗口`/`连接`/`选取` 也排得很深
（51/79/87 名），它们同样是「问句不会提的结构性步骤」。

*分档拒答率与兄弟块诱骗率*（规划器层，top=8，与检索层并列）：

- 远离档 10 条：规划器层拒答率 **50.0%**（拒下 5 条） ← 检索层 **0.0%**
- 近边缘档 8 条：规划器层拒答率 **0.0%**（拒下 0 条） ← 检索层 **0.0%**
- 近边缘档兄弟块诱骗率：规划器层 **62.5%**（5/8） ← 检索层 **62.5%**，**逐位相同**

**这三个数字要一起读**。词表覆盖判定按**字面子串**匹配业务词：远离档漏掉的 5 条全是
问句里蹭到一个制造业务词的（「预测明年各**车型**的销量」蹭 `车型`、「给我画个各产线
**产量**的折线图」蹭 `产量`），判据看不出「预测/画图」这层动作。近边缘档更彻底——
`中位达成率` 里 `达成率` 就在词表里、`单车水耗` 里 `水耗` 就在词表里，覆盖判定**必然**
判 True。所以：

- **拒答判据能拦「整类能力不在库里」，拦不住「口径差一档」**。后者只能靠 `分歧告警`
  逼 LLM 显式选一条（近边缘档 2 条带告警、远离档 1 条），以及 AGENTS.md §四那句
  「判断权在你」。这条边界必须写进 W161 的 AGENTS.md 改动里，不许含糊成「规划器会拒答」。
- **规划器层的 50.0% 不改变检索层仍是 0.0%**。两层量的是不同的东西。

*端到端 vs `期望结果`*：11 份校验通过的录像**全部**组无占位、跑无错误（`组失败 0`）。
`关键实体命中率 75.0%`（8 份录像的 `期望结果` 里有 `M003`/`L002`/`C005` 这类机读 ID，
6 份的 ID 全出现在 stdout 里）。`期望结果` 的口径散文（「先汇总后相除」「夜班明显低于
白班」）**不做自动判定**——写清楚「这部分要人读」比造一个假装量过了的数字强。

*一个真踩到的坑*：`bench_chatbi.py` 那套 `_reconfigure_utf8()` 在**模块导入期**换掉
`sys.stdout`/`sys.stderr`。本脚本要被测试直接导入，照抄会把 pytest 的 capture tmpfile
关掉，整轮 pytest 在 teardown 抛 `ValueError: I/O operation on closed file`、**一个
测试都跑不了**。故这一步下沉到 `if __name__ == '__main__'`；同理 `run()` 里为读赛题
CSV 而 `chdir` 到仓库根，必须在 `finally` 里还原。

### W160 遗留 → W161

- 4 份录像暴露的口径债（W145 方案并排跑两侧口径 / 没有 §5.4 参照系块可选）要不要
  在 v0.27.0 内补一份「v0.27.0 口径」的新录像，还是挂到 v0.28.0。**默认挂账**：改录像
  等于替 W145 重做四道题的口径拍板，那是独立的一份工作量，不该塞进收尾周。
- `bench_planner.py` 目前**没进 CI**。W161 的 G23 要用 `--只回放 --门禁` 串进
  `check_stdlib_contract.py`，并按执行原则 5 造 ≥3 类反例（篡改录像方案的块名 /
  删掉某步 `参数` / 改清单的 `期望判定`）。


### W161 · G23 规划器契约门禁 + AGENTS.md 与三臂对照

**目标**：新门禁上线并造反例；把 v0.26.0 挂的那笔三臂对照账一起还掉。

**动作**
- `scripts/check_planner_contract.py`（G23），串进
  `check_stdlib_contract.py`：上下文包字段只从 schema 常量取 / 候选白名单校验在位 /
  「`参数` 缺失即拒」有反例 / 录像回放全绿。
- 反例 ≥3 类，用 `tmp_path` 造（执行原则 5）。
- AGENTS.md 改动：第一节块数 137 → **139**；第二节补 `规划`/`问` 两条命令；
  第四节补规划器层拒答的定位（**并明确写「检索层仍是 0.0%，规划器的拒答不改变
  这一点」**）。
- **重跑 `tools/ai-bridge/agents-md-ab/` 三臂对照**（`构建判分.py 构建|判分`）：
  本轮真动了第四节的指导语句，按 AGENTS.md §六规矩必须重跑，**且把 v0.26.0
  「只改事实数字故不重跑」那笔判断一并复核**。需人在仓外空目录开三个独立会话跑。

**DoD**
- G23 在**本机绿 + gitea CI 上亲眼绿过一次**（v0.25.0 W129 / v0.26.0 W144 的教训：
  新门禁没在 CI 真跑过就不算落地）
- 三臂对照有新数字；若结论与旧结论不同，**照新数字改 AGENTS.md**
- BACKLOG §12.4 唯一未清挂账清零

**W161 落地口径（2026-08-19）**
- `scripts/check_planner_contract.py`（G23）已串进 `check_stdlib_contract.py`
  （紧跟 G22），四条断言：
  1. **协议字段名只从 schema 常量取** —— AST 扫 `planner.py` 的**键位**字面量
     （下标 / `.get()` 首参 / 字典键 / `in` 左侧），命中 `schema` 协议字段全集即违约；
     另断言上下文包 / 候选 / 回填信封九组字段集与 ADR-41 §3 冻结值严格相等。
  2. **五条硬规则长在原处** —— 七个必备函数在位、`validate_filled` 真调
     `_规则1与2/_规则3/_规则5/validate_filled_envelope`、`_候选索引` 仍是三元键。
  3. **行为断言（守卫真在守）** —— 合成一份不依赖块库/数据集的上下文包，跑六个场景
     （正常 / 缺 `参数` / 幻觉块名 / `参数` 长度错 / 分歧点两侧都没选 / 两侧同时出现）
     + 多余键。这一条是 v0.22.0 主教训「守卫绿≠守卫在守」的直接落地：静态断言看不见
     「函数还在但被 `return []` 架空」，行为断言看得见。
  4. **录像回放全绿** —— 子进程跑 `bench_planner.py --只回放 --门禁`。
- **静态 + 行为两套都要**：只静态时注释掉规则循环体照样绿；只行为时把校验器改成
  「凡不认识的键就拒」也能六场景全过而规则 1 已没了。两套一起才卡得住。
- **键位而非全字符串**：`main()` 里 `add_argument('需求')` 是 argparse 位置参数名，
  与协议字段撞名却非协议使用，按全字符串扫会假红。豁免 `_I*/_Y*/_S*/分歧点表` 及其
  循环变量（`for 处 in 分歧点表` 的 `处['名']` 读的是那张表自己的键）。
- **G23 慢在第 4 条**（本机 ≈ 8.5s：15 份录像逐份过校验器→组→跑 + 读 8 张 CSV）。
  刻意仍串主流程而不学 G20 只打提示——录像回放不进 CI 等于没有。录像 / `赛题/`
  数据集不在场时显式打「跳过第 4 条」，不静默当通过。
- 反例 5 类（`tests/test_v0_27_0_w161_G23规划器契约.py`，14 passed）：录像侧 3 类
  （篡改块名 / 删 `参数` / 改清单 `期望判定`，用 `copytree` + `--录像目录` 指向
  tmp）、规划器侧 2+ 类（植入裸字面量键 / 摘 `_规则3` 调用 / 校验器恒放行 /
  白名单退化成单键，用 `copytree` 整棵桥目录 + `--规划器` + `--跳过回放`）。
- AGENTS.md 改动已落：块数 137→**139**（G15 六处投影在 W162 收）、第二节补 `规划`/`问`
  两条命令与回填契约、第四节新增「规划器层有个拒答判据但拦的不是你以为的那件事」
  （远离 50% / 近边缘 0% / 兄弟诱骗 62.5% 与检索层同数，**明确写检索层仍 0.0%、
  规划器拒答不改变这一点**）、第六节 G10-G22→**G10-G23**。
- gitea `ci.yml` / `release.yml` 的门禁步骤名 G10-G22 / G10-G19 → **G10-G23**。

### W161 遗留 → W162 / v0.28.0
- **三臂对照（`agents-md-ab/`）尚未重跑**：本轮真动了 AGENTS.md 第四节（新增规划器层
  拒答那段）+ 第二节，按 §六规矩必须重跑，且要复核 v0.26.0「只改事实数字故不重跑」
  那笔判断。**做不成 CI**（要人在仓外空目录开三个独立会话），挂到 W162 的人工步骤，
  BACKLOG §12.4 在三臂跑完前不清零。


### W162 · 版本号投影、CHANGELOG、全量回归、复盘

**动作**
- 版本号 0.27.0 推**六处投影**（G15 守，`check_stdlib_contract.py:403-430`）。
- CHANGELOG **双份**（仓库根 + vscode 扩展）。
- 全量回归 `python -m pytest`；`check_stdlib_contract.py` **G10-G23 全绿、退出码 0**。
- `python -m build` 三包 + G20 `check_wheel_contents.py` + G21
  `check_dist_metadata.py --要求成对`。
- 写 `docs/v0.27-复盘.md`：被证实 / 被证伪的判断、错估的估算、必记单点。
- 三家远端（gitea / gitcode / github）main + tag 对齐。**github 连接抖动严重**
  （`Empty reply from server` / `SSL/TLS handshake failed`），`git ls-remote` 一通
  就立刻补推比死等重试有效；**tag/commit 消息一律走 `-F 文件`**，PowerShell 里
  内联中文会被按 GBK 编坏。

**DoD**
- G10-G23 全绿；回归零 fail
- 成功度量（路线图 §九 第 2 条）：**5 道留出题在没看过 miss 明细的前提下答对**，
  且每个数字可回溯到「块名 + 版本 + 口径声明 + 生成的 `.jk` 源码」
- 复盘里**至少写清一条被证伪的判断**（v0.23/v0.26 都是靠这个避免下一轮重踩）

**W162 落地口径（2026-08-19）**
- 版本号 0.27.0 六处投影已推（`_version.py` 三份 + `package.json` + 双 CHANGELOG），
  G15 绿。
- 双 CHANGELOG 已写；扩展侧无代码改动（规划器在 `tools/`，不进 wheel、不经 LSP）。
- `docs/v0.27-复盘.md` 已写，四条被证伪/修正的判断，主条是**「规划器层的拒答能挡住
  近边缘」被证伪**（近边缘 0.0%、兄弟诱骗 62.5% 与检索层逐位相同）。
- **成功度量 3/5 而不是 5/5，如实记**：`Q_HID_003`/`Q_HID_005` 被规则 3 拒，因为两条都
  命中 §5.4 参照系分歧点而方案没选边。**根因是录像陈旧不是能力缺失**——§5.4 的
  `窗间对比`/`基线偏离` 在本轮 W153-W155 才落地，录像却是 W145 转录的。修法是补一条
  `模型: 人工·v0.27.0 口径` 的新录像，旧录像原样留着。**本轮不做**：把陈旧录像改成能过，
  与「为让指标好看而改期望」只隔一层自我说服，留着 3/5 更有用。挂 v0.28.0。
- **三臂对照本轮不跑**（判断，非遗忘）：新增段是防误读注解 + `bench_planner` 已跑出的
  确定性数字，不是说服性选块指导。降级挂 v0.28.0，并入条件写在 BACKLOG §12.4，
  AGENTS.md §六 也记了这条例外，不悄悄绕过。
- **G23 在 gitea CI 上是否真绿过一次：待人核对**。W161 的 push 已触发 run，但本轮没在
  CI 页面确认。这正是 v0.25.0 W129 那笔账的形状，复盘 §七 按未完成记。


---

## 相关文档

- 本轮架构正本：[`ADR-41-规划器与NL层.md`](ADR-41-规划器与NL层.md)
- 口径正本：[`ADR-40-制造域数据模型.md`](ADR-40-制造域数据模型.md)
- 三版切分：[`路线图-v0.26-v0.28.md`](路线图-v0.26-v0.28.md)
- 待办真源：[`BACKLOG.md`](BACKLOG.md) §12.3 / §12.4
- 上一轮怎么收的：[`v0.26-复盘.md`](v0.26-复盘.md)、[`v0.26.0-WBS.md`](v0.26.0-WBS.md)
- 槽绑定歧义消解（W151/W152 前置）：[`ADR-30-槽绑定歧义消解.md`](ADR-30-槽绑定歧义消解.md)
- 类型词表（W149 的 `类型` 取值域）：[`ADR-26-类型词表规范.md`](ADR-26-类型词表规范.md)
