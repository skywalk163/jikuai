-- 示例：中文文本规范化
-- 演示：简繁转换 + 分词的块组合管道
-- 依赖块：
--   blocks.中文.简繁包装（在建，M2 交付，导出 `转简`）
--   blocks.中文.分词包装（在建，M2 交付，导出 `切词`）
-- 预期输出（待对应块落地后启用）：
--   ["中国", "程序员", "的", "中文", "编程", "语言"]

-- ====== 待对应块落地后启用 ======
-- 从 blocks.中文.简繁包装 导入 转简。
-- 从 blocks.中文.分词包装 导入 切词。
--
-- -- 先将繁体转为简体，再分词
-- 定义赵文本="中國程序員的中文編程語言"。
-- 打印 转简(赵文本)，切词。
--
-- -- 等价写法：
-- 定义赵简体=转简(赵文本)。
-- 打印 切词(赵简体)。

-- 当前无可运行的替代，仅作文档记录
打印 "（本示例待 blocks.中文 领域块落地后启用）"。
