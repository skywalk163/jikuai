-- 示例：函数封装块调用
-- 演示：将块调用封装进自定义函数，展示块作为"零件"的组合方式
-- 依赖块：blocks.数据.求和（已存在，导出 `汇总`）
-- 预期输出：
--   6.0
--   5.5

从 blocks.数据.求和 导入 汇总。

-- 封装：求平均值（利用汇总 + 长度）
函数 赵均值 接收 赵数值：
  定义赵总=汇总(赵数值)。
  定义赵个=长度 赵数值。
  返回 赵总 除 赵个。
。

打印 赵均值(列 2 4 6 8 10)。
打印 赵均值(列 1 10)。
