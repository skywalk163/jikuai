-- 示例：批量统计管道
-- 演示：读取文件 → 清洗 → 求和的完整数据管道
-- 依赖块：
--   blocks.数据.求和（已存在，导出 `汇总`）
--   blocks.数据.清洗（在建，M2 交付，导出 `规整`）
--   blocks.数据.读取文件（在建，M2 交付，导出 `装载`）
-- 预期输出（待对应块落地后启用）：
--   （取决于数据文件内容）

-- ====== 待对应块落地后启用 ======
-- 从 blocks.数据.读取文件 导入 装载。
-- 从 blocks.数据.清洗 导入 规整。
-- 从 blocks.数据.求和 导入 汇总。
--
-- -- 三步管道：读取 → 清洗 → 求和
-- 打印 装载("数据.txt")，规整，汇总。
--
-- -- 带中间变量的写法（便于调试）
-- 定义赵原始=装载("数据.txt")。
-- 定义赵干净=规整(赵原始)。
-- 定义赵结果=汇总(赵干净)。
-- 打印 赵结果。

-- 当前无可运行的替代，仅作文档记录
打印 "（本示例待 blocks.数据.读取文件 和 blocks.数据.清洗 落地后启用）"。
