-- 示例：管道清洗后求和
-- 演示：先用清洗块去除无效数据，再用求和块汇总（管道组合）
-- 依赖块：
--   blocks.数据.求和（已存在，导出 `汇总`）
--   blocks.数据.清洗（在建，M2 交付，导出 `规整`）
-- 预期输出（待 blocks.数据.清洗 落地后启用）：
--   9

-- ====== 待对应块落地后启用 ======
-- 从 blocks.数据.求和 导入 汇总。
-- 从 blocks.数据.清洗 导入 规整。
--
-- -- 原始数据含零值和无效项，先清洗再求和
-- 定义赵原始=列 1 0 3 0 5。
-- 打印 规整(赵原始)，汇总。          -- 去除零值后求和：9
--
-- -- 等价写法（不用管道）：
-- 定义赵干净=规整(赵原始)。
-- 打印 汇总(赵干净)。                -- 9

-- 临时替代：手动过滤后调用已有块
从 blocks.数据.求和 导入 汇总。
打印 汇总(列 1 3 5)。               -- 手动去零后求和：9
