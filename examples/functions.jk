-- 极快语言示例：函数定义与调用

-- 定义函数
函数 赵阶乘 接收 赵n：
  如果 赵n 小于等于 1 那么：
    返回 1。
  否则：
    返回 赵n 乘 赵阶乘(赵n减1)。
  。
。

打印 赵阶乘(5)。
打印 赵阶乘(10)。

-- 斐波那契数列
函数 李斐波那契 接收 李n：
  如果 李n 小于等于 1 那么：
    返回 李n。
  否则：
    返回 加 李斐波那契(李n减1) 李斐波那契(李n减2)。
  。
。

遍历 李i 于 范围 1 11：
  打印 李斐波那契(李i)。
。
