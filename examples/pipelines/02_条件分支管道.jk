-- 示例 02 · 条件分支管道
-- 教学目标：在管道数据流中结合条件判断（如果/否则如果/否则），展示如何对管道
--          每一步的结果做分支处理，以及如何按条件选择不同的管道加工路线。

-- ========== 场景1：对管道结果做条件判断 ==========
-- 先用管道归约求总分，再算平均，最后据平均分判定等级
定义赵成绩=列 92 78 85 90 66 73 88。
定义赵总分=赵成绩，归加0。
定义赵平均=平均 赵成绩。
打印 拼接 "总分：" (转字符串 赵总分)。
打印 拼接 "平均分：" (转字符串 赵平均)。

如果 赵平均 大于等于 85 那么：
  打印 "等级：优秀"。
否则如果 赵平均 大于等于 60 那么：
  打印 "等级：及格"。
否则：
  打印 "等级：不及格"。
。

-- ========== 场景2：根据条件选择不同的管道 ==========
定义李数据=列 3 7 1 9 4 6 2 8。
定义李阈值=5。

定义李高=李数据，只大于李阈值。
定义李低=李数据，只小于等于李阈值。

打印 拼接 "高于阈值：" (转字符串 李高) " 共" (转字符串 (长度 李高)) "个"。
打印 拼接 "不高于阈值：" (转字符串 李低) " 共" (转字符串 (长度 李低)) "个"。

-- 哪一组元素多，就对那一组做进一步管道加工
如果 (长度 李高) 大于 (长度 李低) 那么：
  定义李结果=李高，皆乘10。
  打印 拼接 "高组皆乘10：" (转字符串 李结果)。
否则：
  定义李结果=李低，皆加100。
  打印 拼接 "低组皆加100：" (转字符串 李结果)。
。

-- ========== 场景3：把条件判断封装进函数，逐元素分类 ==========
函数 周分类 接收 周值：
  如果 周值 大于等于 85 那么：
    返回 "优"。
  否则如果 周值 大于等于 60 那么：
    返回 "中"。
  否则：
    返回 "差"。
  。
。

遍历 周项 于 赵成绩：
  打印 拼接 (转字符串 周项) " → " 周分类(周项)。
。
