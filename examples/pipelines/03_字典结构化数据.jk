-- 示例 03 · 字典结构化数据
-- 教学目标：展示如何在极快里处理「字典（键值对）」这种结构化数据 ——
--          用 `.键名` 成员访问、用 `取值 字典 "键"` 动词访问，
--          并把 `皆取值"键"` 作为管道一段，从一批记录里投影出某个字段。

-- ========== 数据来源1：Python 桥的 json 模块（ADR-10，显式 蟒: 前缀） ==========
导入 蟒:json。

定义赵员工=json.loads("[{\"姓名\": \"张三\", \"部门\": \"研发\", \"绩效\": 92}, {\"姓名\": \"李四\", \"部门\": \"销售\", \"绩效\": 78}, {\"姓名\": \"王五\", \"部门\": \"研发\", \"绩效\": 85}, {\"姓名\": \"赵六\", \"部门\": \"财务\", \"绩效\": 66}]")。

打印 拼接 "员工记录数：" (转字符串 (长度 赵员工))。

-- 逐条读取：字典既支持 `.键名`，也支持 `取值 字典 "键"`
遍历 赵人 于 赵员工：
  打印 拼接 "  " 赵人.姓名 "（" (取值 赵人 "部门") "）绩效 " (转字符串 赵人.绩效)。
。

-- ========== 管道投影：皆取值"键" 把一批字典投影成一个字段列表 ==========
定义赵绩效=赵员工，皆取值"绩效"。
打印 拼接 "绩效列表：" (转字符串 赵绩效)。

-- 投影 → 过滤 → 归约：绩效 80 分以上的总和
定义赵高分总和=赵员工，皆取值"绩效"，只大于等于80，归加0。
打印 拼接 "80分以上绩效总和：" (转字符串 赵高分总和)。

-- 投影 → 排序 / 去重
定义赵部门=赵员工，皆取值"部门"。
打印 拼接 "部门（含重复）：" (转字符串 赵部门)。
打印 拼接 "部门（去重）：" (转字符串 (去重 赵部门))。
打印 拼接 "绩效（升序）：" (转字符串 (排序 赵绩效))。
打印 拼接 "绩效最高：" (转字符串 (最大 赵绩效))。

-- ========== 数据来源2：内建动词返回的字典（身份证信息） ==========
定义李身份证="110101199003070011"。
定义李信息=提取身份证信息 李身份证。
打印 拼接 "身份证字典：" (转字符串 李信息)。

-- 遍历字典 → 得到的是键；再用 取值 拿对应的值
遍历 李键 于 李信息：
  打印 拼接 "  " 李键 " = " (转字符串 (取值 李信息 李键))。
。

-- 缺失键的行为：取值 返回 空（nil），不抛异常。
-- 注意用 打印 直接输出才会渲染成中文的「空」；转字符串 走的是通用字符串化。
打印 "不存在的键：" (取值 李信息 "住址")。
