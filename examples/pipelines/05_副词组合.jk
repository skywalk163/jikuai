-- 示例 05 · 副词组合
-- 教学目标：系统展示三大副词 皆(map) / 只(filter) / 归(reduce) 的用法与组合。
--          副词把普通二元/一元动词提升为「作用于列表每个元素」的高阶操作，
--          管道逗号让它们首尾相接、按流向从左到右阅读。

-- ========== 副词一览 ==========
-- 皆 verb args...   映射：对每个元素调用 verb(元素, args...)
-- 只 verb args...   过滤：保留 verb(元素, args...) 为真的元素
-- 归 verb 初值      归约：从 初值 起，累积计算 verb(累积, 元素)

定义赵数据=列 1 2 3 4 5 6 7 8。

-- ========== 单副词 ==========
打印 拼接 "皆乘2：" (转字符串 (赵数据，皆乘2))。
打印 拼接 "皆加10：" (转字符串 (赵数据，皆加10))。
打印 拼接 "只大于3：" (转字符串 (赵数据，只大于3))。
打印 拼接 "只小于等于4：" (转字符串 (赵数据，只小于等于4))。
打印 拼接 "归加0（求和）：" (转字符串 (赵数据，归加0))。
打印 拼接 "归乘1（累乘）：" (转字符串 (赵数据，归乘1))。

-- ========== 双副词组合：顺序会影响结果 ==========
-- 先过滤再映射
打印 拼接 "只大于5→皆加2：" (转字符串 (赵数据，只大于5，皆加2))。
-- 先映射再过滤（同样两步，换个顺序结果不同）
打印 拼接 "皆加2→只大于5：" (转字符串 (赵数据，皆加2，只大于5))。
-- 映射 + 归约：先加工再求和
打印 拼接 "皆乘2→归加0：" (转字符串 (赵数据，皆乘2，归加0))。

-- ========== 三副词全链路（map-filter-reduce）==========
打印 拼接 "皆乘2→只大于6→归加0：" (转字符串 (赵数据，皆乘2，只大于6，归加0))。
打印 拼接 "只大于3→皆加100→归加0：" (转字符串 (赵数据，只大于3，皆加100，归加0))。

-- ========== 与非副词动词混用 ==========
-- 副词段之后接普通聚合动词（长度 / 最大 / 求和 / 平均）
定义赵偶数=赵数据，只大于3，皆乘2。
打印 拼接 "剩余长度：" (转字符串 (长度 赵偶数))。
打印 拼接 "剩余最大：" (转字符串 (最大 赵偶数))。
打印 拼接 "剩余求和：" (转字符串 (求和 赵偶数))。
打印 拼接 "剩余平均：" (转字符串 (平均 赵偶数))。

-- ========== 副词参数可以是变量 ==========
定义赵阈值=5。
打印 拼接 "阈值=" (转字符串 赵阈值) " 过滤后：" (转字符串 (赵数据，只大于赵阈值))。

-- ========== 字符串场景：皆大写 / 皆小写 ==========
定义赵词=列 "Alice" "BOB" "Carol"。
打印 拼接 "皆小写：" (转字符串 (赵词，皆小写))。
打印 拼接 "皆大写：" (转字符串 (赵词，皆大写))。
