-- 示例 06 · 中国特色管道
-- 教学目标：把「人民币精确金额」与「农历/干支/生肖」这两组中国国情内建能力
--          放进逗号管道里，展示中国特色类型在管道中同样是一等公民。
--
-- 提示：`打印 X，皆...` 会被 打印（变参动词）先吞掉 X，管道随后作用于 打印 的
--      返回值（空）。要在管道结果上打印，请加括号：`打印 (X，皆...)`，
--      或先 `定义` 成变量再打印。

-- ========== 第一部分：人民币管道（报销单） ==========
-- ￥ 字面量按分精确存储，不是浮点数
定义赵明细=列 ￥15.50 ￥28.80 ￥42.30 ￥18.00 ￥6.50。
打印 拼接 "报销明细：" (转字符串 赵明细)。
打印 拼接 "笔数：" (转字符串 (长度 赵明细))。

-- 管道：求和 / 过滤大额 / 大额合计
定义赵合计=求和 赵明细。
打印 拼接 "合计：" (转字符串 赵合计)。
打印 拼接 "大写：" (大写金额 赵合计)。

定义赵大额=赵明细，只大于 ￥20.00。
打印 拼接 "超过￥20.00的：" (转字符串 赵大额)。
打印 拼接 "大额合计：" (转字符串 (求和 赵大额))。

-- 管道：批量打折（皆乘）后再汇总
定义赵打折=赵明细，皆乘0.9。
打印 拼接 "九折后：" (转字符串 赵打折)。
打印 拼接 "九折合计：" (转字符串 (求和 赵打折))。

-- 管道：批量转中文大写金额
打印 拼接 "逐笔大写：" (转字符串 (赵大额，皆大写金额))。

-- ========== 第二部分：农历 / 干支 / 生肖管道 ==========
定义李年份=列 2024 2025 2026 2027 2028。
打印 拼接 "年份：" (转字符串 李年份)。
打印 拼接 "生肖：" (转字符串 (李年份，皆生肖))。
打印 拼接 "干支：" (转字符串 (李年份，皆干支纪年))。
打印 拼接 "中文数字：" (转字符串 (李年份，皆汉字数字))。

-- 逐年打印「干支(生肖)年」
遍历 李年 于 李年份：
  打印 拼接 "  " (转字符串 李年) " 年 → " (干支纪年 李年) "（" (生肖 李年) "）"。
。

-- 公历 → 完整农历日期
打印 拼接 "2026-08-07 农历：" (农历完整日期 2026 8 7)。
打印 拼接 "2026-01-01 农历：" (农历完整日期 2026 1 1)。

-- ========== 第三部分：国情校验 + 管道 ==========
定义周手机=列 "13800138000" "18612345678" "13312345678"。
打印 拼接 "手机号：" (转字符串 周手机)。
遍历 周号 于 周手机：
  打印 拼接 "  " 周号 " → " (判断运营商 周号)。
。
