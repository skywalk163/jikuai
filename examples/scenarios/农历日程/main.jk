-- 农历日程生成.jk
-- 场景：给定一组固定的「公历日期 + 事件」日程表，批量生成农历日期、
--       干支纪年与生肖，输出一份「传统农历日程」清单。
-- 输入全部为固定字面量（均落在农历数据表 1900-2100 有效区间内），
-- 不读取系统当前时间，因此输出稳定，可作快照。

打印 "========== 传统农历日程 =========="。

-- ========== 一、单日详细信息 ==========
定义赵年=2026。
定义赵月=8。
定义赵日=8。
打印 "----- 单日详情（2026-08-08）-----"。
打印 拼接 "公历：" (转字符串 赵年) "-" (转字符串 赵月) "-" (转字符串 赵日)。
打印 拼接 "干支纪年：" (干支纪年 赵年)。
打印 拼接 "生肖：" (生肖 赵年)。
打印 拼接 "农历完整日期：" (农历完整日期 赵年 赵月 赵日)。
-- 公历转农历 返回 [农历年, 农历月, 农历日, 是否闰月]
打印 拼接 "公历转农历四元组：" (转字符串 (公历转农历 赵年 赵月 赵日))。

-- ========== 二、批量：年份表 → 生肖与干支（逗号管道） ==========
-- 生肖 / 干支纪年 都是内建动词，可以直接作为副词 皆 的动词使用
打印 "----- 生肖与干支（年份表）-----"。
定义赵年份=列 2024 2025 2026 2027 2028。
打印 拼接 "年份：" (转字符串 赵年份)。
打印 拼接 "生肖：" (转字符串 (赵年份，皆生肖))。
打印 拼接 "干支：" (转字符串 (赵年份，皆干支纪年))。

-- ========== 三、日程表：公历事件 → 农历 ==========
-- 用四条等长的平行列表存日程（年 / 月 / 日 / 事件），
-- 再用 范围 做下标遍历。注：`追加 表 (列 ...)` 里
-- 「标识符 + 左括号」会被解析成函数调用，所以这里改用平行列表。
打印 "----- 2026 年日程表 -----"。
定义钱年表=列 2026 2026 2026 2026 2026 2026 2026。
定义钱月表=列    1    2    4    5    6    9   10。
定义钱日表=列    1   17    5    1   19   25    1。
定义钱事表=列 "元旦" "春节" "清明" "劳动节" "端午" "中秋" "国庆"。
定义钱条数=长度 钱事表。
打印 拼接 "日程条数：" (转字符串 钱条数)。

遍历 钱i 于 范围 0 钱条数：
  定义孙年=钱年表[钱i]。
  定义孙月=钱月表[钱i]。
  定义孙日=钱日表[钱i]。
  定义孙事=钱事表[钱i]。
  打印 拼接 "  " (转字符串 孙年) "-" (转字符串 孙月) "-" (转字符串 孙日) "  " 孙事 "  →  " (农历完整日期 孙年 孙月 孙日)。
。

-- ========== 四、跨年份的同一天 ==========
打印 "----- 每年 1 月 1 日的农历 -----"。
遍历 赵某年 于 赵年份：
  打印 拼接 "  " (转字符串 赵某年) "-01-01  →  " (农历完整日期 赵某年 1 1) "  【" (干支纪年 赵某年) "年 属" (生肖 赵某年) "】"。
。

-- ========== 五、60 甲子循环验证 ==========
打印 "----- 60 甲子循环 -----"。
打印 拼接 "1984 → " (干支纪年 1984) "（" (生肖 1984) "）"。
打印 拼接 "2044 → " (干支纪年 2044) "（" (生肖 2044) "）"。
如果 (干支纪年 1984) 等于 (干支纪年 2044) 那么：
  打印 "验证通过：1984 与 2044 干支同名"。
否则：
  打印 "验证失败"。
。

-- ========== 六、有效范围自检（详见 README「已知边界」）==========
-- 农历数据表覆盖 1900-2100。本示例查询年份全部落在区间内。
打印 "----- 有效范围自检 -----"。
定义赵下界=1900。
定义赵上界=2100。
打印 拼接 "农历数据表覆盖：" (转字符串 赵下界) " - " (转字符串 赵上界)。
定义赵查询年=列 1984 2024 2025 2026 2027 2028 2044。
定义赵偏低=赵查询年，只小于赵下界。
定义赵偏高=赵查询年，只大于赵上界。
打印 拼接 "本示例查询年份：" (转字符串 赵查询年)。
打印 拼接 "低于下界的：" (转字符串 赵偏低)。
打印 拼接 "高于上界的：" (转字符串 赵偏高)。
如果 (加 (长度 赵偏低) (长度 赵偏高)) 等于 0 那么：
  打印 "全部查询年份均在有效范围内"。
否则：
  打印 "存在越界年份，结果不可信"。
。

打印 "========== 日程结束 =========="。
