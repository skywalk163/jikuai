-- examples/scenarios/客户对账演示.jk
-- 端到端 demo：使用 L3 块 blocks.财务.客户对账 做贷款还款核对。
-- 场景：客户贷款 50000 元、6 期、年化 6%，实际到账只有 3 期 × 9000。

从 blocks.财务.客户对账 导入 对账。

打印 "===== 客户对账 ====="。
打印 "贷款额 50000 / 6 期 / 年化 6%"。
打印 "实际到账：9000 × 3 期"。
打印 "分组每组 2"。
打印 ""。

定义赵结果=对账(50000, 6, 0.06, 列 9000 9000 9000, 2)。

打印 拼接 "应总：" (转字符串 (取值 赵结果 0))。
打印 拼接 "实总：" (转字符串 (取值 赵结果 1))。
打印 拼接 "差额：" (转字符串 (取值 赵结果 2))。
打印 拼接 "分组明细：" (转字符串 (取值 赵结果 3))。
打印 拼接 "对账状态：" (转字符串 (取值 赵结果 4))。
打印 "===== 结束 ====="。
