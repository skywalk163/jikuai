-- 推理演示 · 主入口（非交互，稳定快照）
-- 展示如何用「极快」写一个 Chain-of-Thought 推理引擎 demo：
--   ● 类 / 构造 / 方法 —— 引擎自身承担 4 阶段编排（ADR-22 词法作用域）
--   ● 模块系统 导入 / 从…导入 / 导出 —— 拆分 4 个中文文件名的模块
--   ● 字典字面量 {"键": 值}（v0.7 新增）—— 承载阶段记录 / 历史条目
--   ● 遍历 / 条件 / 拼接 / 分割 —— 组织流程与输出
--
-- 场景测试通过：expected.txt 是本文件真实运行输出的快照。

从 引擎 导入 创建引擎 是否在线 模型名 问题清单。
从 工具 导入 显示横幅 显示历史 分隔线。

打印 "========== 极快 Reasonix 推理引擎 =========="。
显示横幅()。
打印 拼接 "  " 分隔线("─", 40)。
如果 是否在线() 那么:
  打印 拼接 "  [AI 模式] 使用 " 模型名() " 模型"。
否则:
  打印 "  [模式] 离线模拟推理（不联网，快照可复现）"。
。
打印 "  [数据] 阶段记录用字典字面量承载"。
打印 拼接 "  " 分隔线("─", 40)。

-- 问题来源：命令行位置参数 / REASONIX_QUESTIONS 环境变量；都没给就用下面两个默认题。
定义赵问题集=列 "从 1 加到 100 的和是多少？" "为什么夜晚天空整体是黑的？"。
定义赵自定=问题清单()。
如果 (长度 赵自定) 大于 0 那么:
  定义赵问题集=赵自定。
。
定义赵引擎=创建引擎()。

遍历 赵问 于 赵问题集:
  打印 ""。
  打印 分隔线("━", 60)。
  赵引擎.处理问题(赵问)。
。

打印 ""。
打印 分隔线("━", 60)。
显示历史(赵引擎.获取历史)。

打印 "========== 推理结束 =========="。
