-- 推理演示 · 工具模块
-- 负责界面输出格式化：横幅、分隔线、阶段框、答案框。

导出 显示横幅 显示帮助 分隔线 格式化阶段 格式化答案 显示历史。

-- 生成一条重复 N 次的分隔线
函数 分隔线 接收 赵符 赵长:
  定义赵结果=""。
  定义赵计=0。
  当 赵计 小于 赵长:
    定义赵结果=拼接 赵结果 赵符。
    定义赵计=赵计加1。
  。
  返回 赵结果。
。

函数 显示横幅:
  打印 "╔══════════════════════════════════════════╗"。
  打印 "║   极快 JiKuai Reasonix 推理引擎 v1.0     ║"。
  打印 "║   基于 Chain-of-Thought 的多步推理演示   ║"。
  打印 "╚══════════════════════════════════════════╝"。
。

函数 显示帮助:
  打印 "  命令说明："。
  打印 "    问题        直接输入问题即可"。
  打印 "    历史        查看已推理过的问题"。
  打印 "    退出        结束会话"。
。

-- 分阶段的框（不含内文），供 引擎 逐条打印
函数 格式化阶段 接收 赵序号 赵总数 赵标题 赵内容:
  定义赵头=拼接 "┌─ 第" (转字符串 赵序号) "阶段/共" (转字符串 赵总数) "阶段 · " 赵标题 " " 分隔线("─", 10) "┐"。
  打印 赵头。
  遍历 赵行 于 分割 赵内容 "\n":
    打印 拼接 "│ " 赵行。
  。
  打印 拼接 "└" 分隔线("─", 58) "┘"。
。

函数 格式化答案 接收 赵答案:
  打印 分隔线("═", 60)。
  打印 "║  最终答案"。
  打印 分隔线("═", 60)。
  遍历 赵行 于 分割 赵答案 "\n":
    打印 赵行。
  。
  打印 分隔线("═", 60)。
。

函数 显示历史 接收 赵历史:
  定义赵n=长度 赵历史。
  打印 拼接 "已记录 " (转字符串 赵n) " 条历史"。
  定义赵i=0。
  遍历 赵条 于 赵历史:
    定义赵i=赵i加1。
    打印 拼接 "  [" (转字符串 赵i) "] " 赵条.问题。
  。
。
