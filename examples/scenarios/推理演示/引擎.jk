-- 推理演示 · 核心引擎
-- ADR-22 之后，方法体以**类定义处**（本模块）为父作用域，
-- 因此方法可直接用本模块 导入 的名字：创建思考链 / 生成分析阶段 / 格式化* / 智言。
--
-- AI 接入（ADR-23 同目录 sidecar）：
--   `导入 蟒:智言。` 加载同目录的 智言.py（封装 OpenAI 兼容 Chat Completions）。
--   有 .env + API_KEY 且非 REASONIX_OFFLINE=1 时走真实大模型；
--   任何一步失败（无 key / 网络错 / 空回复）都回退到 提示词 模块预设的模拟内容，
--   demo 永远能跑完。

导出 推理引擎 创建引擎 是否在线 模型名 问题清单。

从 思考链 导入 创建思考链。
从 提示词 导入 生成分析阶段 系统提示词。
从 工具 导入 格式化阶段 格式化答案。
导入 蟒:智言。

函数 是否在线:
  返回 智言.available()。
。

函数 模型名:
  返回 智言.model_name()。
。

-- 用户自定义问题：来自 REASONIX_QUESTIONS 环境变量或命令行位置参数。
-- 返回空列表表示「用 main.jk 里的默认问题」。
函数 问题清单:
  返回 智言.user_questions()。
。

类 推理引擎:
  构造:
    自身.名称="Reasonix 推理引擎"。
    自身.版本="1.0.0"。
    自身.历史记录=列。
  。

  方法 推理 接收 赵问题:
    定义赵链=创建思考链(赵问题)。
    定义赵阶段=生成分析阶段(赵问题)。
    定义赵总数=长度 赵阶段。
    定义赵在线=智言.available()。
    定义赵系统=系统提示词()。

    定义赵i=0。
    当 赵i 小于 赵总数:
      定义赵当前=赵阶段[赵i]。
      定义赵标题=赵当前.标题。
      -- 默认用模拟内容；在线可用时尝试真实大模型，失败则回退
      定义赵内容=赵当前.内容。
      如果 赵在线 那么:
        定义赵回复=智言.chat(赵系统, 赵当前.提示)。
        如果 赵回复 不等于 "" 那么:
          定义赵内容=赵回复。
        。
      。
      赵链.推演阶段(赵标题, 赵内容)。
      格式化阶段(赵i加1, 赵总数, 赵标题, 赵内容)。
      定义赵i=赵i加1。
    。

    返回 赵链.综合答案。
  。

  方法 处理问题 接收 赵问题:
    打印 拼接 "  问题：" 赵问题。
    打印 "  开始推理..."。
    定义赵答=自身.推理(赵问题)。
    格式化答案(赵答)。
    定义赵条={"问题": 赵问题, "答案": 赵答}。
    自身.历史记录=追加 自身.历史记录 赵条。
    返回 赵答。
  。

  方法 获取历史:
    返回 自身.历史记录。
  。
。

函数 创建引擎:
  返回 新建 推理引擎()。
。
