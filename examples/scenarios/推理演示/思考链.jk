-- 推理演示 · 思考链模块
-- 数据结构：把推理过程中的每个阶段（标题+内容）依次记录下来。
-- 特意用「字典字面量」构造阶段记录，展示 jikuai 新增的字典字面量语法。

导出 思考链 创建思考链。

类 思考链:
  构造 接收 赵问题:
    自身.问题=赵问题。
    自身.阶段列表=列。
    自身.当前=0。
  。

  方法 推演阶段 接收 赵标题 赵内容:
    定义赵序号=自身.当前加1。
    -- 字典字面量：{键: 值, 键: 值}
    定义赵条={"标题": 赵标题, "内容": 赵内容, "序号": 赵序号}。
    自身.阶段列表=追加 自身.阶段列表 赵条。
    自身.当前=赵序号。
  。

  方法 阶段数:
    返回 自身.当前。
  。

  方法 所有阶段:
    返回 自身.阶段列表。
  。

  方法 综合答案:
    定义赵头=拼接 "基于 " (转字符串 自身.当前) " 个推理阶段，结论如下："。
    定义赵体=""。
    遍历 赵阶 于 自身.阶段列表:
      -- 字典的两种访问方式：.键 与 [键]；摘要只取内容首行，保证一行一条
      定义赵首=首个 (分割 赵阶["内容"] "\n")。
      定义赵行=拼接 "  " (转字符串 赵阶.序号) ") " 赵阶.标题 "：" 赵首。
      如果 赵体 等于 "" 那么:
        定义赵体=赵行。
      否则:
        定义赵体=拼接 赵体 "\n" 赵行。
      。
    。
    返回 拼接 赵头 "\n" 赵体。
  。
。

函数 创建思考链 接收 赵问题:
  返回 新建 思考链(赵问题)。
。
