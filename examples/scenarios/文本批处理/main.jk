-- 中文文本批量处理.jk
-- 场景：一批脏中文文本 → 排版规范化 → 分词 → 词频统计 → 成语识别
-- 覆盖 AC-M6-03-02：导入 排版 + 分词 + 成语（M4/M5 新增标准库模块）
-- 所有输入固定，不读系统时间，输出可作稳定快照。

导入 排版。
导入 分词。
导入 成语。

打印 "========== 中文文本批量处理 =========="。

-- ========== 一、脏文本清单（固定输入） ==========
-- 毛病：中西文无间距、半角标点、句尾标点不规范
定义赵文一="JiKuai是第1个中文编程语言,值得推广!"。
定义赵文二="中国程序员的中文编程语言。极快版本V0.7.0于2026年发布 !"。
定义赵文三="团队做事讲究胸有成竹,同事们学富五车,合作画蛇添足要避免."。
定义赵文集=列 赵文一 赵文二 赵文三。

打印 "----- 原文 -----"。
定义赵编号=1。
遍历 赵文 于 赵文集：
  打印 拼接 "[" (转字符串 赵编号) "] " 赵文。
  定义赵编号=赵编号加1。
。

-- ========== 二、排版规范化 ==========
-- 注意：副词 皆 只识别内建动词，不识别模块函数 —— 写 `皆规范化文本`
-- 会触发 JK-W1001 并原值透传。所以这里用 遍历 逐条调用。
打印 "----- 排版规范化后 -----"。
定义赵规范集=列。
定义赵编号=1。
遍历 赵文 于 赵文集：
  定义赵成=排版.规范化文本(赵文)。
  定义赵规范集=追加 赵规范集 赵成。
  打印 拼接 "[" (转字符串 赵编号) "] " 赵成。
  定义赵编号=赵编号加1。
。
-- 幂等自检：再规范化一次结果不变（AC-M4-05-02）
定义赵二次=排版.规范化文本(赵规范集[0])。
如果 赵二次 等于 赵规范集[0] 那么：
  打印 "幂等自检：规范化文本 f(f(x)) == f(x) 成立"。
否则：
  打印 "幂等自检：失败"。
。

-- ========== 三、分词 ==========
定义赵词全=列。
定义赵编号=1。
打印 "----- 分词结果 -----"。
遍历 赵成 于 赵规范集：
  定义赵词=分词.分词(赵成)。
  打印 拼接 "[" (转字符串 赵编号) "] " (转字符串 赵词)。
  定义赵编号=赵编号加1。
  遍历 赵项 于 赵词：
    定义赵词全=追加 赵词全 赵项。
  。
。
打印 拼接 "总词项数(含标点与单字兜底)：" (转字符串 (长度 赵词全))。
打印 拼接 "去重词表大小：" (转字符串 (长度 (去重 赵词全)))。

-- ========== 四、词频统计（只统计长度≥2的多字词） ==========
打印 "----- 词频统计(长度≥2) -----"。
定义赵多字=列。
遍历 赵项 于 赵词全：
  如果 (长度 赵项) 大于等于 2 那么：
    定义赵多字=追加 赵多字 赵项。
  。
。
打印 拼接 "多字词项数：" (转字符串 (长度 赵多字))。
定义赵多字唯=排序 (去重 赵多字)。
遍历 赵词项 于 赵多字唯：
  定义钱计=0。
  遍历 钱x 于 赵多字：
    如果 钱x 等于 赵词项 那么：
      定义钱计=钱计加1。
    。
  。
  打印 拼接 "  " 赵词项 " × " (转字符串 钱计)。
。

-- ========== 五、成语识别（4 字滑动窗口 + 成语.是成语） ==========
-- 分词词典不含四字成语（会被单字兜底切开），因此在规范后的整句上
-- 做 4 字滑动窗口，逐窗询问 成语.是成语。
打印 "----- 成语识别(4 字滑动窗口) -----"。
定义赵成语集=列。
遍历 赵成 于 赵规范集：
  定义钱n=长度 赵成。
  定义钱窗数=钱n减3。
  遍历 钱i 于 范围 0 钱窗数：
    定义孙止=钱i加4。
    定义孙片=子串 赵成 钱i 孙止。
    如果 成语.是成语(孙片) 那么：
      定义赵成语集=追加 赵成语集 孙片。
    。
  。
。
定义赵成语唯=排序 (去重 赵成语集)。
打印 拼接 "命中成语数：" (转字符串 (长度 赵成语唯))。
遍历 赵成条 于 赵成语唯：
  打印 拼接 "  " 赵成条 " → " (成语.成语释义(赵成条))。
。

打印 "========== 处理结束 =========="。
