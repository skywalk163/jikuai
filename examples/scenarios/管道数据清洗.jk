-- 管道数据清洗.jk
-- 场景脚本：模拟从「脏数据」经多级管道清洗成「干净结果」
-- 覆盖：字符串→整数转换、过滤无效值、映射标准化、归约汇总（≥3 段管道）

-- ===== 模拟脏数据：混合了字符串数字 =====
-- 极快里列表可以混合类型；我们手动做清洗
定义赵原始=列 "12" "abc" "45" "" "7" "xyz" "100" "0" "33"。
打印 "===== 原始数据 ====="。
打印 赵原始。
打印 拼接 "记录数：" (转字符串 (长度 赵原始))。

-- ===== 第一步：过滤 —— 只保留可转整数的字符串 =====
-- 极快的 转整数 在非数字串上会抛异常；直接用 尝试/捕获 判定是否可转。
函数 赵可转 接收 赵值：
  尝试：
    转整数 赵值。
    返回 真。
  捕获 赵错误：
    返回 假。
  。
。

-- 手动遍历过滤（副词 只 目前仅支持内建比较动词）
定义赵有效=列。
遍历 赵项 于 赵原始：
  如果 赵可转(赵项) 那么：
    定义赵有效=追加 赵有效 赵项。
  。
。
打印 "===== 第一步：过滤有效数字串 ====="。
打印 赵有效。

-- ===== 第二步：映射 —— 字符串转为整数 =====
定义赵数值=赵有效，皆转整数。
打印 "===== 第二步：转为整数 ====="。
打印 赵数值。

-- ===== 第三步：管道过滤 —— 只保留大于 0 的正数 =====
定义赵正数=赵数值，只大于0。
打印 "===== 第三步：过滤正数(>0) ====="。
打印 赵正数。

-- ===== 第四步：归约 —— 求和 / 平均 / 最大 / 最小 =====
打印 "===== 第四步：统计 ====="。
打印 拼接 "求和：" (转字符串 (求和 赵正数))。
打印 拼接 "平均：" (转字符串 (平均 赵正数))。
打印 拼接 "最大：" (转字符串 (最大 赵正数))。
打印 拼接 "最小：" (转字符串 (最小 赵正数))。
打印 拼接 "个数：" (转字符串 (长度 赵正数))。

-- ===== 一条龙管道（3段+）：转整数→只正数→皆乘2→归加0 =====
定义赵一条龙=赵有效，皆转整数，只大于0，皆乘2，归加0。
打印 "===== 一条龙管道 ====="。
打印 拼接 "转整→只正→皆乘2→归加0 = " (转字符串 赵一条龙)。

-- ===== 排序与去重 =====
定义赵排序=排序 赵正数。
打印 拼接 "排序后：" (转字符串 赵排序)。

-- 造一个有重复的场景
定义赵含重=列 12 45 7 100 33 7 12。
打印 拼接 "含重复：" (转字符串 赵含重)。
打印 拼接 "去重后：" (转字符串 (去重 赵含重))。
打印 拼接 "去重并排序：" (转字符串 (排序 (去重 赵含重)))。

打印 "===== 数据清洗结束 ====="。
