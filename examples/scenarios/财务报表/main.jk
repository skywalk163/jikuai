-- 财务报表自动处理.jk
-- 场景：一份跨部门的多笔报销单，自动完成
--   1) 逐笔明细展示（人民币精确金额）
--   2) 小计 / 税费(6%增值税) / 含税合计
--   3) 中文大写金额
--   4) 用逗号管道筛选「大额支出」与聚合
-- 所有输入均为固定字面量，输出可作稳定快照。

打印 "========== 报销报表 =========="。

-- ========== 明细数据（固定输入） ==========
-- 每笔：￥ 字面量按分精确存储，非浮点
定义赵差旅=￥1280.00。
定义赵餐饮=￥356.50。
定义赵办公=￥89.90。
定义赵设备=￥4599.00。
定义赵通讯=￥128.00。
定义赵培训=￥680.00。

定义赵明细=列 赵差旅 赵餐饮 赵办公 赵设备 赵通讯 赵培训。

打印 "----- 报销明细 -----"。
打印 拼接 "差旅费：" (转字符串 赵差旅)。
打印 拼接 "餐饮费：" (转字符串 赵餐饮)。
打印 拼接 "办公费：" (转字符串 赵办公)。
打印 拼接 "设备费：" (转字符串 赵设备)。
打印 拼接 "通讯费：" (转字符串 赵通讯)。
打印 拼接 "培训费：" (转字符串 赵培训)。
打印 拼接 "笔数：" (转字符串 (长度 赵明细))。

-- ========== 小计 ==========
定义赵小计=求和 赵明细。
打印 "----- 汇总 -----"。
打印 拼接 "报销小计：" (转字符串 赵小计)。

-- ========== 税费（6% 增值税） ==========
定义赵税率=0.06。
定义赵税额=赵小计乘赵税率。
打印 拼接 "增值税(6%)：" (转字符串 赵税额)。

-- ========== 含税合计 ==========
定义赵合计=赵小计加赵税额。
打印 拼接 "含税合计：" (转字符串 赵合计)。

-- ========== 中文大写金额 ==========
打印 "----- 大写金额 -----"。
打印 拼接 "小计大写：" (大写金额 赵小计)。
打印 拼接 "合计大写：" (大写金额 赵合计)。

-- ========== 管道分析：筛选大额支出（只大于 ￥500） ==========
定义赵大额=赵明细，只大于 ￥500.00。
打印 "----- 大额支出(>￥500) -----"。
打印 拼接 "大额明细：" (转字符串 赵大额)。
打印 拼接 "大额笔数：" (转字符串 (长度 赵大额))。
打印 拼接 "大额合计：" (转字符串 (求和 赵大额))。
打印 拼接 "大额合计大写：" (大写金额 (求和 赵大额))。

-- ========== 管道分析：报销标准核减（全部按 9 折报销） ==========
定义赵核减=赵明细，皆乘0.9。
打印 "----- 9折核减后 -----"。
打印 拼接 "核减后明细：" (转字符串 赵核减)。
打印 拼接 "核减后合计：" (转字符串 (求和 赵核减))。

-- ========== 逐笔大写清单 ==========
打印 "----- 逐笔大写 -----"。
遍历 赵项 于 赵明细：
  打印 拼接 "  " (转字符串 赵项) " → " (大写金额 赵项)。
。

打印 "========== 报表结束 =========="。
