-- 财务计算.jk
-- 场景脚本：模拟一份小型报销单的完整流程
-- 覆盖：￥ 字面量、精确加减乘除、大写金额转换、税费计算、汇总

-- ===== 报销明细 =====
定义赵早餐=￥15.50。
定义赵午餐=￥28.80。
定义赵晚餐=￥42.30。
定义赵打车=￥18.00。
定义赵住宿=￥280.00。
定义赵文具=￥36.90。

定义赵明细=列 赵早餐 赵午餐 赵晚餐 赵打车 赵住宿 赵文具。
打印 "===== 报销明细 ====="。
打印 拼接 "早餐：" (转字符串 赵早餐)。
打印 拼接 "午餐：" (转字符串 赵午餐)。
打印 拼接 "晚餐：" (转字符串 赵晚餐)。
打印 拼接 "打车：" (转字符串 赵打车)。
打印 拼接 "住宿：" (转字符串 赵住宿)。
打印 拼接 "文具：" (转字符串 赵文具)。

-- ===== 小计 =====
定义赵小计=求和 赵明细。
打印 拼接 "小计：" (转字符串 赵小计)。

-- ===== 税费（假设 6% 增值税） =====
定义赵税率=0.06。
定义赵税额=赵小计乘赵税率。
打印 拼接 "增值税(6%)：" (转字符串 赵税额)。

-- ===== 含税合计 =====
定义赵合计=赵小计加赵税额。
打印 拼接 "含税合计：" (转字符串 赵合计)。

-- ===== 大写金额 =====
打印 拼接 "大写（小计）：" (大写金额 赵小计)。
打印 拼接 "大写（合计）：" (大写金额 赵合计)。

-- ===== 管道分析：超过 ￥30 的大额支出 =====
定义赵大额=赵明细，只大于 ￥30.00。
打印 拼接 "大额支出（>￥30）：" (转字符串 赵大额)。
打印 拼接 "大额合计：" (转字符串 (求和 赵大额))。

-- ===== 折扣模拟：全部 9 折 =====
定义赵折后=赵明细，皆乘0.9。
打印 拼接 "九折后明细：" (转字符串 赵折后)。
打印 拼接 "九折合计：" (转字符串 (求和 赵折后))。

-- ===== 每笔大写 =====
打印 "逐笔大写："。
遍历 赵项 于 赵明细：
  打印 拼接 "  " (转字符串 赵项) " → " (大写金额 赵项)。
。

打印 "===== 报销单结束 ====="。
