-- 极快语言示例：中文分词（M5 · T-M5-S03）
导入 分词。

打印 分词.分词("中国程序员的中文编程语言")。
-- [中国, 程序员, 的, 中文, 编程语言]

打印 分词.分词("北京欢迎你2026")。
-- 未命中的字按单字兜底切；连续字母数字整体成词

打印 分词.分词("")。       -- []
打印 分词.分词("。，、")。  -- [。, ，, 、]

-- 幂等：同输入连续调用输出完全相同
定义 赵一次 = 分词.分词("中文编程语言")。
定义 赵二次 = 分词.分词("中文编程语言")。
打印 赵一次。
打印 赵二次。