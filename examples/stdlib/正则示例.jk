-- 极快语言示例：中文正则（M5 · T-M5-S01）
导入 正则。

-- 整串匹配
打印 正则.匹配("[一-十]+", "一二三")。
打印 正则.匹配("[a-z]+", "Hello")。

-- 搜索首个匹配，返回字典（含 文本/起始/结束）或空
-- 提示：.jk 字符串里反斜杠要写两遍，或直接用字符类规避
打印 正则.搜索("[0-9]+", "价格199元")。
打印 正则.搜索("\\汉+", "abc中国def")。
打印 正则.搜索("找不到", "abc")。

-- 全部替换（导出名叫 替代，见 正则.jk 的命名说明）
打印 正则.替代("世界", "中国", "你好，世界！世界你好。")。

-- 编译后重复使用同一模式
定义 赵模式 = 正则.编译("[0-9]+")。
打印 正则.搜索(赵模式, "价格 199 元")。
打印 正则.搜索(赵模式, "版本 v3 更新")。