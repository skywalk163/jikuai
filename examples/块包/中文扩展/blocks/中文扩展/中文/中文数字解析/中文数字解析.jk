-- 极快 v0.19.0 块包 · 中文扩展.中文数字解析
-- 一级块（层级 1）——中文数字串 → 整数。
--
-- 命名（ADR-15 §3.7）：目录 `中文数字解析`、导出 `解汉数` 均已验证词法原子。
--
-- 【为什么不依赖 文本切分】原设想用 `切片(s, "")` 逐字切开，但 `分割 s ""`
--   在 Python 对空串 split 抛 ValueError，走不通。改为逐下标 `子串 s i (i+1)`
--   取字，因此**不导入任何块**，`依赖块` 为 `[]`（G11 要求声明与实际 import 一致）。
--
-- 【算法】字典查位 + 累乘：
--   1. 逐字遍历，对每个字查「数字表」和「位权表」。
--   2. 遇数字字 → 记到 `赵当前`。
--   3. 遇位权字（十百千万亿）→ 乘入 `赵段`。
--      万/亿是**节权**，触发时先把低节收进高节。
--   4. "零" 只推进指针不产数值。
--   5. 遍历完把残余的 `赵当前` / `赵段` 加进总数。
--
-- 支持范围：一~九、十百千、万、亿（十万/百万/千万/亿 级别的组合）。
-- 不支持：「两」（可后续追加）、小数、负数。
--
-- ⚠️ 全程不写 `变量 (子表达式)`：IDENT 紧跟 `(` 会被当函数调用（W67 在
--    分位数.jk 踩过），所以每个复合算式都拆成命名中间变量。

函数 解汉数 接收 赵汉数：
  定义赵数表={"零": 0, "一": 1, "二": 2, "三": 3, "四": 4, "五": 5, "六": 6, "七": 7, "八": 8, "九": 9}。
  定义赵位表={"十": 10, "百": 100, "千": 1000, "万": 10000, "亿": 100000000}。
  定义赵总=0。
  定义赵段=0。
  定义赵当前=0。
  定义赵长=长度 赵汉数。
  定义赵位=0。
  当 小于 赵位 赵长：
    定义赵下=加 赵位 1。
    定义赵字=子串 赵汉数 赵位 赵下。
    如果 包含 赵数表 赵字 那么：
      赋值赵当前=取值 赵数表 赵字。
    否则：
      如果 包含 赵位表 赵字 那么：
        定义赵权=取值 赵位表 赵字。
        如果 等于 赵权 10000 那么：
          如果 等于 赵当前 0 那么：
            如果 等于 赵段 0 那么：
              赋值赵段=1。
            。
          否则：
            赋值赵段=加 赵段 赵当前。
          。
          定义赵万值=乘 赵段 10000。
          赋值赵总=加 赵总 赵万值。
          赋值赵段=0。
          赋值赵当前=0。
        否则：
          如果 等于 赵权 100000000 那么：
            如果 等于 赵当前 0 那么：
              如果 等于 赵段 0 那么：
                赋值赵段=1。
              。
            否则：
              赋值赵段=加 赵段 赵当前。
            。
            定义赵亿前=加 赵总 赵段。
            赋值赵总=乘 赵亿前 100000000。
            赋值赵段=0。
            赋值赵当前=0。
          否则：
            如果 等于 赵当前 0 那么：
              赋值赵当前=1。
            。
            定义赵位值=乘 赵当前 赵权。
            赋值赵段=加 赵段 赵位值。
            赋值赵当前=0。
          。
        。
      。
    。
    赋值赵位=赵下。
  。
  赋值赵段=加 赵段 赵当前。
  赋值赵总=加 赵总 赵段。
  返回 赵总。
。

导出 解汉数。
