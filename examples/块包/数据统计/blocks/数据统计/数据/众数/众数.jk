-- 极快 v0.19.0 块包 · 数据统计.众数
-- 原子块（层级 0）——列表中出现次数最多的元素。
--
-- 命名（ADR-15 §3.7）：目录 `众数`、导出 `众数值` 均已验证词法原子。
--
-- 实现：先 `排序`（内置动词），再走一遍统计最长同值段。
--   多值同频时返回排序后最先达到最大计数的那个（`大于 赵计 赵最计` 严格不等）。
--   空列表返回 0（experimental；调用方保证非空以免歧义）。
--   `排序` 是内置动词而非另一个块，故 `依赖块` 为空。

函数 众数值 接收 赵数值：
  定义赵长=长度 赵数值。
  如果 等于 赵长 0 那么：
    返回 0。
  。
  定义赵顺=排序 赵数值。
  定义赵前=取值 赵顺 0。
  定义赵计=1。
  定义赵最众=赵前。
  定义赵最计=1。
  定义赵位=1。
  当 小于 赵位 赵长：
    定义赵当=取值 赵顺 赵位。
    如果 等于 赵当 赵前 那么：
      赋值赵计=加 赵计 1。
    否则：
      赋值赵计=1。
      赋值赵前=赵当。
    。
    如果 大于 赵计 赵最计 那么：
      赋值赵最计=赵计。
      赋值赵最众=赵前。
    。
    赋值赵位=加 赵位 1。
  。
  返回 赵最众。
。

导出 众数值。
