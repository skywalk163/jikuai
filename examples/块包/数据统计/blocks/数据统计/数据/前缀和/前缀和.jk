-- 极快 v0.19.0 块包 · 数据统计.前缀和
-- 原子块（层级 0）——返回累计和列表。
--
-- 命名（ADR-15 §3.7）：目录 `前缀和`、导出 `累和表` 均已验证词法原子。
--
-- 实现：遍历累加，追加到结果列表。

函数 累和表 接收 赵数值：
  定义赵集=列。
  定义赵累=0。
  遍历赵项于赵数值：
    赋值赵累=加 赵累 赵项。
    赋值赵集=追加 赵集 赵累。
  。
  返回 赵集。
。

导出 累和表。
