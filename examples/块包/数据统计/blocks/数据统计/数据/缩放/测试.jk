-- 块 `数据统计.数据.缩放` 的单元测试。
从 blocks.数据统计.数据.缩放 导入 缩放。

-- 测试1：普通列表 → [0, 0.25, 0.5, 0.75, 1]
打印 缩放(列 1 2 3 4 5)。

-- 测试2：全等值 → [0, 0, 0]
打印 缩放(列 3 3 3)。

-- 测试3：含负数 → min=-2, max=8, 范围=10
打印 缩放(列 -2 3 8)。
