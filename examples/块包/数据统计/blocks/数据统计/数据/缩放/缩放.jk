-- 极快 v0.19.0 块包 · 数据统计.缩放
-- 一级块（层级 1）——min-max 归一化到 [0,1]。
--
-- 命名（ADR-15 §3.7）：原名 `归一化` 会被切成 `归(ADVERB)+1(NUMBER)+化(IDENT)`，
--   故改 `缩放`（单 IDENT ✓）。导出名同 `缩放`。
--
-- 实现：(x - min) / (max - min)。极差为 0 时全部返回 0（等值序列定义为零）。
-- 依赖 stdlib `极值` 块取 [min, max]。

从 blocks.数据.极值 导入 峰谷。

函数 缩放 接收 赵数值：
  定义赵端=峰谷(赵数值)。
  定义赵小=取值 赵端 0。
  定义赵大=取值 赵端 1。
  定义赵差=减 赵大 赵小。
  如果 等于 赵差 0 那么：
    定义赵零集=列。
    遍历赵项于赵数值：
      赋值赵零集=追加 赵零集 0。
    。
    返回 赵零集。
  。
  定义赵集=列。
  遍历赵项于赵数值：
    定义赵移=减 赵项 赵小。
    定义赵比=除 赵移 赵差。
    赋值赵集=追加 赵集 赵比。
  。
  返回 赵集。
。

导出 缩放。
