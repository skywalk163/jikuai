-- 块 `网页数据.网络.小饼解析` 的单元测试。
从 blocks.网页数据.网络.小饼解析 导入 饼表。

-- 测试1：标准两项（分号 + 空格分隔）
打印 饼表("a=1; b=2")。

-- 测试2：真实浏览器形态（无空格 + 多项）
打印 饼表("sessionid=abc123;theme=dark;lang=zh-CN")。

-- 测试3：夹带无 `=` 的 flag 段 → 跳过
打印 饼表("token=xyz; HttpOnly; path=/")。
