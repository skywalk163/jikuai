-- 极快 v0.19.0 块包 · 财务扩展.成本溢价
-- 原子块（层级 0）——按成本 × (1 + 加成率) 反推定价。
--
-- 命名（ADR-15 §3.7）：
--   原名 `成本加成` 会被切成 `成本(IDENT)+加(VERB)+成(IDENT)`（加是动词），
--   改 `成本溢价`（已验证单 IDENT）。
--   导出名原 `加成价` 同因 `加(VERB)` 被切，改 `成价`。
--   形参原 `赵加率` 同因 `加(VERB)` 被切，改 `赵成率`。
--
-- 加成率是小数（0.3 表示加价 30%）。分位收口由调用方决定，本块不圆分。

函数 成价 接收 赵成本 赵成率：
  定义赵倍=加 1 赵成率。
  返回 乘 赵成本 赵倍。
。

导出 成价。
