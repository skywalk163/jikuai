-- 极快 v0.19.0 块包 · 财务扩展.毛利率
-- 原子块（层级 0）——毛利率 = (售价 − 成本) ÷ 售价。
--
-- 命名（ADR-15 §3.7）：目录 `毛利率`、导出 `毛利` 均已验证词法原子。
--
-- 返回小数（`0.3333...`），不做百分比转换、不做四舍五入——
-- 由调用方决定报表形态（乘 100、圆分、保留分）。
--
-- 边界：售价为 0 会产生除零错，experimental 阶段不做防御；调用方保证售价 > 0。

函数 毛利 接收 赵售价 赵成本：
  定义赵差=减 赵售价 赵成本。
  返回 除 赵差 赵售价。
。

导出 毛利。
