-- 极快 v0.12.0 块生态：中文.农历
-- 原子块（层级 0）——把公历年月日转成完整农历日期字符串。
-- 导出名 `阴历` 已验证词法原子（ADR-15 §3.7）：tokenize('阴历。') → [('IDENT','阴历')]。
--
-- 为什么不叫 `农历`：`农历` 本身是极快内建动词（keywords.py 第 120 行），
-- 用作导出名会被 lexer 切成 VERB，调用方一侧必然引用失败。
-- 形参 `赵年`/`赵月`/`赵日` 三个均已验证原子。
--
-- 底层复用内建动词 `农历完整日期`（元数 3，免括号前缀调用）。

函数 阴历 接收 赵年 赵月 赵日：
  返回 农历完整日期 赵年 赵月 赵日。
。

导出 阴历。
