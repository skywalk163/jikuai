-- 极快 v0.13.0 块生态：中文.叠词
-- 原子块（层级 0）——把词/字重复一遍构成叠词（"开" → "开开"，"漂亮" → "漂亮漂亮"）。
--
-- 命名（ADR-15 §3.7）：导出 `重言`、目录 `叠词`、形参 `赵字` 均已跑原子性预检。
-- 场景：儿化口语、口令重叠强调、儿童教育文案。
-- 实现：纯极快，`拼接 赵字 赵字`，两次都 str()——数字也能叠 ("3" → "33")。

函数 重言 接收 赵字:
  返回 拼接 赵字 赵字。
。

导出 重言。
