-- 极快 v0.13.0 块生态：中文.序数中文
-- 原子块（层级 0）——把整数转成汉字序数："第一"/"第二"/"第十"…。
--
-- 命名（ADR-15 §3.7）：导出 `序汉`、目录 `序数中文`、形参 `赵数` 均已跑原子性预检。
-- 与 `中文.数字中文` 的区别：本块负责序数（带 `第` 前缀），本块调用 `汉字数字` 得基数。
-- 用中间变量拆开是为了绕开「变参内建里嵌固定元数动词」的解析陷阱（坑 #1）。

函数 序汉 接收 赵数:
  定义赵汉=汉字数字 赵数。
  返回 拼接 "第" 赵汉。
。

导出 序汉。
