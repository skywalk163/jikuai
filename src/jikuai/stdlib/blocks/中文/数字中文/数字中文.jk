-- 极快 v0.12.0 块生态：中文.数字中文
-- 原子块（层级 0）——把阿拉伯数字转成汉字小写数字（一二三…）。
-- 导出名 `汉数` 已验证词法原子（ADR-15 §3.7）：tokenize('汉数。') → [('IDENT','汉数')]。
--
-- 为什么不叫 `汉字数字`：`汉字数字` 是极快内建动词（keywords.py 第 122 行）。
-- 取缩写 `汉数`（汉字数字的简称）作为原子导出名。
-- 形参 `赵数值` 已验证原子。
--
-- 底层复用内建动词 `汉字数字`（元数 1，先 int(a) 再转，返回小写数字字符串）。

函数 汉数 接收 赵数值：
  返回 汉字数字 赵数值。
。

导出 汉数。
