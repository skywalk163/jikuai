-- 极快 v0.14.0 块生态：中文.用户档案
-- 二级块（层级 2）——把「姓名 + 证号 + 地址 + 今日」聚成一张结构化档案：
--   [姓, 名, 证号是否有效, 省, 市, 区, 周岁]
--
-- 导出名 `档帖` 已验证词法原子（ADR-15 §3.7）：
--   check_export_atomicity('档帖') → (True, [('IDENT', '档帖')])
-- 与兄弟块 `阴历日程`(历帖) / `生辰`(命帖) 的 `…帖` 命名族保持一致。
--
-- 聚合四个层级 0 原子块，跨 中文 / 历法 两个领域：
--   中文.姓名拆分 → 析名(全)                        → [姓, 名]
--   中文.身份证   → 甄别(证号)                      → 布尔
--   中文.地址剖解 → 拆址(址)                        → [省, 市, 区, 街道]
--   历法.周岁     → 实岁(生年,生月,生日,今年,今月,今日) → 数
--
-- 【出生日期的来源】不额外收 出生年/月/日 三个入参——身份证号第 7~14 位
-- （0 基下标 6..14）就是 YYYYMMDD。极快内建 `子串 串 起 止`（元数 3，语义
-- 等价 Python `a[b:c]`）与 `转整数`（元数 1）足以切出来，故按需求原案实现，
-- 入参维持 姓名/证号/地址/今年/今月/今日 六个。
--
-- 【无效证号的兜底】证号不合法时（长度不足、非数字、校验位错）证号里的
-- 日期位不可信，甚至切不出 8 位数字，硬转会抛异常。因此 `岁算` 先过 `甄别`，
-- 不合法直接返回哨兵 −1 表示「周岁未知」，由调用方结合第 3 位的 假 判读。
--
-- 形参与局部名 赵姓名/赵证号/赵址/赵今年/赵今月/赵今日/赵名对/赵有效/赵址组/
-- 赵姓/赵名/赵省/赵市/赵区/赵证/钱生年/钱生月/钱生日/钱岁/钱负 均已跑原子性预检。

从 blocks.中文.姓名拆分 导入 析名。
从 blocks.中文.身份证 导入 甄别。
从 blocks.中文.地址剖解 导入 拆址。
从 blocks.历法.周岁 导入 实岁。

-- 私有辅助：从证号切出生日再算周岁；证号不合法返回 −1（未知）。
函数 岁算 接收 赵证 赵今年 赵今月 赵今日：
  如果 甄别(赵证) 那么：
    定义钱生年=转整数 (子串 赵证 6 10)。
    定义钱生月=转整数 (子串 赵证 10 12)。
    定义钱生日=转整数 (子串 赵证 12 14)。
    返回 实岁(钱生年, 钱生月, 钱生日, 赵今年, 赵今月, 赵今日)。
  。
  定义钱负=0减1。
  返回 钱负。
。

函数 档帖 接收 赵姓名 赵证号 赵址 赵今年 赵今月 赵今日：
  定义赵名对=析名(赵姓名)。
  定义赵姓=取值 赵名对 0。
  定义赵名=取值 赵名对 1。
  定义赵有效=甄别(赵证号)。
  定义赵址组=拆址(赵址)。
  定义赵省=取值 赵址组 0。
  定义赵市=取值 赵址组 1。
  定义赵区=取值 赵址组 2。
  定义钱岁=岁算(赵证号, 赵今年, 赵今月, 赵今日)。
  返回 列 赵姓 赵名 赵有效 赵省 赵市 赵区 钱岁。
。

导出 档帖。
