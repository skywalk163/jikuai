-- 极快 v0.12.0 块生态：中文.繁简
-- 原子块（层级 0）——把简体文本转为繁体。
-- 导出名 `转繁` 已验证词法原子（ADR-15 §3.7）：tokenize('转繁。') → [('IDENT','转繁')]。
--
-- 为什么不叫 `转繁体`：`转繁体` 是 stdlib 简繁模块的导出名，同名会遮蔽本块导入的
-- 桥函数，造成自引用递归。取 `转繁` 与底层区分。
-- 目录名 `繁简` 本身已是单个 IDENT，可直接作点分路径片段。
-- 形参 `赵文本` 已验证原子。
--
-- 底层复用 stdlib 混合模块 `简繁`（实现在 简繁.py）的 `转繁体`。
-- 注意：`转繁体` 是 Python 桥函数，**必须括号调用**，不能用免括号前缀形式。

从 简繁 导入 转繁体。

函数 转繁 接收 赵文本：
  返回 转繁体(赵文本)。
。

导出 转繁。
