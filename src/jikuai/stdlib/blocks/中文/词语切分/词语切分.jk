-- 极快 v0.12.0 块生态：中文.词语切分
-- 原子块（层级 0）——对中文文本做分词，返回词语列表。
-- 导出名 `断词` 已验证词法原子（ADR-15 §3.7）：tokenize('断词。') → [('IDENT','断词')]。
--
-- 为什么不叫 `分词`：`分词` 是 stdlib 分词模块的导出名，本块要导入它；
-- 同名会遮蔽桥函数造成自引用。取 `断词`（断句切词）与底层区分。
-- 目录名 `词语切分` 已验证是单个 IDENT，可直接作点分路径片段。
-- 形参 `赵文本` 已验证原子。
--
-- 底层复用 stdlib 混合模块 `分词`（实现在 分词.py，正向最大匹配 FMM）。
-- `分词` 是 Python 桥函数，**必须括号调用**；返回 Python 列表。

从 分词 导入 分词。

函数 断词 接收 赵文本：
  返回 分词(赵文本)。
。

导出 断词。
