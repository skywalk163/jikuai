-- 极快 v0.12.0 块生态：中文.身份证
-- 原子块（层级 0）——校验中国大陆身份证号，返回真/假。
-- 导出名 `甄别` 已验证词法原子（ADR-15 §3.7）：tokenize('甄别。') → [('IDENT','甄别')]。
--
-- 为什么不叫 `校验身份证`：它是极快内建动词（keywords.py 第 124 行）。
-- 取 `甄别`（甄别真伪）作为原子导出名。
-- 目录名 `身份证` 本身已是单个 IDENT，可直接作点分路径片段。
-- 形参 `赵证号` 已验证原子。
--
-- 底层复用内建动词 `校验身份证`（元数 1，走 stdlib 校验模块，返回布尔）。

函数 甄别 接收 赵证号：
  返回 校验身份证 赵证号。
。

导出 甄别。
