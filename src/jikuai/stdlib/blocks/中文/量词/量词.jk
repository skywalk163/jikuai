-- 极快 v0.13.0 块生态：中文.量词
-- 原子块（层级 0）——把「数字 + 中文量词」拼成中文数量表达（3, "个" → "三个"）。
--
-- 命名（ADR-15 §3.7）：导出 `计量`、目录 `量词`、形参 `赵数`/`赵单` 均已跑原子性预检。
-- 底层依赖内建 `汉字数字` 生成中文基数，再 `拼接` 附单位。

函数 计量 接收 赵数 赵单:
  定义赵汉=汉字数字 赵数。
  返回 拼接 赵汉 赵单。
。

导出 计量。
