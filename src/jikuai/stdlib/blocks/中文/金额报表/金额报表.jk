-- 极快 v0.12.0 块生态：中文.金额报表
-- 一级块（层级 1）——输入数值，返回 [数值, 中文大写金额, 汉字数字] 三元列表。
-- 导出名 `账单` 已验证词法原子（ADR-15 §3.7）：tokenize('账单。') → [('IDENT','账单')]。
-- 目录名 `金额报表` 已验证是单个 IDENT，可直接作点分路径片段。
--
-- 依赖两个层级 0 原子块：
--   中文.金额雅写 → 银码(数值) → 中文大写金额字符串（壹贰叁…元角分整）
--   中文.数字中文 → 汉数(数值) → 汉字小写数字字符串（一二三）
-- 用户块函数**必须括号调用**。
-- 形参 `赵数值` 已验证原子。
--
-- 注意底层语义差异（来自内建动词，非本块引入）：
--   `大写金额` 保留小数（角/分），`汉字数字` 内部先做 int() 截断。
--   故 账单(1234.56) 的第三元是「一千二百三十四」而不含小数部分。

从 blocks.中文.金额雅写 导入 银码。
从 blocks.中文.数字中文 导入 汉数。

函数 账单 接收 赵数值：
  返回 列 赵数值 银码(赵数值) 汉数(赵数值)。
。

导出 账单。
