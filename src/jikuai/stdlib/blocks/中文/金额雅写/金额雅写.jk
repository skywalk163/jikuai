-- 极快 v0.12.0 块生态：中文.金额雅写
-- 原子块（层级 0）——把数值转成中文大写金额（壹贰叁肆…元角分整）。
-- 导出名 `银码` 已验证词法原子（ADR-15 §3.7）：tokenize('银码。') → [('IDENT','银码')]。
--
-- 【换名记录 1】导出名：`大写金额` → `银码`
--   `大写金额` 是极快内建动词（keywords.py 第 121 行）；且 `大写` 单独也是内建动词，
--   任何含 `大写` 字样的名字都会被切碎。取旧账房「银两码字」的雅称 `银码`。
--
-- 【换名记录 2】目录名：`金额大写` → `金额雅写`
--   目录名要作为点分导入路径的一段，必须是单个词形 token。
--   `金额大写` 被切成 `金额`(IDENT) + `大写`(VERB) 两个 token，导入语句会报语法错误。
--   `金额雅写` 整体成词为单个 IDENT，故改用它。
--
-- 形参 `赵数值` 已验证原子。
-- 底层复用内建动词 `大写金额`（元数 1，接受数或人民币对象，返回中文大写金额字符串）。

函数 银码 接收 赵数值：
  返回 大写金额 赵数值。
。

导出 银码。
