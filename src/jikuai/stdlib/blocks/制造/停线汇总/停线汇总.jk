-- 极快 v0.26.0 W142 · 制造域口径层（ADR-40 §3.2）：制造.停线汇总
-- 口径块（层级 0）——按维度列汇总 `fact_production_actual.downtime_minutes`。
--
-- 导出名 `计停` 已过两道验名（v0.26.0 WBS 执行原则 7）：
--   1. 词法原子：check_export_atomicity('计停') → (True, [('IDENT', '计停')])
--   2. 全局唯一：索引.json 125 条的 125 个导出名里无 `计停`
--
-- 【列名与所在表是自己读表头核实的，不是抄文档】
--   `fact_production_actual` 真实表头（utf-8-sig 读出）：
--     actual_id, plan_id, production_date, line_id, model_id, shift,
--     actual_quantity, working_hours, downtime_minutes, achievement_rate
--   所以停线时长列名是 `downtime_minutes`，在**生产实绩表**上——不是计划表、
--   不是能耗表。粒度：日 × 产线 × 车型 × 班次。
--
-- 【单位陷阱，本块反复点名的一条】`downtime_minutes` 是**分钟**，而同表
--   `working_hours` 是**小时**。停线占比 = downtime_minutes / (working_hours × 60)，
--   直接相除会差 60 倍。本块**只汇总分钟数、不算占比**，就是为了不把这个换算
--   藏进一个看不见的地方。
--
-- 【空值口径：这一列实测零空值，但本块照样排除并计数】
--   ADR-40 §4.3：全数据集只有 fact_orders 两列有空值。`downtime_minutes` 零空值。
--   本块仍然显式排除空值行、报出排除条数，两条理由：
--     1. 零空值是**这份数据集的事实，不是本块的前提**。把它写进假设，遇到
--        真有停线未记录的数据时会静默把空当 0 分钟，得出「这条线没停过」。
--     2. 这一列里**有 128 行是 0**（W139 实测）。0 = 那个班次真的没停线，
--        是有效数据、进分母；空 = 没记录、不可比。本块另出 `零停线记录数` 列，
--        让「真没停」看得见，不至于和「没数据」在同一个 0 上打架。
--
-- 【本块不排序、不切窗口】结果按维度键升序（可复现）。
--   「4-08~4-18 哪条线停线偏高」= 先 `制造.窗口`（`截期`）切窗口 → 本块按
--   line_id 汇总 → `制造.排序` + `制造.取前N` 排名。窗口口径与排序口径都必须
--   由调用方声明，不许块替使用者猜，否则溯源说明写不出来。
--
-- 实现走 Python 背衬（ADR-16 §3.3）：见同目录 `停线汇总.py`。下沉理由同
-- `制造.分组汇总`——分组建桶、含空值维度键的确定性排序、以维度列名为键动态
-- 构造结果字典，纯 .jk 写起来又长又易错；判空还要看运行时类型，极快侧没有
-- 内建的类型判定动词。背衬只用 Python 标准库，零第三方依赖。
--
-- 【表参数不受类型图守护】ADR-40 §3.1 的 `值类型: 任意` 让 type_feeds 双向
--   放行。正确性由 `测试.jk` 与 tests/test_v0_26_0_w142_交付停线.py 保证。
--
-- 稳定性 experimental：依赖 pybridge，且口径层接口形状本轮仍可能调整。

导出 计停。
