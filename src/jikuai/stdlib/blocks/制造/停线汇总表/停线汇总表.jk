-- 极快 v0.28.0 W167 · 制造域口径层姊妹出口：制造.停线汇总表
-- 复合块（层级 1）——`制造.停线汇总`（导出 `计停`）的「只出汇总表」出口。
--
-- 导出名 `计停表` 与目录名 `停线汇总表` 都已过两道验名（v0.28.0 WBS 执行原则 7）：
--   1. 目录名词法原子：check_module_segment_atomicity('停线汇总表')
--      → (True, [('IDENT', '停线汇总表')])       ← 目录名进点分路径段，必须先验这个
--   2. 导出名词法原子：check_export_atomicity('计停表')
--      → (True, [('IDENT', '计停表')])
--   3. 全局唯一：索引.json 139 条的 139 个导出名里既无 `计停表` 也无块名 `停线汇总表`
--
-- 【第三处要验的名：形参名】W167 写这个块时真栽过一次——原先形参叫 `赵维度列`，
--   词法器把它切成 `IDENT 赵维度` + `VERB 列`（`列` 是变参列表构造动词），于是
--   `计停(赵表, 赵维度列)` 编译成 `计停(赵表, 赵维度, 列())` 三个实参，运行期抛
--   `计停() takes 2 positional arguments but 3 were given`，且错误指向调用行、
--   完全不提形参名。`jk 块 新建` 里本来有这道预检（`pkg/blocks_cli.py` 的
--   `形参名` 原子性拒绝），但手写文件建块**绕过了它**。所以形参名一律先验：
--     check_export_atomicity('赵表')       → (True, [('IDENT', '赵表')])
--     check_export_atomicity('赵维度清单') → (True, [('IDENT', '赵维度清单')])
--     check_export_atomicity('赵维度列')   → (False, [('IDENT','赵维度'),('VERB','列')])  ← 反例
--   凡以内建动词/关键字结尾的名（`赵列表`/`赵次`/`赵长度`）都会被切碎。

--
-- 【立项理由是接线形状，不是新口径】BACKLOG §12.3 缺口 1 + 缺口 6 是同一个结：
--   `计停` 出的是元组 `(汇总表, 排除的空值行数)`，而 `制造.排序`（`定序`）与
--   `制造.取前N`（`摘前`）吃的是**列表**。元组接不上列表输入，于是「按维度汇总
--   停线 → 排序 → 取前 N」这条链在方案 JSON 里断在中间，得靠人手写一步
--   `【0】` 拆包才能续上。本块把那一步固化成一个块，让整条链能直连。
--
-- 【口径一个数不改】本块**不重算任何东西**，只调 `计停` 再取元组第 1 项。所以
--   分母仍是 `计入记录数`（不是 `记录数`）、0 分钟仍是有效数据进分母、空值仍被
--   排除、负分钟仍报错、行序仍按维度键升序（空值键组排最前）。这正是 AGENTS.md
--   第四节要求的做法：有现成口径就别重写一份——重写出来的「差一档」口径会跑通
--   但算错。
--
-- 【它丢掉了什么，要说清】元组第 2 项「排除的空值行数」在本块的返回值里**没有**。
--   要审计缺测条数，用 `制造.停线汇总`（`计停`）拿完整元组。本块是为「下游能直接
--   排序」而生的出口，**不是** `计停` 的替代品。每组的 `缺测记录数` 列仍在汇总表里，
--   所以逐组的缺测信息并未丢失，丢的只是那个整表合计数。
--
-- 【为什么不改粘合器支持声明式拆包】（本轮自裁的取舍，偏离点已记在 v0.28.0 WBS）
--   拆包是类型图与粘合器的语言级能力，牵动 `pkg/blocks.py` 的类型词表 + G14 +
--   `bench_glue.py` 指标，属 v0.29.0「语言级表类型」那条线。而缺口本身只卡在
--   三个块的出口形状上，加姊妹块是局部、可测、不动语言的解。
--
-- 【本块没有 Python 背衬】纯 `.jk`，所以 `块背衬PY数`（check_wheel_contents.py）
--   不用动。取元组下标一个动词都不需要，下沉到 Python 没有任何收益。
--
-- 稳定性 experimental：口径层接口形状本轮仍可能调整（与 `停线汇总` 同档）。

从 blocks.制造.停线汇总 导入 计停。

函数 计停表 接收 赵表 赵维度清单：
  定义 赵元组 = 计停(赵表, 赵维度清单)。
  返回 赵元组【0】。
。

导出 计停表。
