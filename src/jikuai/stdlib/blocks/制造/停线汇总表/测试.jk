-- 块 `制造.停线汇总表` 的单元测试（W167 · v0.28.0，导出名 `计停表`）。
--
-- 本块的全部价值在**出口形状**：`计停` 出元组、本块出列表，于是能直连
-- `制造.排序`（`定序`）+ `制造.取前N`（`摘前`）。所以测试的重点不是重测停线口径
-- （那是 `停线汇总/测试.jk` 的事），而是钉死两件事：
--   ① 本块返回值**逐元素等于** `计停(...)【0】`——口径一个数都没被本块改动；
--   ② 「汇总 → 排序 → 取前 N」这条链在**不手动拆包**的情况下真的跑通。
--
-- 另外钉一条反向断言：本块返回的**不是**元组——`长度` 在元组上会得到元数 2，
-- 在汇总表上得到组数。拿一个组数 ≠ 2 的输入就能把「忘了取【0】」当场抓出来。
--
-- 写法注记（沿用 `停线汇总/测试.jk`）：列表/字典字面量一律先落到变量再传参。

从 blocks.制造.停线汇总表 导入 计停表。
从 blocks.制造.停线汇总 导入 计停。
从 blocks.制造.排序 导入 定序。
从 blocks.制造.取前N 导入 摘前。
从 blocks.工具.断言 导入 验实。

-- ① 与 计停 的元组第 1 项逐元素相同（口径没被本块动过）
定义 赵表 = 【
  {"line_id": "L001", "downtime_minutes": 10},
  {"line_id": "L002", "downtime_minutes": 20},
  {"line_id": "L001", "downtime_minutes": 30}
】。
定义 赵维 = 【"line_id"】。
定义 赵元组 = 计停(赵表, 赵维)。
定义 赵表果 = 计停表(赵表, 赵维)。
打印 "① 只出汇总表："。
打印 赵表果。
验实(赵表果, 赵元组【0】)。
验实(长度 赵表果, 2)。
验实(赵表果【0】["line_id"], "L001")。
验实(赵表果【0】["停线合计分钟"], 40)。
验实(赵表果【0】["停线均值分钟"], 20)。
验实(赵表果【1】["line_id"], "L002")。
验实(赵表果【1】["停线合计分钟"], 20)。

-- ② ★ 出口是列表不是元组：三组输入下 长度 = 3；若忘了取【0】，
--    拿到的是元组、长度会是 2。这条把「姊妹块退化成原块」当场抓住。
定义 钱表 = 【
  {"line_id": "L001", "downtime_minutes": 1},
  {"line_id": "L002", "downtime_minutes": 2},
  {"line_id": "L003", "downtime_minutes": 3}
】。
定义 钱果 = 计停表(钱表, 赵维)。
打印 "② 三组 → 长度 3（元组会是 2）："。
打印 钱果。
验实(长度 钱果, 3)。
验实(等于 (长度 钱果) 2, 假)。

-- ③ ★ 直连 定序 + 摘前：这是本块的立项理由——中间没有任何手动拆包
定义 孙表 = 【
  {"line_id": "L001", "downtime_minutes": 10},
  {"line_id": "L002", "downtime_minutes": 90},
  {"line_id": "L003", "downtime_minutes": 50},
  {"line_id": "L002", "downtime_minutes": 5}
】。
定义 孙汇 = 计停表(孙表, 赵维)。
定义 孙排 = 定序(孙汇, "停线合计分钟", 真)。
定义 孙榜 = 摘前(孙排, 2)。
打印 "③ 汇总 → 降序 → 取前 2（无手动拆包）："。
打印 孙榜。
验实(长度 孙榜, 2)。
验实(孙榜【0】["line_id"], "L002")。
验实(孙榜【0】["停线合计分钟"], 95)。
验实(孙榜【1】["line_id"], "L003")。
验实(孙榜【1】["停线合计分钟"], 50)。

-- ④ 维度列传空列表 → 整表一行（与 计停 同口径）
定义 李空维 = 【】。
定义 李果 = 计停表(赵表, 李空维)。
打印 "④ 整表一行："。
打印 李果。
验实(长度 李果, 1)。
验实(李果【0】["记录数"], 3)。
验实(李果【0】["停线合计分钟"], 60)。

-- ⑤ 空表 → 空列表（不是 空、不是元组）
定义 周零 = 【】。
定义 周果 = 计停表(周零, 赵维)。
打印 "⑤ 空表 → 空列表："。
打印 周果。
验实(长度 周果, 0)。
验实(周果, 【】)。

-- ⑥ 缺测信息没全丢：逐组的 缺测记录数 列仍在（丢的只是整表合计那个数）
定义 吴表 = 【
  {"line_id": "L004", "downtime_minutes": 0},
  {"line_id": "L004", "downtime_minutes": 空},
  {"line_id": "L004", "downtime_minutes": 20}
】。
定义 吴果 = 计停表(吴表, 赵维)。
打印 "⑥ 逐组缺测列仍在："。
打印 吴果。
验实(吴果【0】["缺测记录数"], 1)。
验实(吴果【0】["计入记录数"], 2)。
验实(吴果【0】["零停线记录数"], 1)。
验实(吴果【0】["停线均值分钟"], 10)。

-- ⑦ 行序确定：同一输入两次调用完全相同
验实(计停表(孙表, 赵维), 计停表(孙表, 赵维))。

-- ⑧ 反例：报错照旧从 计停 透传上来（本块不吞异常）
定义 郑捕 = 假。
定义 郑坏 = 【{"line_id": "L001", "planned_quantity": 100}】。
尝试：
  计停表(郑坏, 赵维)。
捕获 赵错一：
  郑捕 = 真。
  打印 "⑧ 停线时长列缺失报错透传："。
  打印 转字符串 赵错一。
。
验实(郑捕, 真)。

-- ⑨ 反例：负分钟照旧报错（口径没被姊妹块放松）
定义 卫捕 = 假。
定义 卫坏 = 【{"line_id": "L001", "downtime_minutes": -5}】。
尝试：
  计停表(卫坏, 赵维)。
捕获 赵错二：
  卫捕 = 真。
  打印 "⑨ 负分钟报错透传："。
  打印 转字符串 赵错二。
。
验实(卫捕, 真)。

打印 "制造.停线汇总表：全过"
