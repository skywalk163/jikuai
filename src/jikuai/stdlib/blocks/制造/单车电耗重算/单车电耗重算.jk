-- 极快 v0.26.0 制造域口径层（ADR-40 §3.2 / §5.2）：制造.单车电耗重算（导出名 车耗算）
-- 口径块（层级 0）——**分歧点 2 的「重算」这一侧**。
--
-- 【口径自报：分母来源写死】
--   单车电耗 = sum(electricity_kwh) / sum(actual_quantity)。
--   分子 electricity_kwh 来自 fact_energy_usage；**分母 actual_quantity 来自
--   fact_production_actual**（能耗表里根本没有产量列——energy_per_vehicle 是数据
--   生成时算好的，其分母就取自产量表，ADR-40 §5.2）。两表按
--   line_id + model_id + 日期 + shift **四键关联**再各自求和后相除，属**产量加权**口径。
--   日期列两表名字不同（能耗 usage_date、产量 production_date），本块内部按位对齐、
--   不要求两边同名。
--
-- 【另一侧口径：制造/单车电耗现成（导出 车耗现）】
--   那个块直接取现成列 energy_per_vehicle 求**记录等权**的算术平均，不碰产量表。
--   两块在候选列表里并排出现，选块的人自己拍：要数据字典定义的现成值用 车耗现，
--   要按真实产量加权 + 交叉校验现成列对不对用本块。
--
-- 【本块的价值，务必写清】2026-08-18 实测两口径在本数据集上**数值等值**
--   （2896/2896 行逐行核对，最大绝对差 0.005 系现成列两位小数舍入、最大相对差
--   5.4e-5、零漏配）。所以本块**不是**用来在本数据集上算出不同的数——它的价值是：
--     (a) 换一份真实工厂数据、两个口径会分叉时，重算块已经在，不用临时补；
--     (b) 交叉校验现成列 energy_per_vehicle 到底对不对（本轮就是靠它证明了等值）。
--   等值的原因是数据生成时口径一致，不是本块写错了。
--
-- 【分母为 0 / 分母键缺失：不静默当 0，与 制造/缺陷率 同口径】
--   · 某维度组的 sum(actual_quantity) = 0（停产）→ 单车电耗返回 空，产量合计如实给 0；
--   · 能耗有该维度组、但四键在产量表里一条都关联不上（分母键缺失）→ 单车电耗返回 空、
--     产量合计返回 空（不是 0），该组照常出现在结果里。
--   两条都不报错也不丢弃，理由同 缺陷率.py。
--
-- 实现走 Python 背衬 单车电耗重算.py（ADR-16 §3.3）：四键哈希对齐 + 两侧求和 + 除法
-- 边界。零第三方依赖。
--
-- 导出名 车耗算 已过两道验名（WBS 执行原则 7）：词法原子 + 全局唯一（G13）。

导出 车耗算。
