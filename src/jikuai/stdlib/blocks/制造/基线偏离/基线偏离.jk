-- 极快 v0.27.0 W155 · 制造域口径层（ADR-40 §3.2 / §5.4）：制造.基线偏离
-- 口径块（层级 0）——观察窗相对**自身基线窗**的升幅。
-- **这是 Q_PUB_004（「哪条产线单车电耗异常升高」）的正确答案形状。**
--
-- 【两道验名都过（v0.26.0 WBS 执行原则 7）】
--   1. 目录名词法原子：check_module_segment_atomicity('基线偏离')
--      → (True, [('IDENT', '基线偏离')])，可作点分路径段导入
--   2. 导出名词法原子 + 全局唯一：check_export_atomicity('基偏') → (True, [IDENT])，
--      索引.json 137 条的导出名里无 `基偏`
--   特别核过的字：`线`/`偏`/`离` 都不在 VERB / KEYWORD / ADVERB 词表里
--   （v0.26.0 栽掉三个块的 `次`/`列`/`归` 才在）。
--
-- 【姊妹块 · 互相点名】ADR-40 §5.4 分歧点的两侧：
--   - 本块 `基线偏离`（`基偏`）：**相对自身基线，不是横向比较**。基线窗代表
--     「这条产线平时什么水平」，口径**不对称**——调换两个入表不是简单变符号
--     （分母换了），会得到另一个数。主输出是**升幅比率**。
--   - `制造.窗间对比`（`窗比`）：两个**平级**窗口相比，谁当基期由调用方指定，
--     口径对称，主输出是**绝对量差值**。「六月比五月多产了多少台」用它。
--
-- ══════════════════════════════════════════════════════════════════
-- 坑：**绝对量榜与比率榜的结论不同，这不是 bug**（BACKLOG §12.3 第 5 条）
-- ══════════════════════════════════════════════════════════════════
--   `fact_energy_usage`、观察窗 `2026-06-10..06-24`、基线 = 该窗之前全部日期、
--   按 `line_id` 对现成列 `energy_per_vehicle` 取算术平均，实测：
--     升幅榜（本块）  ：L002 151.8007 → 191.0953 = **+25.89%**，遥遥领先；
--                       其余七条全在 **−3.79% ~ +3.15%**（本底噪声）→ **答案 L002**
--     横向绝对量降序  ：L006 256.5050 第一、L005 247.8620 第二、L007 205.0937 第三，
--                       **L002 191.0953 只排第 4**
--   两个榜结论不同，各自都对：L006 平时就是耗电大户（工艺/车型不同），它没有
--   「升高」；L002 平时 151、这一段 191，**它才是异常升高的那条**。
--   问「哪条电耗最高」用横向榜，问「哪条异常升高」用本块。
--   **后人若发现两榜不一致而去「修正」其中一个，那是把正确答案改错**——
--   所以两个榜的名次都进了断言（`测试.jk` 第二部分 + pytest）。
--
-- ══════════════════════════════════════════════════════════════════
-- 决议：本块**不汇总**，也**不替调用方选基线窗**
-- ══════════════════════════════════════════════════════════════════
--   1. **不汇总**：两个入表须已一行一组（同 `窗间对比`）。求和 / 算术平均 /
--      产量加权是三个不同的答案，替调用方拍一个正是 ADR-40 §5 分歧点的病根。
--      重复键当场报错，文案点名该用哪个汇总块。
--   2. **不替调用方选基线窗**。「基线」是业务判断：上面那组数用的是「观察窗之前
--      全部日期」；换「前 15 天」L002 是 +27.13%、换「同月观察窗之前」是 +26.78%
--      ——**三种基线同一个结论（L002 异常）、数值不同**。基线窗怎么切由调用方用
--      `制造.窗口`（`截期`）决定并自己写进报告。开成参数会让人以为有个
--      「标准基线」，那是本块给不出的承诺。
--
-- 【空 ≠ 0，且三种空分得开】（`班间对比` 的同一条教训，源头是
--   `fact_orders.delay_days` 的 57 个空 vs 977 个 0）
--   1. 维度键只在一窗出现（新投产 / 停产）→ `对齐` = `缺基线`/`缺观察`，
--      缺侧值 `空`，`偏离`/`升幅` 为 `空`。**不当 0**——当 0 会算出「从 0 升到
--      191，升幅无穷」，而它会稳稳占据升幅榜首。
--   2. 两窗都有该键、某侧汇总值本身为 `空` → `对齐` = `两窗齐`、值 `空`。
--   3. 基线值是真的 0 → `偏离` 照算、`升幅` 为 `空` **而不是 `inf`**；
--      基线为负 → `升幅` 为 `空`（符号会翻），`偏离` 仍然照算仍然可信。
--
-- 【单位】`升幅` 是 **0-1 小数**（`0.2589` 即升 25.89%），与数据集的
--   `achievement_rate`/`defect_rate`（Q_PUB_005 已踩过「不是百分数」）和姊妹块
--   `窗间对比` 的 `变化率` 一致。WBS W155 行文写的「升幅 %」是口语说法，
--   单位统一取 0-1 小数，偏离点记档在交付报告。
--
-- 【输出形状】每组一行：各维度列名 + `对齐` + `基线值` + `观察值` + `偏离` +
--   `升幅`。两窗键的**并集**都出现，行序按维度键升序（空值键最前）。
--   **本块不排序、不取前 N**——要「升幅最大的那条」接 `制造.排序` + `制造.取前N`。
--
-- 实现走 Python 背衬（ADR-16 §3.3）：见同目录 `基线偏离.py`。下沉理由——
-- 判空要看运行时类型（只有 `空` 算空值，`0` 不算），极快侧没有内建的类型判定动词；
-- 且要做键并集 + 确定序排序。背衬只用 Python 标准库，零第三方依赖。
--
-- 【表参数不受类型图守护】ADR-40 §3.1 的 `值类型: 任意` 让 type_feeds 双向放行，
--   两个表参数（观察表 / 基线表）粘合器分不清谁是谁，而本块口径**不对称**、
--   接反了不会报错只会给错数——**方案里必须显式写 `参数`**。
--   正确性由 `测试.jk` 与 tests/test_v0_27_0_w155_基线偏离.py 保证。
--
-- 稳定性 experimental：依赖 pybridge，且口径层接口形状本轮仍可能调整。

导出 基偏。
