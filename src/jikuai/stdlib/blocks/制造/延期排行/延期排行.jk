-- 极快 v0.26.0 W142 · 制造域口径层（ADR-40 §3.2）：制造.延期排行
-- 口径块（层级 0）——按 `fact_orders.delay_days` 降序取前 N 单，
-- **空值行排除且排除条数一起返回**。
--
-- 导出名 `延榜` 已过两道验名（v0.26.0 WBS 执行原则 7）：
--   1. 词法原子：check_export_atomicity('延榜') → (True, [('IDENT', '延榜')])
--   2. 全局唯一：索引.json 125 条的 125 个导出名里无 `延榜`
--
-- ══════════════════════════════════════════════════════════════════
-- 为什么不是「制造.排序 + 制造.取前N 拼一下」
-- ══════════════════════════════════════════════════════════════════
-- 那两个引擎层块拼起来能排出前 N 行，但**拼不出这一条关键信息：有多少单因为
-- `delay_days` 为空而根本没进榜**。`制造.排序` 的空值口径是「排末尾」——57 张
-- 未交付订单会静默沉到表尾，取前 10 时看不见，于是调用方拿到一张「看起来完整」
-- 的榜，却不知道样本里有 57 单还没交付、压根没有延期天数可比。
--
-- 排行榜口径下「空」比「0」更隐蔽：0（按期交付，977 行）排在榜尾无害，
-- 空（未交付，57 行）排在榜尾却是**「不可比」被当成了「最不延期」**。
-- 所以本块先剔空值、再排序，并把剔掉的条数与实际参与排序的行数一起交出去。
--
-- 排序口径全部写死、不给开关：
--   * 主键 `delay_days` **降序**；
--   * 次键 **原表行序**（稳定排序），同一份输入两次调用结果完全一致。
--     刻意不用 `order_id` 当次键——那会让本块依赖一个它本不该认识的列。
--   * 返回**原行对象本身**，列一个不裁、值一个不改（同 `制造.取前N`）；
--     想只要几列请接 `制造.投影`。
--
-- N 的三条边界与 `制造.取前N`（导出 `摘前`）完全同口径，刻意保持一致：
--   1. N 大于参与排序的行数 → 返回全部，不报错（同 SQL LIMIT）；
--   2. N = 0 → 空榜，不报错；
--   3. N < 0 → 报错，绝不照 Python 切片当「去掉末尾几行」。N 必须是整数。
--
-- 实现走 Python 背衬（ADR-16 §3.3）：见同目录 `延期排行.py`。下沉理由同
-- `制造.取前N`——极快没有列表切片动词，纯 .jk 得 `遍历`+`追加` 逐行搬，
-- 每次 `追加` 复制整表，1850 行是 O(行数²)；且判空要看运行时类型，极快侧
-- 没有内建的类型判定动词。背衬只用 Python 标准库，零第三方依赖。
--
-- 【表参数不受类型图守护】ADR-40 §3.1：表的 `值类型: 任意` 让 type_feeds
--   双向放行。本块正确性由 `测试.jk` 与
--   tests/test_v0_26_0_w142_交付停线.py 保证，不由粘合器保证。
--
-- 稳定性 experimental：依赖 pybridge，且口径层接口形状本轮仍可能调整。

导出 延榜。
