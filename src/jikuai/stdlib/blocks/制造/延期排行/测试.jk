-- 块 `制造.延期排行` 的单元测试（W142 · v0.26.0，导出名 `延榜`）。
--
-- 本块与「`制造.排序` + `制造.取前N` 拼一下」的分水岭是：**空值行被剔掉、
-- 而不是排到末尾**，且剔了多少条要报出来。所以第 ③ 组专门造一张空值行排在
-- 前面的表，证明它们既不进榜、也不占名额，且 `排除的空值行数` 如实计数。
--
-- 自足部分用内联字典列表字面量自造小表；真实数据集部分用 `表载入.读表` 读
-- fact_orders.csv 验 Q_PUB_010 的前 10 单 + 排除 57 行。数据集缺失时
-- （`赛题/` 不进 wheel 与 sdist，ADR-40 §7）第二部分明确跳过并打印原因。
--
-- 覆盖：降序正确、稳定次键、空值剔除并计数、边界（空表 / 全空值列 / N=0 /
--       N 大于行数）、N 为负数 / 小数 / 布尔 / 字符串四类反例、纯函数不改入参。
--
-- 写法注记：跨行的实参 + 列表字面量会被压平成多余的位置参数——列表/字典字面量
-- 一律先落到变量再传参；带括号的嵌套表达式别直接跟在下标取值后面（会被当成
-- 「拿取出来的值去调用」）。

从 blocks.制造.延期排行 导入 延榜。
从 blocks.制造.表载入 导入 读表。
从 blocks.工具.断言 导入 验实。

导入 蟒:os。
导入 蟒:importlib.util。

-- ===========================================================================
-- 一、自足用例
-- ===========================================================================

-- ① 降序正确：4 单，延期 2 / 9 / 5 / 0 天 → 前 3 名依次 B(9) C(5) A(2)
定义 赵表 = 【
  {"order_id": "A", "delay_days": 2},
  {"order_id": "B", "delay_days": 9},
  {"order_id": "C", "delay_days": 5},
  {"order_id": "D", "delay_days": 0}
】。
定义 赵果 = 延榜(赵表, 3)。
打印 "① 降序取前 3："。
打印 赵果。
验实(长度 赵果【0】, 3)。
验实(赵果【1】, 0)。
验实(赵果【2】, 4)。
验实(赵果【0】【0】["order_id"], "B")。
验实(赵果【0】【1】["order_id"], "C")。
验实(赵果【0】【2】["order_id"], "A")。
-- 返回原行，列一个不裁
验实(长度 赵果【0】【0】, 2)。

-- ② 稳定次键 = 原表行序：同为 7 天的两单按原序 X 在 Y 前
定义 钱表 = 【
  {"order_id": "X", "delay_days": 7},
  {"order_id": "Y", "delay_days": 7},
  {"order_id": "Z", "delay_days": 1}
】。
定义 钱果 = 延榜(钱表, 2)。
打印 "② 同天数按原表行序（稳定排序）："。
打印 钱果。
验实(钱果【0】【0】["order_id"], "X")。
验实(钱果【0】【1】["order_id"], "Y")。
-- 两次调用完全一致（可复现）
验实(延榜(钱表, 2), 延榜(钱表, 2))。

-- ③ ★ 分水岭：空值行被剔掉，不是排到末尾
--    5 行里 2 行是空（未交付）。参与排序只有 3 行；取前 5 只能给 3 行，
--    **空值行一个都不进榜、也不占名额**；排除条数 = 2。
--    对比：若照 `制造.排序` 的「空值排末尾」，取前 5 会把 2 个空值行也塞进来。
定义 孙表 = 【
  {"order_id": "空1", "delay_days": 空},
  {"order_id": "延3", "delay_days": 3},
  {"order_id": "空2", "delay_days": 空},
  {"order_id": "按0", "delay_days": 0},
  {"order_id": "延8", "delay_days": 8}
】。
定义 孙果 = 延榜(孙表, 5)。
打印 "③ 空值行剔除并计数（本块的分水岭）："。
打印 孙果。
验实(孙果【1】, 2)。
验实(孙果【2】, 3)。
验实(长度 孙果【0】, 3)。
验实(孙果【0】【0】["order_id"], "延8")。
验实(孙果【0】【1】["order_id"], "延3")。
-- 0 天（按期交付）是**有效数据**，照样进榜，排在最后
验实(孙果【0】【2】["order_id"], "按0")。
-- 恒等式：原表行数 = 排除的空值行数 + 参与排序的行数
验实(加 孙果【1】 孙果【2】, 长度 孙表)。

-- ④ 边界：整列全是空值 → 空榜，排除数 = 行数，参与 0
定义 李表 = 【
  {"order_id": "P", "delay_days": 空},
  {"order_id": "Q", "delay_days": 空}
】。
定义 李果 = 延榜(李表, 3)。
打印 "④ 边界 全空值列："。
打印 李果。
验实(长度 李果【0】, 0)。
验实(李果【1】, 2)。
验实(李果【2】, 0)。

-- ⑤ 边界：空表 → 空榜、0、0（合法，非错误）
定义 周零 = 【】。
定义 周果 = 延榜(周零, 10)。
打印 "⑤ 边界 空表："。
打印 周果。
验实(长度 周果【0】, 0)。
验实(周果【1】, 0)。
验实(周果【2】, 0)。

-- ⑥ 边界：N = 0 → 空榜，不报错；但排除数与参与数照样如实报出
定义 吴果 = 延榜(赵表, 0)。
打印 "⑥ 边界 N=0："。
打印 吴果。
验实(长度 吴果【0】, 0)。
验实(吴果【2】, 4)。

-- ⑦ 边界：N 大于参与排序行数 → 返回全部，不报错（同 SQL LIMIT）
定义 郑果 = 延榜(赵表, 99)。
打印 "⑦ 边界 N 大于行数："。
验实(长度 郑果【0】, 4)。
验实(郑果【0】【3】["order_id"], "D")。

-- ⑧ 反例：N 为负数 → 报错（绝不当「去掉末尾几行」）
定义 王捕 = 假。
尝试：
  延榜(赵表, -1)。
捕获 赵错一：
  王捕 = 真。
  打印 "⑧ N 为负数报错："。
  打印 转字符串 赵错一。
。
验实(王捕, 真)。

-- ⑨ 反例：N 是小数 → 报错（不猜 2 还是 3）
定义 冯捕 = 假。
尝试：
  延榜(赵表, 2.5)。
捕获 赵错二：
  冯捕 = 真。
  打印 "⑨ N 是小数报错："。
  打印 转字符串 赵错二。
。
验实(冯捕, 真)。

-- ⑩ 反例：N 是布尔 / 字符串 → 报错
定义 陈捕 = 假。
尝试：
  延榜(赵表, 真)。
捕获 赵错三：
  陈捕 = 真。
。
验实(陈捕, 真)。
定义 褚捕 = 假。
尝试：
  延榜(赵表, "3")。
捕获 赵错四：
  褚捕 = 真。
。
验实(褚捕, 真)。

-- (11) 反例：延期天数列不存在 → 报错（不静默返回空榜）
--      注：这里起不再用带圈数字——⑪ 及以后（U+246A+）不在 GBK 码位里，
--      Windows 控制台 `打印` 会当场 UnicodeEncodeError。
定义 卫捕 = 假。
定义 卫坏 = 【{"order_id": "A"}】。
尝试：
  延榜(卫坏, 1)。
捕获 赵错五：
  卫捕 = 真。
  打印 "(11) 延期天数列缺失报错："。
  打印 转字符串 赵错五。
。
验实(卫捕, 真)。

-- (12) 反例：延期天数是非空的非数值 → 报错
定义 蒋捕 = 假。
定义 蒋坏 = 【{"order_id": "A", "delay_days": "很多天"}】。
尝试：
  延榜(蒋坏, 1)。
捕获 赵错六：
  蒋捕 = 真。
  打印 "(12) 非数值延期天数报错："。
  打印 转字符串 赵错六。
。
验实(蒋捕, 真)。

-- (13) 纯函数：不改动入参
定义 沈原 = 【
  {"order_id": "甲", "delay_days": 1},
  {"order_id": "乙", "delay_days": 2}
】。
定义 沈果 = 延榜(沈原, 1)。
验实(长度 沈原, 2)。
验实(沈原【0】["order_id"], "甲")。
验实(长度 沈果【0】, 1)。
验实(沈果【0】【0】["order_id"], "乙")。

打印 "自足用例：全过"。

-- ===========================================================================
-- 二、真实数据集用例（Q_PUB_010：上半年延期天数前 10 单）
-- ===========================================================================

定义 赵源 = importlib.util.find_spec("jikuai").origin。
定义 赵根 = os.path.dirname(os.path.dirname(os.path.dirname(赵源)))。
定义 赵集 = os.path.join(赵根 "赛题" "chatbi" "数据集")。

如果 非 os.path.isdir(赵集) 那么：
  打印 拼接 "跳过真实数据集用例：" 赵集 " 不存在（赛题/ 不进 wheel 与 sdist，ADR-40 §7）。"。
否则：
  定义 钱订单 = 读表(os.path.join(赵集 "fact_orders.csv"))。
  定义 钱榜 = 延榜(钱订单, 10)。
  打印 "Q_PUB_010 上半年延期天数前 10 单（数据集覆盖 2026-01-01~06-30，即上半年全量）："。
  遍历 钱单 于 钱榜【0】：
    打印 拼接 钱单["order_id"] " " 钱单["customer_id"] " " 钱单["model_id"] " " (转字符串 钱单["delay_days"]) " 天 " 钱单["order_status"]。
  。
  验实(长度 钱订单, 1850)。
  -- ★ DoD：被排除的空值行数必须报出来，且是 57
  验实(钱榜【1】, 57)。
  验实(钱榜【2】, 1793)。
  验实(长度 钱榜【0】, 10)。
  验实(钱榜【0】【0】["order_id"], "O001328")。
  验实(钱榜【0】【0】["delay_days"], 19)。
  验实(钱榜【0】【9】["order_id"], "O000415")。
  验实(钱榜【0】【9】["delay_days"], 11)。
  -- 前 10 单全是 M003（C005 的 M003 集中延期这个预置异常的侧证）
  遍历 钱单二 于 钱榜【0】：
    验实(钱单二["model_id"], "M003")。
  。
  打印 "真实数据集用例：前 10 单全为 M003，排除的空值行数 = 57"。
。
