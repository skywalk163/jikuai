-- 极快 v0.28.0 W167 · 制造域口径层姊妹出口：制造.延期排行表
-- 复合块（层级 1）——`制造.延期排行`（导出 `延榜`）的「只出榜单」出口。
--
-- 三道验名都已过（v0.28.0 WBS 执行原则 7 + W167 补的第三处）：
--   1. 目录名词法原子：check_module_segment_atomicity('延期排行表')
--      → (True, [('IDENT', '延期排行表')])       ← 目录名进点分路径段
--   2. 导出名词法原子：check_export_atomicity('延榜表') → (True, [('IDENT', '延榜表')])
--   3. 形参名词法原子：`赵表` / `赵前几` 各自 check_export_atomicity 均 True
--      —— 形参名是第三处必须验的名。W167 在 `停线汇总表` 上真栽过一次：形参写成
--      `赵维度列` 会被切成 `IDENT 赵维度` + `VERB 列`，调用被编译成多一个实参、
--      运行期才抛 Python 的 arity TypeError，错误文案完全不提形参名。
--      这里刻意**不叫** `赵前N`（拉丁 N 混排）也不叫 `赵行数列`，用 `赵前几`。
--   4. 全局唯一：索引.json 的导出名里既无 `延榜表`，块名里也无 `延期排行表`
--
-- 【立项理由是接线形状，不是新口径】`延榜` 出的是三元组
--   `(榜单, 排除的空值行数, 参与排序的行数)`，而 `制造.投影`（`择字段`）、
--   `制造.选取`（`筛行`）这类下游算子吃的是**列表**。元组接不上列表，
--   「排延期榜 → 只取几列 → 展示」这条链在方案 JSON 里就断在中间。
--
-- 【口径一个数不改】本块只调 `延榜` 再取第 1 项，**不重排、不重算**：
--   空值行仍是**先剔除再排序**，不是排到末尾——这正是 `延期排行` 与
--   「`制造.排序` + `制造.取前N`」拼装的分水岭：`定序` 把空排末尾，于是 57 张
--   未交付订单（`delay_days` 空、`order_status='生产中'`）会静默沉到榜尾，
--   取前 10 时看不见，而榜尾在排行榜语义下会被读成「最不延期」。
--   排序口径仍是主键 `delay_days` 降序 + 次键原表行序的稳定排序；
--   `前N` 三条边界仍同 `制造.取前N`（超出返回全部 / 0 返回空榜 / 负数报错）。
--
-- 【它丢掉了什么，要说清】元组第 2、3 项没了，所以恒等式
--   `原表行数 = 排除的空值行数 + 参与排序的行数` 在**本块的返回值上验不了**。
--   要审计那两个数，用 `制造.延期排行`（`延榜`）拿完整元组。本块是「为下游能
--   直连」而生的出口，**不是** `延榜` 的替代品。
--
-- 【本块没有 Python 背衬】纯 `.jk`，`块背衬PY数` 不用动。
--
-- 稳定性 experimental：与 `延期排行` 同档。

从 blocks.制造.延期排行 导入 延榜。

函数 延榜表 接收 赵表 赵前几：
  定义 赵元组 = 延榜(赵表, 赵前几)。
  返回 赵元组【0】。
。

导出 延榜表。
