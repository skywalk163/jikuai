-- 块 `制造.延期排行表` 的单元测试（W167 · v0.28.0，导出名 `延榜表`）。
--
-- 本块的全部价值在**出口形状**：`延榜` 出三元组、本块出列表，于是能直连
-- `制造.投影`（`择字段`）这类下游算子。所以测试重点不是重测排行口径
-- （那是 `延期排行/测试.jk` 的事），而是钉死三件事：
--   ① 本块返回值**逐元素等于** `延榜(...)【0】`——排序与空值口径一个数没改；
--   ② 「排榜 → 投影」这条链在**不手动拆包**的情况下真的跑通；
--   ③ 空值行是**先剔除再排序**（不是排末尾）这条根本口径透过姊妹块照旧成立
--      —— 未交付订单一单都不该出现在榜里，哪怕榜位没占满。
--
-- 写法注记：列表/字典字面量一律先落到变量再传参。

从 blocks.制造.延期排行表 导入 延榜表。
从 blocks.制造.延期排行 导入 延榜。
从 blocks.制造.投影 导入 择字段。
从 blocks.工具.断言 导入 验实。

-- ① 与 延榜 的三元组第 1 项逐元素相同（口径没被本块动过）
定义 赵表 = 【
  {"order_id": "O1", "delay_days": 3},
  {"order_id": "O2", "delay_days": 9},
  {"order_id": "O3", "delay_days": 1}
】。
定义 赵元组 = 延榜(赵表, 2)。
定义 赵榜 = 延榜表(赵表, 2)。
打印 "① 只出榜单："。
打印 赵榜。
验实(赵榜, 赵元组【0】)。
验实(长度 赵榜, 2)。
验实(赵榜【0】["order_id"], "O2")。
验实(赵榜【0】["delay_days"], 9)。
验实(赵榜【1】["order_id"], "O1")。
验实(赵榜【1】["delay_days"], 3)。

-- ② ★ 出口是列表不是三元组：取前 4 时 长度 = 4，三元组恒为 3
定义 钱表 = 【
  {"order_id": "P1", "delay_days": 1},
  {"order_id": "P2", "delay_days": 2},
  {"order_id": "P3", "delay_days": 3},
  {"order_id": "P4", "delay_days": 4}
】。
定义 钱榜 = 延榜表(钱表, 4)。
打印 "② 取前 4 → 长度 4（三元组会是 3）："。
打印 钱榜。
验实(长度 钱榜, 4)。
验实(等于 (长度 钱榜) 3, 假)。
验实(钱榜【0】["order_id"], "P4")。
验实(钱榜【3】["order_id"], "P1")。

-- ③ ★ 空值先剔除、不排末尾：五单里两单未交付，取前 5 也只回 3 单
--    若空被当「排末尾」，榜尾会出现未交付单、被读成「最不延期」
定义 孙表 = 【
  {"order_id": "Q1", "delay_days": 空},
  {"order_id": "Q2", "delay_days": 7},
  {"order_id": "Q3", "delay_days": 空},
  {"order_id": "Q4", "delay_days": 2},
  {"order_id": "Q5", "delay_days": 5}
】。
定义 孙榜 = 延榜表(孙表, 5)。
打印 "③ 未交付先剔除（取前 5 只回 3 单）："。
打印 孙榜。
验实(长度 孙榜, 3)。
验实(孙榜【0】["order_id"], "Q2")。
验实(孙榜【1】["order_id"], "Q5")。
验实(孙榜【2】["order_id"], "Q4")。
-- 榜里一单未交付都没有：逐行检查 delay_days 非空
遍历 孙行 于 孙榜：
  验实(等于 孙行["delay_days"] 空, 假)。
。

-- ④ ★ 直连 择字段：本块的立项理由——中间没有任何手动拆包
定义 李列名清单 = 【"order_id", "delay_days"】。
定义 李窄 = 择字段(延榜表(孙表, 2), 李列名清单)。
打印 "④ 排榜 → 投影（无手动拆包）："。
打印 李窄。
验实(长度 李窄, 2)。
验实(长度 李窄【0】, 2)。
验实(李窄【0】["order_id"], "Q2")。
验实(李窄【0】["delay_days"], 7)。

-- ⑤ 稳定排序：同天数保持原表行序（次键是原行序，不是 order_id）
定义 周表 = 【
  {"order_id": "R9", "delay_days": 4},
  {"order_id": "R1", "delay_days": 4},
  {"order_id": "R5", "delay_days": 9}
】。
定义 周榜 = 延榜表(周表, 3)。
打印 "⑤ 并列保持原行序（R9 在 R1 前）："。
打印 周榜。
验实(周榜【0】["order_id"], "R5")。
验实(周榜【1】["order_id"], "R9")。
验实(周榜【2】["order_id"], "R1")。

-- ⑥ 前N 三条边界（与 制造.取前N 同口径）
--    a) N 大于参与行数 → 返回全部，不报错
定义 吴超 = 延榜表(赵表, 99)。
验实(长度 吴超, 3)。
--    b) N = 0 → 空榜，不报错
定义 吴零 = 延榜表(赵表, 0)。
验实(长度 吴零, 0)。
验实(吴零, 【】)。
--    c) N 为负数 → 报错（绝不当「去掉末尾几行」）
定义 郑捕 = 假。
尝试：
  延榜表(赵表, -1)。
捕获 赵错一：
  郑捕 = 真。
  打印 "⑥c 负数 前N 报错透传："。
  打印 转字符串 赵错一。
。
验实(郑捕, 真)。

-- ⑦ 空表 → 空列表
定义 卫零 = 【】。
定义 卫榜 = 延榜表(卫零, 3)。
打印 "⑦ 空表 → 空列表："。
打印 卫榜。
验实(长度 卫榜, 0)。

-- ⑧ 行序确定：同一输入两次调用完全相同
验实(延榜表(孙表, 3), 延榜表(孙表, 3))。

-- ⑨ 反例：延期天数列不存在 → 报错从 延榜 透传上来（本块不吞异常）
定义 陈捕 = 假。
定义 陈坏 = 【{"order_id": "S1", "planned_quantity": 100}】。
尝试：
  延榜表(陈坏, 1)。
捕获 赵错二：
  陈捕 = 真。
  打印 "⑨ 延期天数列缺失报错透传："。
  打印 转字符串 赵错二。
。
验实(陈捕, 真)。

打印 "制造.延期排行表：全过"
