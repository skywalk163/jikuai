-- 极快 v0.26.0 制造域引擎层：制造.投影（导出名 择字段）
-- 引擎层关系算子（层级 0）——取列子集，保持调用方给定的列序。
--
-- **不含任何业务口径**（ADR-40 §3.1）：只取列，不过滤行、不汇总、不加权、
-- 不做时间窗口对齐、不碰任何现成比率列、不改任何数值、不改行序。
-- 口径判断全部留给口径层（产量 / 达成率 / 缺陷率是 W140-W142 的事）。
--
-- 表的运行时表示：列表<字典<字符串,任意>>（行的列表，每行是列名→值的字典）。
--
-- 三条边界见 `块.json` 的 `描述`，最要紧的一条：**输出列序 = 调用方给的列名
-- 清单顺序，不是原表列序**——想保持原序请自己按原序传清单。列名不存在时报错
-- 而**不悄悄少给一列**。
--
-- 实现走 Python 背衬（ADR-16 §3.3）：见同目录 `投影.py`。下沉的硬理由——
-- 极快没有「给字典按动态键写值」的手段（`赵典["甲"]=1` 会去 int("甲") 报错），
-- 按 N 个列名拼一行只能反复 `合并`，O(列数²) 复制还要两层 `遍历`（副词不接
-- 用户函数）；Python 一个 dict 推导就完事，且天然保序。本 .jk 仅做门面与导出。
--
-- 导出名 `择字段` 词法原子（ADR-15 §3.7）。
-- 【偏离点】WBS/任务分配的导出名是 `择列`，但 `择列` 词法上被切成
-- `择`(IDENT)+`列`(VERB) 两个 token（`列` 是列表构造动词），做导出名时
-- 跨模块 `导入` 会在 `择` 处截断、报 JK-E5002「未导出：择」，根本无法使用。
-- 凡含 `列` 字的候选（择列/选列/取列/留列…）同理全废。故改用等义原子名
-- `择字段`，已过 check_export_atomicity 且经 grep 索引确认全局唯一（G13）。

导出 择字段。
