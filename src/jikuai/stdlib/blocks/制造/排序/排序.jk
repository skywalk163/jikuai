-- 极快 v0.26.0 制造域引擎层：制造.排序（导出名 定序）
-- 引擎层关系算子（层级 0）——按单列升序 / 降序排，空值排末尾，排序稳定。
--
-- **不含任何业务口径**（ADR-40 §3.1）：只排序，不过滤行、不汇总、不加权、
-- 不做时间窗口对齐、不碰任何现成比率列、不改任何数值。
-- 口径判断全部留给口径层（产量 / 达成率 / 缺陷率是 W140-W142 的事）。
--
-- 表的运行时表示：列表<字典<字符串,任意>>（行的列表，每行是列名→值的字典）。
--
-- 三条边界见 `块.json` 的 `描述`，其中两条是别的块要依赖的地基：
--   1. **空值一律排末尾**（升序降序都一样，空值行之间保持原相对次序）。
--      `fact_orders.delay_days` 未交付时是空（ADR-40 §4.3），若把空当极大 /
--      极小参与排序，未交付订单就会挤进「延期最多的前 N 单」里。
--   2. **排序稳定**（相等的值保持输入次序）。不稳定排序会让 `制造.取前N` 在
--      有并列时结果不确定，溯源说明也就没法复现。
-- 列名不存在时报错而**不返回原表**——静默不排序会被误读成「已按此列排好」。
--
-- 实现走 Python 背衬（ADR-16 §3.3）：见同目录 `排序.py`。下沉的理由——
-- 内建 `排序` 动词没有「按某列排行的列表」的语义，而 Python `sorted` 是标准库
-- 保证的稳定排序；空值分桶逻辑在 .jk 里写（副词不接用户函数，得写显式 `遍历`）
-- 也更繁琐。本 .jk 仅做门面与导出。
--
-- 导出名 `定序` 词法原子（ADR-15 §3.7）。
-- 【偏离点】WBS/任务分配的导出名是 `列排`，但 `列排` 词法上被切成
-- `列`(VERB)+`排`(IDENT) 两个 token（`列` 是列表构造动词），做导出名时跨模块
-- `导入` 会截断报 JK-E5002，根本无法使用；凡含 `列` 字的候选同理全废，
-- `排序` / `顺排` 又分别是内建动词与 `数据.升序` 的已用导出名（G13）。
-- 故改用等义原子名 `定序`，已过 check_export_atomicity 且确认全局唯一。

导出 定序。
