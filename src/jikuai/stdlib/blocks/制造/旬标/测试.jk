-- 块 `制造.旬标` 的单元测试（W137）。
-- 内联字典列表字面量自造小表，不依赖 CSV 载入。
-- 重点：证明本块的旬口径与 历法.旬日 的 旬序 逐日一致（不是照文档抄的）。

从 blocks.制造.旬标 导入 标旬。
从 blocks.历法.旬日 导入 旬序。

定义赵表=列 {"usage_date": "2026-06-01", "line_id": "L002"} {"usage_date": "2026-06-10", "line_id": "L002"} {"usage_date": "2026-06-11", "line_id": "L002"} {"usage_date": "2026-06-20", "line_id": "L002"} {"usage_date": "2026-06-21", "line_id": "L002"} {"usage_date": "2026-06-30", "line_id": "L002"}。

-- 测试1：旬边界 1/10/11/20/21/30 → 上旬 上旬 中旬 中旬 下旬 下旬
定义赵标=标旬(赵表, "usage_date", "旬")。
打印 "T1 行数(期望6)：" (长度 赵标)。
打印 "T1 06-01(期望 上旬)：" 赵标[0]["旬"]。
打印 "T1 06-10(期望 上旬)：" 赵标[1]["旬"]。
打印 "T1 06-11(期望 中旬)：" 赵标[2]["旬"]。
打印 "T1 06-20(期望 中旬)：" 赵标[3]["旬"]。
打印 "T1 06-21(期望 下旬)：" 赵标[4]["旬"]。
打印 "T1 06-30(期望 下旬)：" 赵标[5]["旬"]。

-- 测试2：与 历法.旬日 的 旬序 口径逐行对齐（同源即证明没另造一套边界）
定义赵齐=真。
遍历 赵行 于 赵标：
  定义 赵日号 = 转整数 (子串 赵行["usage_date"] 8 10)。
  定义 赵实旬 = 赵行["旬"]。
  定义 赵原旬 = 旬序(赵日号)。
  如果 非 (等于 赵实旬 赵原旬) 那么：
    赵齐 = 假。
  。
。
打印 "T2 与 旬序 口径逐行一致(期望 真)：" 赵齐。

-- 测试3：原有列不丢、入参表不被改坏
打印 "T3 原列还在(期望 L002)：" 赵标[0]["line_id"]。
打印 "T3 加列后列数(期望3)：" (长度 赵标[0]) " 原表列数(期望2)：" (长度 赵表[0])。

-- 测试4：目标列名已存在 → 报可读中文错误
尝试：
  标旬(赵表, "usage_date", "line_id")。
  打印 "T4 失败：列名冲突没报错"。
捕获 赵误：
  打印 "T4 列名冲突报错(期望)：" 赵误。
。

-- 测试5：日期为空 → 旬列写空，不报错，行保留
定义赵含空=列 {"d": "2026-06-15"} {"d": 空}。
定义赵标2=标旬(赵含空, "d", "旬")。
打印 "T5 行数(期望2)：" (长度 赵标2) " 有值行(期望 中旬)：" 赵标2[0]["旬"] " 空行(期望 空)：" 赵标2[1]["旬"]。

-- 测试6：日期非法 → 报错
定义赵脏=列 {"d": "6/15/2026"}。
尝试：
  标旬(赵脏, "d", "旬")。
  打印 "T6 失败：非法日期没报错"。
捕获 赵误2：
  打印 "T6 非法日期报错(期望)：" 赵误2。
。

-- 测试7：日期列不存在 → 报错
尝试：
  标旬(赵表, "defect_date", "旬")。
  打印 "T7 失败：缺列没报错"。
捕获 赵误3：
  打印 "T7 缺列报错(期望)：" 赵误3。
。

-- 测试8：空表 → 空结果
定义赵空表=【】。
定义赵空果=标旬(赵空表, "d", "旬")。
打印 "T8 空表行数(期望0)：" (长度 赵空果)。

-- 测试9：2 月末 28/29 日仍是下旬（旬序 只看日数，与月长无关）
定义赵二月=列 {"d": "2026-02-28"} {"d": "2024-02-29"}。
定义赵标3=标旬(赵二月, "d", "旬")。
打印 "T9 02-28 / 02-29(期望 下旬 / 下旬)：" 赵标3[0]["旬"] " / " 赵标3[1]["旬"]。
