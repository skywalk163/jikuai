-- 块 `制造.月标` 的单元测试（W137）。
-- 内联字典列表字面量自造小表，不依赖 CSV 载入。

从 blocks.制造.月标 导入 标月。

定义赵表=列 {"production_date": "2026-05-31", "line_id": "L001", "actual_quantity": 10} {"production_date": "2026-06-01", "line_id": "L002", "actual_quantity": 20} {"production_date": "2026-06-30", "line_id": "L002", "actual_quantity": 30}。

-- 测试1：加列后原有列一个不丢，年月值正确
定义赵标=标月(赵表, "production_date", "年月")。
打印 "T1 行数(期望3)：" (长度 赵标)。
打印 "T1 第0行年月(期望 2026-05)：" 赵标[0]["年月"]。
打印 "T1 第1行年月(期望 2026-06)：" 赵标[1]["年月"]。
打印 "T1 第2行年月(期望 2026-06)：" 赵标[2]["年月"]。
打印 "T1 原列还在(期望 L001 / 10)：" 赵标[0]["line_id"] " / " 赵标[0]["actual_quantity"]。
打印 "T1 第0行列数(期望4)：" (长度 赵标[0])。

-- 测试2：入参表没被改坏（原表仍是 3 列）
打印 "T2 原表第0行列数(期望3)：" (长度 赵表[0])。

-- 测试3：目标列名已存在 → 报可读中文错误
尝试：
  标月(赵表, "production_date", "line_id")。
  打印 "T3 失败：列名冲突没报错"。
捕获 赵误：
  打印 "T3 列名冲突报错(期望)：" 赵误。
。

-- 测试4：日期为空的行 → 年月列写空，不报错，行保留
定义赵含空=列 {"d": "2026-06-10"} {"d": 空}。
定义赵标2=标月(赵含空, "d", "年月")。
打印 "T4 行数(期望2)：" (长度 赵标2) " 空行年月(期望 空)：" 赵标2[1]["年月"]。

-- 测试5：日期非法 → 报错
定义赵脏=列 {"d": "2026-6-1"}。
尝试：
  标月(赵脏, "d", "年月")。
  打印 "T5 失败：非法日期没报错"。
捕获 赵误2：
  打印 "T5 非法日期报错(期望)：" 赵误2。
。

-- 测试6：日期列不存在 → 报错
尝试：
  标月(赵表, "usage_date", "年月")。
  打印 "T6 失败：缺列没报错"。
捕获 赵误3：
  打印 "T6 缺列报错(期望)：" 赵误3。
。

-- 测试7：空表 → 空结果
定义赵空表=【】。
定义赵空果=标月(赵空表, "d", "年月")。
打印 "T7 空表行数(期望0)：" (长度 赵空果)。

-- 测试8：跨年月边界 12-31 与次年 01-01
定义赵跨年=列 {"d": "2025-12-31"} {"d": "2026-01-01"}。
定义赵标3=标月(赵跨年, "d", "年月")。
打印 "T8 跨年(期望 2025-12 / 2026-01)：" 赵标3[0]["年月"] " / " 赵标3[1]["年月"]。
