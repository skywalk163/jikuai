-- 块 `制造.窗口` 的单元测试（W137）。
-- 内联字典列表字面量自造小表，不依赖 CSV 载入——`制造.表载入` 还没落地。
-- 写法提醒：嵌套动词实参一律加括号（元数解析规范 §8.1 中缀合并会静默算错）。

从 blocks.制造.窗口 导入 截期。

-- 造一张小表：两端点各一行、区间内一行、区间外两行、日期为空一行。
定义赵表=列 {"d": "2026-06-09", "id": "外左"} {"d": "2026-06-10", "id": "端左"} {"d": "2026-06-17", "id": "中间"} {"d": "2026-06-24", "id": "端右"} {"d": "2026-06-25", "id": "外右"} {"d": 空, "id": "空值"}。

-- 测试1：闭区间 2026-06-10 到 2026-06-24 → 期望 3 行，依次 端左/中间/端右
定义赵窗=截期(赵表, "d", "2026-06-10", "2026-06-24")。
打印 "T1 行数(期望3)：" (长度 赵窗)。
打印 "T1 首行 id(期望 端左)：" 赵窗[0]["id"]。
打印 "T1 中行 id(期望 中间)：" 赵窗[1]["id"]。
打印 "T1 末行 id(期望 端右)：" 赵窗[2]["id"]。

-- 测试2：端点包含性单独钉死——单日窗口只应命中该端点那一行
定义赵单=截期(赵表, "d", "2026-06-10", "2026-06-10")。
打印 "T2 单日 6-10 行数(期望1)：" (长度 赵单) " id(期望 端左)：" 赵单[0]["id"]。
定义赵单2=截期(赵表, "d", "2026-06-24", "2026-06-24")。
打印 "T2 单日 6-24 行数(期望1)：" (长度 赵单2) " id(期望 端右)：" 赵单2[0]["id"]。

-- 测试3：日期列含空值 → 不命中且不报错（T1 已证不报错，这里证不命中）
定义赵全=截期(赵表, "d", "2026-01-01", "2026-12-31")。
打印 "T3 全年行数(期望5，空值行不命中)：" (长度 赵全)。

-- 测试4：起止倒置 → 报可读中文错误
尝试：
  截期(赵表, "d", "2026-06-24", "2026-06-10")。
  打印 "T4 失败：倒置区间没报错"。
捕获 赵误：
  打印 "T4 倒置报错(期望)：" 赵误。
。

-- 测试5：起日格式非法（月日没补零）→ 报错
尝试：
  截期(赵表, "d", "2026-6-10", "2026-06-24")。
  打印 "T5 失败：非法起日没报错"。
捕获 赵误2：
  打印 "T5 非法起日报错(期望)：" 赵误2。
。

-- 测试6：数据里的日期值非法 → 报错，绝不静默跳过
定义赵脏=列 {"d": "2026-06-10"} {"d": "2026/06/11"}。
尝试：
  截期(赵脏, "d", "2026-06-01", "2026-06-30")。
  打印 "T6 失败：脏日期被静默跳过了"。
捕获 赵误3：
  打印 "T6 脏日期报错(期望)：" 赵误3。
。

-- 测试7：日期列不存在 → 报错
尝试：
  截期(赵表, "production_date", "2026-06-01", "2026-06-30")。
  打印 "T7 失败：缺列没报错"。
捕获 赵误4：
  打印 "T7 缺列报错(期望)：" 赵误4。
。

-- 测试8：空表 → 空结果，不报错
定义赵空果=截期(【】, "d", "2026-06-01", "2026-06-30")。
打印 "T8 空表行数(期望0)：" (长度 赵空果)。

-- 测试9：跨月区间（5-30 到 6-02）也对
定义赵跨=列 {"d": "2026-05-29"} {"d": "2026-05-30"} {"d": "2026-06-01"} {"d": "2026-06-02"} {"d": "2026-06-03"}。
打印 "T9 跨月 5-30..6-02 行数(期望3)：" (长度 截期(赵跨, "d", "2026-05-30", "2026-06-02"))。
