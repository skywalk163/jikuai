-- 块 `制造.窗口补集` 的单元测试（W168）。
-- 内联字典列表字面量自造小表。写法提醒：嵌套动词实参一律加括号（元数解析规范 §8.1）。

从 blocks.制造.窗口补集 导入 截外。

-- 同 `窗口/测试.jk` 的小表：外左/端左/中间/端右/外右/空值。
定义赵表=列 {"d": "2026-06-09", "id": "外左"} {"d": "2026-06-10", "id": "端左"} {"d": "2026-06-17", "id": "中间"} {"d": "2026-06-24", "id": "端右"} {"d": "2026-06-25", "id": "外右"} {"d": 空, "id": "空值"}。

-- T1：补集 = 区间外的行，两端点都不在内 → 期望 2 行，依次 外左/外右
定义赵外=截外(赵表, "d", "2026-06-10", "2026-06-24")。
打印 "T1 行数(期望2)：" (长度 赵外)。
打印 "T1 首行 id(期望 外左)：" 赵外[0]["id"]。
打印 "T1 次行 id(期望 外右)：" 赵外[1]["id"]。

-- T2：端点严格不进补集——单日窗口，那一行落窗内，其余全进补集（空值除外）
定义赵单=截外(赵表, "d", "2026-06-17", "2026-06-17")。
打印 "T2 单日 6-17 补集行数(期望4)：" (长度 赵单)。

-- T3（核心口径）：空值行既不进窗口也不进补集。全年窗口 → 补集应为 0 行，
-- 而不是把空值行算进来的 1 行。
定义赵全外=截外(赵表, "d", "2026-01-01", "2026-12-31")。
打印 "T3 全年补集行数(期望0，空值不算区间外)：" (长度 赵全外)。

-- T4：起止倒置 → 报错，绝不当全表返回
尝试：
  截外(赵表, "d", "2026-06-24", "2026-06-10")。
  打印 "T4 失败：倒置区间没报错"。
捕获 赵误：
  打印 "T4 倒置报错(期望)：" 赵误。
。

-- T5：数据里的日期值非法 → 报错，绝不静默跳过
定义赵脏=列 {"d": "2026-06-10"} {"d": "2026/06/11"}。
尝试：
  截外(赵脏, "d", "2026-06-01", "2026-06-30")。
  打印 "T5 失败：脏日期被静默跳过了"。
捕获 赵误2：
  打印 "T5 脏日期报错(期望)：" 赵误2。
。

-- T6：日期列不存在 → 报错
尝试：
  截外(赵表, "production_date", "2026-06-01", "2026-06-30")。
  打印 "T6 失败：缺列没报错"。
捕获 赵误3：
  打印 "T6 缺列报错(期望)：" 赵误3。
。

-- T7：空表 → 空补集，不报错
定义赵空果=截外(【】, "d", "2026-06-01", "2026-06-30")。
打印 "T7 空表补集行数(期望0)：" (长度 赵空果)。
