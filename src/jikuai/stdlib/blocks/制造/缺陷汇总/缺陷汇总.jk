-- 极快 v0.26.0 制造域口径层（ADR-40 §3.2）：制造.缺陷汇总（导出名 计陷）
-- 口径块（层级 0）——按维度列汇总 fact_quality_defects.defect_count，支持按
--   process / defect_type / severity 下钻（下钻 = 把这些列放进 维度列）。
--
-- 【口径自报，ADR-40 §5 / G22 第 2 条】
--   度量列写死为 defect_count（缺陷件数，绝对量），聚合方式是**求和**。
--   缺陷率不在这里算——它是分子分母跨表的派生指标，见姊妹块 制造.缺陷率（陷率）。
--   本块只出分子侧的绝对汇总，不碰任何分母。
--
-- 【空值口径，不许静默当 0】defect_count 出现空值直接报错，不默默按 0 计入。
--   「不知道有几个缺陷」与「零缺陷」是两回事，把前者当 0 会让某车型的缺陷数
--   凭空缩水，而运行期不报错——正是 AGENTS.md 第四节点名的那种审计期才暴露的错。
--
-- 【实现走 Python 背衬，ADR-16 §3.3】见同目录 缺陷汇总.py。分组建桶、对含空值
--   与混合类型的维度键做确定性排序，纯 .jk 又长又易错；背衬只用标准库，零第三方依赖。
--
-- 【引擎层无类型保护，ADR-40 §3.1】表是 列表<字典<字符串,任意>>，值类型 任意 会让
--   type_feeds 双向放行——正确性由 测试.jk 与 tests/test_v0_26_0_w141_质量能耗.py 保证。
--
-- 导出名 计陷 已过两道验名（WBS 执行原则 7）：词法原子 + 全局唯一（G13）。

导出 计陷。
