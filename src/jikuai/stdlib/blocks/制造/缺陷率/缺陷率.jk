-- 极快 v0.26.0 制造域口径层（ADR-40 §3.2 / §5.3）：制造.缺陷率（导出名 陷率）
-- 口径块（层级 0）——**分歧点 3** 的落点。
--
-- 【口径自报，本块存在的全部理由】
--   缺陷率 = sum(fact_quality_defects.defect_count) / sum(fact_production_actual.actual_quantity)，
--   **先按维度把分子分母各自汇总，再相除**。
--   **不是**「先算每行的比率再求平均」——那个口径是错的，而且错得很隐蔽：两张表
--   粒度根本不同（缺陷表多了 工序/缺陷类型/严重度 三维，2992 行 vs 产量表 2896 行），
--   行级比率对不上，平均出来的数没有业务含义。实测 2026-06 全量：先汇总后相除
--   = 1513/29931 = 0.050550，行级比率算术平均 = 0.032218，**两者相差 57%**。
--   数据集里**没有** defect_rate 现成列（ADR-40 §5 的 2026-08-18 修正框已订正
--   初稿那句「三个现成比率列」），所以这个指标只能派生。
--
-- 【对齐键的硬上限】fact_quality_defects **没有 shift 列**（表头实读：defect_id,
--   defect_date, line_id, model_id, process, defect_type, defect_count, severity,
--   rework_status）。所以两表对齐最细只能到「日 × 产线 × 车型」，做不到班次粒度。
--   本块要求 维度列 在两张表里**同名同在**，因此实际可用的对齐维度是
--   line_id / model_id（日期列两表名字不同：defect_date vs production_date，
--   要按期对齐请先各自用 制造/窗口 的 截期 切窗口再传进来）。
--
-- 【分母为 0 与分母键缺失：显式决议，绝不静默当 0】
--   · 分母汇总为 0（该组产量表有记录但产量合计为 0，例如停产）→ 缺陷率返回 空，
--     并如实给出 缺陷数=n、产量=0。
--   · 分子有值但分母键缺失（该维度组在产量表里根本找不到）→ 缺陷率返回 空，
--     产量返回 空（**不是 0**），且该组**照样出现在结果里**。
--   两条都不报错也不静默丢弃：报错会让一整张报表因为一个边缘组全废（某产线某月
--   停产在 BI 里是常态）；静默丢弃会让「各组缺陷数之和」不等于 缺陷汇总 给出的
--   总缺陷数，那种不守恒最难查。返回 空 + 保留原始分子分母，是唯一让人当场看见
--   问题的选择。理由与实现细节写在 缺陷率.py 的模块 docstring。
--   反向那一侧不同：产量表有该组、缺陷表没有 → 缺陷数=0、缺陷率=0.0。那是**真的
--   零缺陷**，不是「把未知当 0」，所以照常给数。
--
-- 【姊妹块】分子侧的绝对汇总在 制造/缺陷汇总（导出 计陷）。
--
-- 实现走 Python 背衬 缺陷率.py（ADR-16 §3.3）：两侧分组建桶 + 按维度键对齐 + 除法
-- 边界，纯 .jk 写不动也不该写。零第三方依赖。
--
-- 导出名 陷率 已过两道验名（WBS 执行原则 7）：词法原子 + 全局唯一（G13）。

导出 陷率。
