-- 极快 v0.26.0 制造域引擎层（ADR-40 §3.1）：制造.表元信息
-- 原子块（层级 0）——给一张表返回 `[行数, 列名列表]`。
--
-- 导出名 `表况` 已验证词法原子（ADR-15 §3.7）：
--   tokenize('表况。') → IDENT('表况')。
--
-- 用途：溯源说明（赛题必选功能「说明使用了哪些表、字段」）与 W139 的数据
-- 质量体检都要先知道「这张表有多少行、有哪些列」。
--
-- 实现走 Python 背衬 `表元信息.py`（ADR-16 §3.3）：列名必须保持**载入时的
-- 原始列序**（ADR-40 §4 把列序也当契约，引擎层的投影依赖它），背衬直接
-- `list(首行.keys())` 就是表头顺序，最省事也最不容易写错。
--
-- 空表（行数 0）返回 `【0, 【】】`：列信息只存在于行字典的键里，没有行就
-- 没有列名可报。这是明确行为、不抛错，也写进了 `块.json` 的描述。
--
-- 本块**不含业务口径**：只数行、只取列名，不看任何列的值，不做汇总、不算
-- 分母、不判现成列。
--
-- 稳定性 experimental：引擎层接口形状本轮仍可能调整。

导出 表况。
