-- 极快 v0.26.0 制造域引擎层（ADR-40 §3.1）：制造.表载入
-- 原子块（层级 0）——把 CSV 载成 `列表<字典<字符串,任意>>`（行的列表，
-- 每行是「列名→值」的字典），列序 = 表头顺序。
--
-- 导出名 `读表` 已验证词法原子（ADR-15 §3.7）：
--   tokenize('读表。') → IDENT('读表')。`读` 单字不是动词，`读取` 才是，
--   故最长匹配不会把它切开。
--
-- 实现走 Python 背衬 `表载入.py`（ADR-16 §3.3 混合模块），本 .jk 仅做门面
-- 与导出。三条理由写在 `表载入.py` 的模块 docstring 里，此处只点关键代价：
--   1. 极快没有 CSV 解析动词；
--   2. 内建 `读取` 限 CWD 内 + 10 MiB 上限（语法参考 §10.1），而 BI 数据集
--      不在运行时 cwd 下；
--   3. **代价**：走 `蟒:` 桥后本块不受那道 CWD 闸约束，路径给什么读什么。
--
-- 引擎层不享有「确定性推链」红利：ADR-40 §3.1 已写明 `值类型: 任意` 会让
-- glue.py 的 type_feeds 双向放行，**类型图在这一层不起作用**。本块的正确性
-- 由 `测试.jk` 与 `scripts/check_chatbi_schema.py` 保证，不由粘合器保证。
--
-- 本块**不含业务口径**：不汇总、不算分母、不判现成列、不重算、不切窗口，
-- 也不校验 schema。口径层的块（W140-W142）才做那些事。
--
-- 稳定性 experimental：依赖 pybridge，且引擎层接口形状本轮仍可能调整。

导出 读表。
