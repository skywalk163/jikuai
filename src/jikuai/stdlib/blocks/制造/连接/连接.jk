-- 极快 v0.26.0 制造域引擎层：制造.连接（导出名 联表）
-- 引擎层关系算子（层级 0）——等值内连接（inner join）。
--
-- **不含任何业务口径**（ADR-40 §3.1）：只做等值连接，不过滤、不汇总、
-- 不做时间窗口对齐、不改任何数值。口径判断全部留给口径层。
--
-- 表的运行时表示：列表<字典<字符串,任意>>（行的列表，每行是列名→值的字典）。
--
-- 实现走 Python 背衬（ADR-16 §3.3）：见同目录 `连接.py`。下沉的两个硬理由——
--   1. 复合键要用元组做哈希键；极快字典的键只接受字符串/整数，纯 .jk 只能拼
--      分隔符，任何分隔符都可能撞键、连出错数。
--   2. 哈希连接要一次建索引；纯 .jk 的嵌套双循环在 2896×2896 上会卡住。
-- 本 .jk 仅做门面与导出。
--
-- 导出名 `联表` 已验证词法原子（ADR-15 §3.7），且经 grep 索引确认全局唯一（G13）。

导出 联表。
