-- 极快 v0.26.0 制造域引擎层：制造.选取（导出名 筛行）
-- 引擎层关系算子（层级 0）——按「列名 + 比较符 + 值」过滤行。
--
-- **不含任何业务口径**（ADR-40 §3.1）：只判命中，不汇总、不加权、
-- 不做时间窗口对齐、不碰任何现成比率列、不改任何数值。
-- 口径判断全部留给口径层（产量 / 达成率 / 缺陷率是 W140-W142 的事）。
--
-- 表的运行时表示：列表<字典<字符串,任意>>（行的列表，每行是列名→值的字典）。
--
-- 比较符七个：= ≠ > ≥ < ≤ 含。四条边界见 `块.json` 的 `描述`，其中最要紧的
-- 两条是：
--   1. 含空值的行在任何比较符下都不命中——**`≠` 也不命中**（SQL 三值逻辑）。
--      这条反直觉，不点明就是「兄弟能力缺位」陷阱：代码跑得通、数是错的。
--   2. > ≥ < ≤ 只支持数值，字符串一律报错，不做字典序。
-- 列名不存在时报错而**不返回空表**——静默空表会被误读成「确实没有满足条件的行」。
--
-- 实现走 Python 背衬（ADR-16 §3.3）：见同目录 `选取.py`。下沉的两个理由——
--   1. 要按运行时类型分流（空 / 数值 / 字符串三档），极快侧没有内建类型判定动词。
--   2. 副词 `只` 不接用户函数，纯 .jk 过滤得写显式 `遍历`；七个比较符 × 三档
--      类型摊在 .jk 里是上百行分支，正确性更难守。
-- 本 .jk 仅做门面与导出。
--
-- 导出名 `筛行` 已验证词法原子（ADR-15 §3.7），且经 grep 索引确认全局唯一（G13）。

导出 筛行。
