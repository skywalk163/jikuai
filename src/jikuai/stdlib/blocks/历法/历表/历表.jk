-- 极快 v0.13.0 块生态：历法.历表
-- 一级块（层级 1）——某年某月的日历表：每项是 [日, 星期]。
--
-- 导出名 `月历` 已验证词法原子（ADR-15 §3.7）。
-- 依赖 历法.月长 拿天数，历法.周几 拿每天的星期名。

从 blocks.历法.月长 导入 月天。
从 blocks.历法.周几 导入 星期。

函数 月历 接收 赵年 赵月：
  定义赵天数=月天(赵年, 赵月)。
  定义赵集=列。
  定义赵日=1。
  当 小于等于 赵日 赵天数：
    定义赵项=列 赵日 星期(赵年, 赵月, 赵日)。
    赋值赵集=追加 赵集 赵项。
    赋值赵日=加 赵日 1。
  。
  返回 赵集。
。

导出 月历。
