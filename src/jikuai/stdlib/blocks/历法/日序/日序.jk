-- 极快 v0.13.0 块生态：历法.日序
-- 原子块（层级 0）——公历日期 → 绝对日序（0001-01-01 起算的天数）。
--
-- 导出名 `序日` 已验证词法原子（ADR-15 §3.7）。
--
-- 实现走背衬：纯 .jk 要自己累加年月天数并处理世纪闰年规则，易错且冗长。
-- `datetime` 属标准库，不破坏零运行时依赖，故下沉到 `日序.py`（ADR-16 §3.3）。
--
-- 本块是历法领域的公共基石：日差、挪日、周岁都建立在绝对日序之上。

导出 序日。
