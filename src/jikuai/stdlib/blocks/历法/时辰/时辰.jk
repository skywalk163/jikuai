-- 极快 v0.13.0 块生态：历法.时辰
-- 原子块（层级 0）——把 24 小时制的钟点换算成十二时辰名。
--
-- 导出名 `辰名` 已验证词法原子（ADR-15 §3.7）。
--
-- 子时跨零点（23:00-1:00），所以序号 = ((小时 + 1) 整除 2) 取余 12。

函数 辰名 接收 赵时：
  定义赵偏=整除 加 赵时 1 2。
  定义赵序=取余 赵偏 12。
  定义赵表=列 "子" "丑" "寅" "卯" "辰" "巳" "午" "未" "申" "酉" "戌" "亥"。
  返回 取值 赵表 赵序。
。

导出 辰名。
