-- 极快 v0.13.0 块生态：历法.生辰
-- 一级块（层级 1）——生辰四柱简版：[农历日期, 干支, 生肖, 时辰]。
--
-- 导出名 `命帖` 已验证词法原子（ADR-15 §3.7）。
--
-- 四个依赖跨两个领域：中文域给农历/干支/生肖的表现层，历法域给时辰换算。
-- 这是块生态"组合优于重写"的典型案例——本块自己一行计算都没有。

从 blocks.中文.农历 导入 阴历。
从 blocks.中文.干支 导入 甲子。
从 blocks.中文.属相表 导入 属相。
从 blocks.历法.时辰 导入 辰名。

函数 命帖 接收 赵年 赵月 赵日 赵时：
  定义赵农=阴历(赵年, 赵月, 赵日)。
  定义赵支=甲子(赵年)。
  定义赵肖=属相(赵年)。
  定义赵辰=辰名(赵时)。
  返回 列 赵农 赵支 赵肖 赵辰。
。

导出 命帖。
