-- 极快 v0.13.0 块生态：历法.节气
-- 原子块（层级 0）——按序号取某年的二十四节气，返回 [节气名, 月, 日]。
--
-- 导出名 `气候` 已验证词法原子（ADR-15 §3.7）。
--
-- 实现走背衬：节气日期要查特征常数表并做闰年修正，用 .py 表达最直白。
-- 采用通用近似公式（1900-2100 误差 ≤1 天），不做真太阳黄经积分——
-- 那需要天文历表，超出块生态的合理复杂度。稳定性标 experimental。

导出 气候。
