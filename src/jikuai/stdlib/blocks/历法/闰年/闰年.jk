-- 极快 v0.13.0 块生态：历法.闰年
-- 原子块（层级 0）——判断公历年份是否闰年。
--
-- 导出名 `闰判` 已验证词法原子（ADR-15 §3.7）。
--
-- 规则：能被 4 整除且不能被 100 整除，或能被 400 整除。
-- 嵌套前缀布尔一律拆中间变量，避免 parser 误绑元数。

函数 闰判 接收 赵年：
  定义赵四=等于 取余 赵年 4 0。
  定义赵百=不等于 取余 赵年 100 0。
  定义赵四百=等于 取余 赵年 400 0。
  定义赵普通=且 赵四 赵百。
  返回 或 赵普通 赵四百。
。

导出 闰判。
