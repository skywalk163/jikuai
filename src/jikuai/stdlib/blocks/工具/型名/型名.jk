-- 极快 v0.13.0 块生态：工具.型名
-- 原子块（层级 0）——返回值的运行时类型名（"整数"/"列表"/"字典"…）。
--
-- 导出名换名记录：原取 `类识` 不可用 —— `类` 是 KEYWORD（`类 甲：`）。改 `型别`。
--
-- 实现走背衬：内建 `类名` 只接受用户类实例，对 int/str 原生值报错；
-- 本块要覆盖所有值，故用 `型名.py` 把 Python 类型名映射成中文。

导出 型别。
