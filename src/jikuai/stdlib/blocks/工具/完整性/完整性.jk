-- 极快 v0.12.0 块生态：工具.完整性
-- 一级块（层级 1）——返回 [SHA256摘要, Base64编码, 长度] 三元列表。
-- 依赖原子块：摘要（指纹）、转码（密文）。
--
-- 导出名 `核验` 词法原子 ✓

从 blocks.工具.摘要 导入 指纹。
从 blocks.工具.转码 导入 密文。

函数 核验 接收 赵文本：
  定义 赵摘 = 指纹(赵文本)。
  定义 赵编 = 密文(赵文本)。
  定义 赵长 = 长度 赵文本。
  返回 列 赵摘 赵编 赵长。
。

导出 核验。
