-- 极快 v0.12.0 块生态：工具.断言
-- 原子块（层级 0）——判定两值相等；不等抛出可读的错误消息。
--
-- 命名（ADR-15 §3.7）：
--   导出名 `验实` 词法原子 ✓（`断言` 若作导出名同样原子，但语义更狭窄）。
--   `赵实际` / `赵预期` 都实测原子。
--
-- 只用内建 `等于`/`如果`/`抛出`/`拼接`，无 Python 桥依赖，故 稳定性=stable。

函数 验实 接收 赵实际 赵预期：
  如果 等于 赵实际 赵预期 那么：
    返回 真。
  。
  抛出 拼接 "断言失败：预期=" 转字符串 赵预期 "，实际=" 转字符串 赵实际。
。

导出 验实。
