-- 极快 v0.12.0 块生态：工具.日志
-- 原子块（层级 0）——带级别前缀打印，并把原消息回传，便于串在管道里。
--
-- 命名（ADR-15 §3.7 词法原子性）：
--   `日志` 作导出名不行——它在**调用方**一侧被切成 `日`+`志`? 不，
--   真正的问题是可读性与语义重复；这里选 `志记`，实测 tokenize 得
--   `[('IDENT','志记')]`，单个 IDENT，词法原子 ✓
--   形参 `赵级别` / `赵消息` 同样实测原子（`赵列表` 会因 `列`(VERB) 切碎）。
--
-- 本块不碰 Python 桥，只用内建 `拼接` / `打印`，故 稳定性=stable。

函数 志记 接收 赵级别 赵消息：
  定义 赵全 = 拼接 "[" 赵级别 "] " 赵消息。
  打印 赵全。
  返回 赵消息。
。

导出 志记。
