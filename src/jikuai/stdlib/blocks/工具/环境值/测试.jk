-- 单元测试：`工具.环境值.环境`。

从 blocks.工具.环境值 导入 环境。

-- 读一个确定不存在的环境变量，应回退默认值
定义 赵r = 环境("JIKUAI_UNITTEST_NO_SUCH_VAR", "回退")。
打印 赵r。
打印 等于 赵r "回退"。
-- PATH 在几乎所有系统上存在
定义 赵p = 环境("PATH", "空")。
打印 不等于 赵p "空"。
