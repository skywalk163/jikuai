-- 单元测试：`工具.目录清单.名录`（os.listdir 语义）。
-- W115（v0.24.0）：自足测试，不再依赖仓库布局。原实现写死列
-- `stdlib/blocks/工具` 并断言含 `日志`——stdlib 搬进包 + runner chdir 到
-- tmp_path 后该路径不存在，且这种「测试知道仓库长什么样」的断言本就脆。
-- 现在改成：先用兄弟块 `存文.落盘` 在当前目录造一个标记文件，再列当前目录，
-- 断言列表里有它。runner 已 chdir 到 pytest tmp_path，产物自动回收。
从 blocks.工具.目录清单 导入 名录。
从 blocks.数据.存文 导入 落盘。

落盘("标记.txt" "x")。
定义 赵项 = 名录(".")。
打印 赵项。
-- 列表里应含刚造的标记文件
打印 包含 赵项 "标记.txt"。
打印 大于 长度 赵项 0。
