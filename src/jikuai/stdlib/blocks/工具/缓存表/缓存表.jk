-- 极快 v0.13.0 块生态：工具.缓存表
-- 原子块（层级 0）——往字典缓存里写一条并返回新字典（不修改原字典）。
--
-- 导出名 `存表` 已验证词法原子（ADR-15 §3.7）。
--
-- 内建 `合并` 是不可变操作（返回新字典），本块把「加一个键」这个高频用法
-- 封装成语义直白的动词，避免调用方每次都构造 `{键: 值}` 临时字典。

函数 存表 接收 赵键 赵值 赵表：
  定义赵新=合并 赵表 {赵键: 赵值}。
  返回 赵新。
。

导出 存表。
