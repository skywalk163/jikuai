-- 极快 v0.12.0 块生态：工具.计时
-- 原子块（层级 0）——测量一个无参函数的执行耗时（毫秒）。
-- 依赖 蟒:time，故 稳定性=experimental。
--
-- 导出名 `秒表` 词法原子 ✓
--
-- 返回值为列表 [函数返回值, 耗时毫秒数]，方便调用方同时拿到结果和耗时。

导入 蟒:time。

函数 秒表 接收 赵活：
  定义 赵始 = time.time()。
  定义 赵果 = 赵活()。
  定义 赵终 = time.time()。
  定义 赵差 = 减 赵终 赵始。
  定义 赵毫秒 = 乘 赵差 1000。
  返回 列 赵果 赵毫秒。
。

导出 秒表。
