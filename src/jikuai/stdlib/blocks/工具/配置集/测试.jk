-- 单元测试：`工具.配置集.载配`。
-- 幂等：只读环境变量，不写任何东西。
-- 注意：刻意不打印 PATH 的内容（会把宿主目录结构回显到 CI 日志里），
-- 只断言「有值 / 无值」这两个可判定的性质。

从 blocks.工具.配置集 导入 载配。

定义 赵果 = 载配(列 "PATH" "JIKUAI_UNITTEST_NO_SUCH_VAR")。

-- 1) 输出列表长度与输入键数一致
打印 等于 长度 赵果 2。

-- 2) PATH 存在 → 非空串
定义 赵有 = 取值 赵果 0。
打印 不等于 赵有 ""。

-- 3) 不存在的键 → 空串（并已在上方打印 [警告] 行）
定义 赵无 = 取值 赵果 1。
打印 等于 赵无 ""。
