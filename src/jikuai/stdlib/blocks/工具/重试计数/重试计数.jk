-- 极快 v0.13.0 块生态：工具.重试计数
-- 原子块（层级 0）——判定重试是否还该继续：当前次数小于上限则真。
--
-- 导出名 `数试` 已验证词法原子（ADR-15 §3.7）。
--
-- 形参换名记录：原用 `赵次` 不可用 —— `次` 是 KEYWORD（`重复 N 次`），
-- `赵次` 被切成 `赵`(IDENT)+`次`(KEYWORD)。改用 `赵轮`（已验证单 IDENT）。
--
-- 与 blocks.网络.重试策略 的分工：那个块决定「等多久后重试」（退避策略），
-- 本块只决定「是否还该重试」（次数守卫）。两者组合用。

函数 数试 接收 赵轮 赵上限：
  返回 小于 赵轮 赵上限。
。

导出 数试。
