-- 极快 v0.13.0 块生态：工具.随机码
-- 原子块（层级 0）——生成指定位数的随机字母数字码。
--
-- 导出名 `掷码` 已验证词法原子（ADR-15 §3.7）。
--
-- 与 工具.唯一码（UUID 格式）区别：本块生成的是短随机码（如 6-16 位），
-- 用于短链、验证码、临时口令等场景。
-- 实现走背衬：用 `secrets` 而非 `random`，避免可预测的伪随机序列。

导出 掷码。
