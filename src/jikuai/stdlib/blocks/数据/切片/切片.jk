-- 极快 v0.13.0 块生态：数据.切片
-- 原子块（层级 0）——取列表的 [起, 止) 半开区间子段。
--
-- 导出名 `截段` 已验证词法原子（ADR-15 §3.7）。
-- 半开区间（含起不含止）与内建索引惯例一致：`截段(表, 0, 3)` 取前三项。

函数 截段 接收 赵项表 赵起 赵止：
  定义赵集=列。
  定义赵计=赵起。
  当 小于 赵计 赵止：
    定义赵项=取值 赵项表 赵计。
    赋值赵集=追加 赵集 赵项。
    赋值赵计=加 赵计 1。
  。
  返回 赵集。
。

导出 截段。
