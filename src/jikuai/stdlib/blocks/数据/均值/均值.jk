-- 极快 v0.12.0 块生态：数据.均值
-- 原子块（层级 0）——计算列表的平均值。
-- 导出名 `均数` 已验证词法原子（ADR-15 §3.7）。
-- 注意：此块算的是算术平均值，不是中位数。中位数在 `数据.中位数` 块（导出 `中值`）。
-- 历史：v0.26.0 W132 前导出名叫 `中位`，与 `数据.中位数` 混淆，已改名。

函数 均数 接收 赵数值：
  返回 平均 赵数值。
。

导出 均数。
