-- 极快 v0.12.0 块生态：数据.存文
-- 原子块（层级 0）——把字符串写入文件，返回写入的字符串。
-- 导出名 `落盘` 已验证词法原子（ADR-15 §3.7）。
--
-- 目录名换名记录：原计划目录名 `文件落盘` 不可用。
-- `文件落盘` 分词为 `文件`(KEYWORD) + `落盘`(IDENT) 两个 token，
-- 而点分模块路径每段只能是一个 token（详见 数据.载入 的说明）。
-- 故目录名改为词法原子的 `存文`。
--
-- v0.16.0 W28：内建 `写入` 已带安全边界落地（路径限 CWD 内 + UTF-8），
-- 原 `存文.py` 背衬回收，本块改为纯 .jk 实现。

函数 落盘 接收 赵路径 赵文本：
  返回 写入 赵路径 赵文本。
。

导出 落盘。
