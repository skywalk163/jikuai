-- 块 `数据.存文` 的单元测试。
-- W114（v0.24.0）：临时文件名**只写裸文件名**，落在运行时的当前目录。
-- runner（`tests/test_blocks_smoke.py`）会先 chdir 到 pytest 的 tmp_path，
-- 所以这些文件生成在临时目录里、随 pytest 自动回收，不再污染源码树。
-- 原先写死的 `stdlib/blocks/数据/存文/临时_测试*.txt` 有两个问题：
-- 相对仓库根的路径在 stdlib 搬进包后已失效；且当年真把产物提交进了库。
从 blocks.数据.存文 导入 落盘。

-- 测试1：普通字符串
定义赵果=落盘("临时_测试.txt" "测试内容")。
打印 赵果。

-- 测试2：空字符串（边界）
定义赵空=落盘("临时_测试2.txt" "")。
打印 赵空。

-- 测试3：多行文本（特殊情况：含换行转义）
定义赵多=落盘("临时_测试3.txt" "行1\n行2\n行3")。
打印 赵多。
