-- 块 `数据.序出` 的单元测试。
-- W114（v0.24.0）：临时文件名只写裸文件名，落在运行时当前目录；
-- runner 会 chdir 到 pytest 的 tmp_path，产物随测试自动回收。
从 blocks.数据.序出 导入 存档。

-- 测试1：乱序列表 → 期望行数 5
定义赵集=列 5 3 1 4 2。
定义赵条数=存档(赵集 "临时_测试.txt")。
打印 赵条数。

-- 测试2：单元素（边界）→ 期望 1
定义赵单=列 42。
定义赵条数2=存档(赵单 "临时_测试2.txt")。
打印 赵条数2。

-- 测试3：已升序（特殊情况：排序为恒等）→ 期望 3
定义赵已排=列 1 2 3。
定义赵条数3=存档(赵已排 "临时_测试3.txt")。
打印 赵条数3。
