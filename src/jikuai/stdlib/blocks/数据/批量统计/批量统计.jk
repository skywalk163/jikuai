-- 极快 v0.12.0 块生态：数据.批量统计
-- 一级块（层级 1）——输入数值列表，返回 [总和, 均值, 最小, 最大] 四元列表。
-- 导出名 `统览` 已验证词法原子（ADR-15 §3.7）。
-- 依赖块：求和、均值、极值

从 blocks.数据.求和 导入 汇总。
从 blocks.数据.均值 导入 中位。
从 blocks.数据.极值 导入 峰谷。

函数 统览 接收 赵数值：
  定义赵总=汇总(赵数值)。
  定义赵均=中位(赵数值)。
  定义赵端=峰谷(赵数值)。
  定义赵小=首个 赵端。
  定义赵大=末个 赵端。
  返回 列 赵总 赵均 赵小 赵大。
。

导出 统览。
