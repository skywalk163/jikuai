-- 块 `数据.批量统计` 的单元测试。
-- 负数写作一元动词 `负 N`（lexer 不支持 `-N` 字面量）。
从 blocks.数据.批量统计 导入 统览。

-- 测试1：普通数值列表 → 期望 [15, 3.0, 1, 5]
定义赵集=列 1 2 3 4 5。
打印 统览(赵集)。

-- 测试2：单元素（边界）→ 期望 [10, 10.0, 10, 10]
定义赵单=列 10。
打印 统览(赵单)。

-- 测试3：含负数（特殊情况）→ 期望 [4, 1.0, -2, 4]
定义赵负=列 负 2 0 2 4。
打印 统览(赵负)。
