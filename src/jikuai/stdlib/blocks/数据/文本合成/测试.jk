-- 块 `数据.文本合成` 的单元测试。
从 blocks.数据.文本合成 导入 缝合。

-- 测试1：逗号合成 → 期望 "苹果,香蕉,橙子"
定义赵集=列 "苹果" "香蕉" "橙子"。
打印 缝合(赵集 ",")。

-- 测试2：空列表（边界）→ 期望 ""
定义赵空=列。
打印 缝合(赵空 ",")。

-- 测试3：数值列表 + 换行分隔（特殊情况：元素非字符串，自动转字符串）
定义赵数=列 1 2 3。
打印 缝合(赵数 "|")。
