-- 块 `数据.极值` 的单元测试。
-- 注意：极快 lexer 不支持负号字面量（`-5` 报「非法字符 '-'」），
-- 负数要写成一元动词 `负 5`。
从 blocks.数据.极值 导入 峰谷。

-- 测试1：普通列表 → 期望 [1, 9]
定义赵集=列 3 1 4 1 5 9 2 6。
打印 峰谷(赵集)。

-- 测试2：单元素（边界）→ 期望 [42, 42]
定义赵单=列 42。
打印 峰谷(赵单)。

-- 测试3：全负数（特殊情况）→ 期望 [-5, -1]
定义赵负=列 负 5 负 1 负 3 负 2。
打印 峰谷(赵负)。
