-- 极快 v0.13.0 块生态：数据.标准差
-- 一级块（层级 1）——方差的平方根，与原数据同量纲。
--
-- 导出名 `标差` 已验证词法原子（ADR-15 §3.7）。
-- 极快没有 `开方` 内建动词，用 `幂 x 0.5` 等价。

从 blocks.数据.方差 导入 离差。

函数 标差 接收 赵项表：
  定义赵方=离差(赵项表)。
  返回 幂 赵方 0.5。
。

导出 标差。
