-- 极快 v0.12.0 块生态：数据.求和
-- 原子块（层级 0）——对数值列表求和。
--
-- 命名约束（ADR-15 §3.7「词法原子性」）：
-- 块的**导出名必须是词法原子的**，即整体被 lexer 识别为单个 IDENT。
-- 调用方在 `导入` 时无法预知本模块的导出名，所以 ADR-09 的作用域白名单
-- 与成员访问松弛都帮不上忙——非原子名在调用方一侧必然被切碎。
--   `块求和` → `块`(IDENT) + `求和`(VERB)   ✗ 调用方拿不到
--   `汇总`   → `汇总`(IDENT)                 ✓
-- 故导出名取 `汇总`，而不是 `块求和` / `累加`（`累加` 会切成 `累`+`加`）。
--
-- 形参同理：`赵列表` 会切成 `赵`+`列`(VERB)+`表`，故用 `赵数值`。

函数 汇总 接收 赵数值：
  返回 求和 赵数值。
。

导出 汇总。
