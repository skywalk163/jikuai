-- 块 `数据.载入` 的单元测试。
-- 用兄弟块 `存文.落盘` 先造样本再读回来对照。
-- W114（v0.24.0）：临时文件名只写裸文件名，落在运行时当前目录；
-- runner 会 chdir 到 pytest 的 tmp_path，产物随测试自动回收。
从 blocks.数据.载入 导入 装载。
从 blocks.数据.存文 导入 落盘。

-- 测试1：普通中文文本
落盘("临时_测试.txt" "你好极快")。
定义赵果=装载("临时_测试.txt")。
打印 赵果。

-- 测试2：空文件（边界）
落盘("临时_测试2.txt" "")。
定义赵空=装载("临时_测试2.txt")。
打印 赵空。

-- 测试3：多行文本（特殊情况：换行原样保留）
落盘("临时_测试3.txt" "甲\n乙\n丙")。
定义赵多=装载("临时_测试3.txt")。
打印 赵多。
