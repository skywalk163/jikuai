-- 极快 v0.12.0 块生态：数据.载入
-- 原子块（层级 0）——读取文本文件内容为字符串。
-- 导出名 `装载` 已验证词法原子（ADR-15 §3.7）。
--
-- 目录名换名记录：原计划目录名 `文件装载` 不可用。
-- 点分模块路径的**每一段也必须是单个 token**（parser._read_module_name 每个
-- `.` 之后只取一个 token）。`文件装载` 分词为 `文件`(KEYWORD) + `装载`(IDENT)
-- 共两个 token，导致 `从 blocks.数据.文件装载 导入 装载。` 报
-- 「从...期望 导入」语法错误。故目录名改为词法原子的 `载入`。
--
-- v0.16.0 W28：内建 `读取` 已带安全边界落地（路径限 CWD 内 + 10 MiB 上限
-- + UTF-8），原 `载入.py` 背衬回收，本块改为纯 .jk 实现。

函数 装载 接收 赵路径：
  返回 读取 赵路径。
。

导出 装载。
