-- 块 `网络.响应处理` 的单元测试。
-- **全部用固定的状态码 + 响应体字面量，绝不发起网络请求**。

从 blocks.网络.响应处理 导入 收讯。

-- 断言 1：200 + JSON 体 → 解析成字典。期望 [True, {'ok': 1}]
打印 收讯(200 "{\"ok\": 1}")。

-- 断言 2：404 + HTML 错误页 → 不解析体，给错误描述。
-- 期望 [False, '请求失败 404 客户端错误']
打印 收讯(404 "<html>Not Found</html>")。

-- 断言 3：500 + 空体 → 不因空体崩溃。期望 [False, '请求失败 500 服务端错误']
打印 收讯(500 "")。

-- 断言 4：201 + JSON 数组 → 成功且解析。期望 [True, [1, 2]]
打印 收讯(201 "[1,2]")。

-- 断言 5：取数据分量并继续访问键。期望 42
定义赵果=收讯(200 "{\"n\": 42}")。
定义赵数据=取值 赵果 1。
打印 赵数据.n。

-- 断言 6：失败时成败分量为假。期望 假
打印 取值 收讯(503 "") 0。
