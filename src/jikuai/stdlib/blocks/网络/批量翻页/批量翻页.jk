-- 极快 v0.12.0 块生态：网络.批量翻页
-- **一级块（层级 1）**——基址 + 路径 + 总数 + 每页数 → 各页完整 URL 列表
--
-- 命名约束（ADR-15 §3.7）：`巡页` 是原子 IDENT。
--
-- 聚合的两个原子块：
--   分页参数 → 翻页：总数+每页 ⇒ offset 列表
--   网址拼装 → 组址：基址+路径+查询串 ⇒ 完整 URL
--
-- 契约取舍（与任务表的偏差，已在交付报告说明）：
-- 任务表写「总数+每页数 → 各页完整 URL 列表」。实际上要产出完整 URL 必须知道
-- 基址与路径，否则只能返回 offset 列表 —— 那就退化成 `翻页` 了。故本块签名
-- 扩展为 (基址, 路径, 总数, 每页)。
--
-- 查询串直接手工拼 `offset=X&limit=Y`（不引入 `查询串` 作依赖，减少依赖面）：
-- 值都是整数，无需百分号编码，简单可预测。
--
-- 稳定性：`stable`（`翻页`、`组址` 均 stable，未走 pybridge）。

从 blocks.网络.分页参数 导入 翻页。
从 blocks.网络.网址拼装 导入 组址。

函数 巡页 接收 赵基址 赵路径 赵总 赵每页：
  定义赵偏集=翻页(赵总 赵每页)。
  定义赵网集=列。
  定义赵每串=转字符串 赵每页。
  遍历赵偏于赵偏集：
    定义赵偏串=转字符串 赵偏。
    定义赵串=拼接 "offset=" 赵偏串 "&limit=" 赵每串。
    定义赵网=组址(赵基址 赵路径 赵串)。
    赋值赵网集=追加 赵网集 赵网。
  。
  返回 赵网集。
。

导出 巡页。
