-- 块 `网络.批量翻页` 的单元测试。**纯 URL 构造，绝不发起网络请求**。

从 blocks.网络.批量翻页 导入 巡页。

-- 断言 1：25 条每页 10 → 3 页。
-- 期望 ['https://a.b/x?offset=0&limit=10',
--       'https://a.b/x?offset=10&limit=10',
--       'https://a.b/x?offset=20&limit=10']
打印 巡页("https://a.b" "/x" 25 10)。

-- 断言 2：整除边界 20 条每页 10 → 2 页
打印 巡页("http://h" "/p" 20 10)。

-- 断言 3：不足一页
打印 巡页("http://h" "/p" 5 10)。

-- 断言 4：0 条 → 空列表
打印 巡页("http://h" "/p" 0 10)。

-- 断言 5：页数可数。100 条每页 30 → 4 页
打印 长度 巡页("http://h" "/p" 100 30)。

-- 断言 6：取第一页 URL。期望 http://h/p?offset=0&limit=30
打印 首个 巡页("http://h" "/p" 100 30)。
