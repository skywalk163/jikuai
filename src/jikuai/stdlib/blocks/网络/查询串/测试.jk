-- 块 `网络.查询串` 的单元测试。
-- 纯函数验证，**不发任何网络请求**（`urllib.parse` 只做字符串处理）。

从 blocks.网络.查询串 导入 参串。

-- 断言 1：多键按字典序输出，与字面量书写顺序无关。期望 a=1&b=2
打印 参串({"b": 2, "a": 1})。

-- 断言 2：空格编码为 %20、斜杠编码为 %2F（safe="" 生效）。期望 q=x%20y%2Fz
打印 参串({"q": "x y/z"})。

-- 断言 3：空字典 → 空串（调用方据此决定是否拼 `?`）。期望空行
打印 参串({})。

-- 断言 4：中文值 UTF-8 百分号编码。期望 name=%E5%BC%A0%E4%B8%89
打印 参串({"name": "张三"})。

-- 断言 5：整数值经 转字符串 归一。期望 limit=10&offset=0
打印 参串({"offset": 0, "limit": 10})。
