-- 块 `网络.状态判定` 的单元测试。纯数值判断，不触网络。
-- 注：布尔在**列表内**按 Python repr 打印为 True/False（顶层单值才显示 真/假）。

从 blocks.网络.状态判定 导入 判态。

-- 断言 1：200 成功。期望 [True, '成功']
打印 判态(200)。

-- 断言 2：404 客户端错误。期望 [False, '客户端错误']
打印 判态(404)。

-- 断言 3：500 服务端错误。期望 [False, '服务端错误']
打印 判态(500)。

-- 断言 4：301 重定向。期望 [False, '重定向']
打印 判态(301)。

-- 断言 5：100 信息。期望 [False, '信息']
打印 判态(100)。

-- 断言 6：取分量（顶层布尔显示 真）。期望 真
打印 取值 判态(204) 0。
