-- 极快 v0.12.0 块生态：网络.状态判定
-- 原子块（层级 0）——HTTP 状态码 → [是否成功, 类别描述]。
--
-- 命名约束（ADR-15 §3.7）：`判态` 是原子 IDENT。
--
-- 设计取舍：
-- 1. **纯极快实现，不碰 Python 桥** ⇒ 稳定性 `stable`。
-- 2. 「成功」= 2xx（200–299），类别描述取五大段：信息/成功/重定向/客户端错误/服务端错误。
-- 3. 返回列表 [布尔, 字符串]，调用方用 `取值 赵结果 0` / `取值 赵结果 1` 取分量。
-- 4. `是否成功` **不用** `且 大于等于 赵码 200 小于 赵码 300` 计算。
--    实测该免括号写法结果错误（`判态(200)` 得 300 而非 真）：嵌套动词在实参位
--    的结合方向与直觉相反，`且` 拿到的不是两个比较结果。
--    改为在 if/否则如果 链的「成功」分支里直接置真 —— 分支本身已经蕴含
--    200<=码<300，既省一次判断，也彻底避开解析歧义。

函数 判态 接收 赵码：
  定义赵成功=假。
  定义赵述=""。
  如果 小于 赵码 200 那么：
    赋值赵述="信息"。
  否则如果 小于 赵码 300 那么：
    -- 落到这里 ⇔ 200 <= 码 < 300
    赋值赵述="成功"。
    赋值赵成功=真。
  否则如果 小于 赵码 400 那么：
    赋值赵述="重定向"。
  否则如果 小于 赵码 500 那么：
    赋值赵述="客户端错误"。
  否则：
    赋值赵述="服务端错误"。
  。
  返回 列 赵成功 赵述。
。

导出 判态。
