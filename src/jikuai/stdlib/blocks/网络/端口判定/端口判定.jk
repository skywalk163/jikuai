-- 极快 v0.13.0 块生态：网络.端口判定
-- 原子块（层级 0）——把端口号归类为「系统/注册/动态/无效」。
--
-- 命名（ADR-15 §3.7）：导出 `判端`、目录 `端口判定`、形参 `赵端` 均已跑原子性预检。
-- 分档遵循 IANA：0..1023=系统，1024..49151=注册，49152..65535=动态，其余=无效。
-- 实现：纯极快，多路 if-else。

函数 判端 接收 赵端:
  如果 小于 赵端 0 那么:
    返回 "无效"。
  否则如果 小于 赵端 1024 那么:
    返回 "系统"。
  否则如果 小于 赵端 49152 那么:
    返回 "注册"。
  否则如果 小于 赵端 65536 那么:
    返回 "动态"。
  否则:
    返回 "无效"。
  。
。

导出 判端。
