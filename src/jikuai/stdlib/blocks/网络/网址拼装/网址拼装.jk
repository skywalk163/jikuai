-- 极快 v0.12.0 块生态：网络.网址拼装
-- 原子块（层级 0）——基址 + 路径 + 查询串 拼成完整 URL。
--
-- 命名约束（ADR-15 §3.7）：导出名 `组址` 整体是单个 IDENT。
-- 不取 `拼址`（虽也原子，但 `拼` 易联想 `拼接` 动词）；不取 `连址`（`连接` 风险）。
-- 自检：tokenize("组址。") == [('IDENT','组址')] ✓
--
-- 契约取舍（与任务表的偏差，已在交付报告说明）：
-- 第三参 `赵查询` 收的是**已编码的查询字符串**（`参串` 的输出），不是字典。
-- 理由：
--   1. 原子块（层级 0）之间不得相互依赖，若这里收字典就得自己再实现一遍
--      编码逻辑，与 `查询串` 块重复；收字符串则让 `参串 → 组址` 天然串成管道。
--   2. 由此 `组址` 退化为**纯字符串拼接**，不碰 Python 桥，稳定性可标 `stable`，
--      而把「需要桥」的编码职责单独隔离在 `查询串` 块里。
--   3. 一级块 `请求构造`/`批量翻页` 正是「先 `参串`/构造串，再 `组址`」的组合，
--      契约吻合。
--
-- 边界：查询串为空 ⇒ 不追加 `?`；非空 ⇒ 追加 `?查询`。基址与路径原样拼接，
-- 由调用方负责各自的斜杠约定（本块不猜测、不规整，保持可预测）。

函数 组址 接收 赵基址 赵路径 赵查询：
  定义赵网=拼接 赵基址 赵路径。
  如果 等于 赵查询 "" 那么：
    返回 赵网。
  否则：
    返回 拼接 赵网 "?" 赵查询。
  。
。

导出 组址。
