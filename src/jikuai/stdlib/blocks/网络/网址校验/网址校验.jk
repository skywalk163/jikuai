-- 极快 v0.13.0 块生态：网络.网址校验
-- 原子块（层级 0）——校验 URL 字符串是否合法（含 scheme + netloc）。
--
-- 命名（ADR-15 §3.7）：导出 `核址`、目录 `网址校验`、形参 `赵网址` 均已跑原子性预检。
-- 实现走背衬（ADR-16 §3.3）：借 urllib.parse.urlparse 检查协议和主机。
-- `urllib.parse` 不发起网络连接，本地校验安全。

导出 核址。
