-- 极快 v0.13.0 块生态：网络.表单串
-- 原子块（层级 0）——把字典编码成 form-urlencoded（`a=1&b=2`，空格→`+`）。
--
-- 命名（ADR-15 §3.7）：导出 `表串`、目录 `表单串`、形参 `赵对` 均已跑原子性预检。
-- 与 `网络.查询串` 区别：本块空格用 `+`（application/x-www-form-urlencoded 惯例），
-- 而 `查询串` 用 `%20`（RFC 3986 query）。POST 表单场景用本块。
-- 实现走背衬：urllib.parse.urlencode 处理排序与编码。

导出 表串。
