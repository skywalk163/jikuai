-- 极快 v0.12.0 块生态：网络.记法织串
-- 原子块（层级 0）——极快字典/列表/标量 → JSON 字符串。
--
-- 命名约束（ADR-15 §3.7）：`织串` 是原子 IDENT。
-- 目录名 `记法织串` 也是原子 IDENT。注意任务建议的 `JSON序列` 不可用：
-- `JSON` 是拉丁串会与后随汉字切成两个 token，且 `序列` 中的 `列` 是列表动词，
-- 会被切出 —— 故整段无法作为 `blocks.网络.<目录名>` 的路径段（见交付报告）。
--
-- 实现取舍：
--   借 `蟒:json.dumps`。`jk_to_py` 会把极快字典/列表递归转成 Python dict/list，
--   dumps 默认 `ensure_ascii=True`，非 ASCII 字符输出为 \uXXXX 转义 —— 这对
--   跨系统传输最稳妥，且使测试输出确定。
--
-- 稳定性：`experimental`（依赖 pybridge）。`json.dumps` 不发起网络。

导入 蟒:json。

函数 织串 接收 赵数据：
  返回 json.dumps(赵数据)。
。

导出 织串。
