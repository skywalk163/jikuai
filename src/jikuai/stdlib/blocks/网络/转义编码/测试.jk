-- 块 `网络.转义编码` 的单元测试。纯字符串处理，不触网络。

从 blocks.网络.转义编码 导入 转义。

-- 断言 1：空格 → %20。期望 a%20b
打印 转义("a b")。

-- 断言 2：斜杠 → %2F。期望 a%2Fb
打印 转义("a/b")。

-- 断言 3：中文 UTF-8 编码。期望 %E4%BD%A0%E5%A5%BD
打印 转义("你好")。

-- 断言 4：已是 ASCII 无保留字 → 不变。期望 hello123
打印 转义("hello123")。

-- 断言 5：特殊符号组合。期望 a%3Db%26c
打印 转义("a=b&c")。
