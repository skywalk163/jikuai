-- 极快 v0.12.0 块生态：网络.重试策略
-- 原子块（层级 0）——重试次数 → 指数退避毫秒列表（如 3 → [100, 200, 400]）。
--
-- 命名约束（ADR-15 §3.7）：`退避` 是原子 IDENT。
-- 形参不能叫 `赵次数`（`次` 是关键字 KW_TIMES，会切成 `赵`+`次`+`数`），改用 `赵回数`。
--
-- 设计取舍：
-- 1. **纯极快实现，不碰 Python 桥** ⇒ 稳定性 `stable`。
-- 2. 退避序列 = base * 2^i，base = 100ms，i 从 0 起。第 i 次重试前等待 100·2^i 毫秒。
--    用 `遍历 于 范围 0 n` 生成下标，`乘 100 (幂 2 赵步)` 算每档时长。
--    ⚠️ **括号是必需的**：免括号写法 `乘 100 幂 2 赵步` 会被解析成
--    `乘 (幂 100 2) 赵步` = 10000·step（实测 3 → [0,10000,20000]），
--    即嵌套动词在实参位置时结合方向与直觉相反。凡「动词嵌动词」一律加括号。
-- 3. n<=0 → 空列表（`范围 0 0` = []），语义：不重试。

函数 退避 接收 赵回数：
  定义赵集=列。
  遍历赵步于范围 0 赵回数：
    定义赵毫=乘 100 (幂 2 赵步)。
    赋值赵集=追加 赵集 赵毫。
  。
  返回 赵集。
。

导出 退避。
