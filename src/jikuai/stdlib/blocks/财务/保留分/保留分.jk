-- 极快 v0.13.0 块生态：财务.保留分
-- 原子块（层级 0）——把数值四舍五入到分（两位小数）。
--
-- 导出名 `圆分` 已验证词法原子（ADR-15 §3.7）。
--
-- 实现走背衬：极快没有内建的四舍五入动词，且必须用 Decimal 的
-- ROUND_HALF_UP（中国会计惯例）而不是 Python 的 banker's rounding，
-- 故下沉到同目录 `保留分.py`（ADR-16 §3.3），本 .jk 仅做门面与导出。
--
-- 本块是财务领域的公共依赖：所有产出金额的财务块都用它做分位收口。

导出 圆分。
