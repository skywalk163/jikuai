-- 块 `财务.保留分` 的单元测试。

从 blocks.财务.保留分 导入 圆分。

-- 常规四舍五入
打印 圆分(3.14159)。

-- ROUND_HALF_UP 的关键分界：2.675 → 2.68（Python round() 会给 2.67）
打印 圆分(2.675)。

-- 已经是两位小数时保持不变
打印 圆分(10.5)。

-- 整数入参
打印 圆分(100)。

-- 负数
打印 圆分(-1.235)。
