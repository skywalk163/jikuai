-- 极快 v0.13.0 块生态：财务.增值税
-- 原子块（层级 0）——价税分离：含税价按税率拆成不含税价与税额。
--
-- 导出名 `增税` 已验证词法原子（ADR-15 §3.7）。
--
-- 公式：不含税价 = 含税价 ÷ (1 + 税率)；税额 = 含税价 − 不含税价。
-- 返回 [不含税价, 税额] 二元列表。

从 blocks.财务.保留分 导入 圆分。

函数 增税 接收 赵含税 赵税率：
  定义赵底=加 1 赵税率。
  定义赵净价=圆分(除 赵含税 赵底)。
  定义赵税额=圆分(减 赵含税 赵净价)。
  返回 列 赵净价 赵税额。
。

导出 增税。
