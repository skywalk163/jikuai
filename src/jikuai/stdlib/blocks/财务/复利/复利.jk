-- 极快 v0.13.0 块生态：财务.复利
-- 原子块（层级 0）——复利滚存：本金 × (1 + 年利率) ^ 年数，返回到期本息合计。
--
-- 导出名 `滚利` 已验证词法原子（ADR-15 §3.7）。
-- 返回**本息合计**而不是纯利息：复利场景里用户要的是"到期能拿多少"，
-- 想要纯利息用 `减 结果 本金` 即可，反过来还得知道公式。
--
-- `幂 底 指` 是内建动词（元数 2）。`(1+r)^n` 写作 `幂 加 1 赵年率 赵年数`。

从 blocks.财务.保留分 导入 圆分。

函数 滚利 接收 赵本金 赵年率 赵年数：
  定义赵底=加 1 赵年率。
  定义赵倍=幂 赵底 赵年数。
  返回 圆分(乘 赵本金 赵倍)。
。

导出 滚利。
