-- 极快 v0.16.0 块生态：财务.客户对账
-- 三级块（层级 3）——按分期还款计划做客户对账：应还款计划 vs 实际到账
-- → 分组核对 → 差额判定 → 网络状态码 mock。
--
-- L3 判定（ADR-28 §3.1）：满足条件 2 —— 1 个 L2+ 依赖（月结单），
-- 依赖跨 3 个领域：财务(月结单) / 数据(分组) / 网络(状态判定)。
--
-- 稳定性：experimental。三条直接依赖都是 stable（月结单 / 分组 /
-- 状态判定），§3.2 允许标 stable，但本块首版对账语义边界（哪些差额算
-- 「已对平」？部分到账怎么记？）未经真实业务打磨，保守标 experimental。
--
-- 【网络域的 mock 约定】不发任何真请求，也不引任何 IO。用 网络.状态判定
-- 把差额编码成 HTTP 状态码：差 = 0 → 200（对账成功）；差 != 0 → 400
-- （客户端错误，对账未平）。真实系统里 200/400 应由远端对账服务返回；
-- 这里在本地用 判态 复现同一份 [是否成功, 描述] 元组，让下游写「对账成功/
-- 失败」分支的代码有稳定的类型契约。W30 明文要求「网络块只用本地可 mock
-- 的部分」，状态判定 是 stable、纯极快、零 IO，正合此意。
--
-- 流水（对齐 W30 场景描述）：
--   1) 数据载入：调用方负责把 实收 列表准备好（读取 / 数据.载入 留给
--      端到端 demo 演示，本 L3 不再多绑一层 IO 依赖 —— 那会把块从纯函数
--      变成有副作用的东西，也会让 测试.jk 需要预置文件）
--   2) 分组汇总：财务.月结单 结单(...) → [月供, 总息, 大写, 每期明细]
--      取 [3] 位 = 期供明细 → 应总 = 求和 明细（内建 VERB）
--      求和 实收表 → 实总
--   3) 差异比对：减 应总 实总 → 差；数据.分组 聚簇(实收, 每组) → 分组明细
--   4) 输出：网络.状态判定 判态(码) → [是否成功, 描述]
--
-- 返回 5 元组：[应总, 实总, 差, 分组明细, 判态结果]
--
-- 【为什么用月结单不用工资条】"客户对账"语义上是**账目**核对：月结单
-- 对应「客户分期账单」，工资条对应「个人薪资」，前者贴合。月结单同样是
-- 财务域现有的 L2 块，跨域预算够用（财务 + 数据 + 网络 = 3）。
--
-- 用户块函数必须括号调用（ADR-11）。
-- 形参 赵贷款额/赵期数/赵年利率/赵实收表/赵每组 与局部
-- 赵结/赵明细/赵应总/赵实总/赵差/赵簇/赵码/赵态 均已过原子性预检。

从 blocks.财务.月结单 导入 结单。
从 blocks.数据.分组 导入 聚簇。
从 blocks.网络.状态判定 导入 判态。

函数 对账 接收 赵贷款额 赵期数 赵年利率 赵实收表 赵每组：
  定义赵结=结单(赵贷款额, 赵期数, 赵年利率)。
  定义赵明细=取值 赵结 3。
  定义赵应总=求和 赵明细。
  定义赵实总=求和 赵实收表。
  定义赵差=减 赵应总 赵实总。
  定义赵簇=聚簇(赵实收表, 赵每组)。
  定义赵码=400。
  如果 等于 赵差 0 那么：
    赋值赵码=200。
  。
  定义赵态=判态(赵码)。
  返回 列 赵应总 赵实总 赵差 赵簇 赵态。
。

导出 对账。
