-- 块 财务.客户对账 的单元测试（三级块，聚合 月结单 + 分组 + 状态判定）。

从 blocks.财务.客户对账 导入 对账。

-- 断言 1（对账未平 · happy path）：应还 12 期（6% 年化），实付只有 6 期
-- 实测 [123935.64, 60000, 63935.64, [[10000,10000,10000],[10000,10000,10000]],
--       [False, '客户端错误']]
--   差 != 0 → 码 400 → 判态 给 [假, "客户端错误"]；分组每组 3 → 2 组
打印 对账(120000, 12, 0.06, 列 10000 10000 10000 10000 10000 10000, 3)。

-- 断言 2（对账平衡 · 零利率边界）：1 期 · 年率 0 → 月供 = 本金 10000
-- 实测 [10000.0, 10000, 0.0, [[10000]], [True, '成功']]
--   这是全套流水里**唯一**走 200 分支的用例，专门守住「差 = 0 → 对账成功」
打印 对账(10000, 1, 0, 列 10000, 1)。

-- 断言 3（超付 · 差为负的边界）：应还 120000（0 利率 12 期），实收 149000
-- 实测 [120000.0, 149000, -29000.0, [[12000,12000,12000,12000] ×3, [5000]],
--       [False, '客户端错误']]
--   负差同样判 400（不是「多付了就算平」）；分组末组不足 4 项 → 末组 = [5000]
打印 对账(120000, 12, 0, 列 12000 12000 12000 12000 12000 12000 12000 12000 12000 12000 12000 12000 5000, 4)。
