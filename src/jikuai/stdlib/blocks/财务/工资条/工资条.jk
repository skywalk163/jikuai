-- 极快 v0.14.0 块生态：财务.工资条
-- 二级块（层级 2）——员工月度工资条：
--   返回 [月薪, 个税, 税后收入, 税后收入中文大写, 员工周岁] 五元列表。
--
-- 导出名 `薪单` 已验证词法原子（ADR-15 §3.7）：tokenize('薪单。') → [('IDENT','薪单')]。
-- 目录名 `工资条` 亦已验证是单个 IDENT，可直接作点分路径片段。
--
-- 三域聚合（层级 2 的典型形态——被聚合的都是层级 1 或 0 的成品块）：
--   财务.税单   → 税据(月收)  → [月收, 个税, 税后, 税额大写]   层级 1
--   中文.金额报表 → 账单(数值) → [数值, 中文大写金额, 汉字数字]   层级 1
--   历法.周岁   → 实岁(生年,生月,生日,今年,今月,今日) → 数        层级 0
--
-- 注意本块取的是**税后收入**的大写（`账单(赵税后)[1]`），而不是 `税据` 自带的
-- 第四元——后者是**税额**大写。工资条上员工最关心的是到手金额，故此处替换。
--
-- 下标取值用 `赵表[N]`（0-based）；用户块函数必须括号调用（ADR-11）。

从 blocks.财务.税单 导入 税据。
从 blocks.中文.金额报表 导入 账单。
从 blocks.历法.周岁 导入 实岁。

函数 薪单 接收 赵月薪 赵生年 赵生月 赵生日 赵今年 赵今月 赵今日：
  定义赵税据=税据(赵月薪)。
  定义赵个税=赵税据[1]。
  定义赵税后=赵税据[2]。
  定义赵账单=账单(赵税后)。
  定义赵银码=赵账单[1]。
  定义赵周岁=实岁(赵生年, 赵生月, 赵生日, 赵今年, 赵今月, 赵今日)。
  返回 列 赵月薪 赵个税 赵税后 赵银码 赵周岁。
。

导出 薪单。
