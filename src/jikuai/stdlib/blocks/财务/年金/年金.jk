-- 极快 v0.13.0 块生态：财务.年金
-- 原子块（层级 0）——等额年金：已知现值求每期应付额。
--
-- 导出名 `年供` 已验证词法原子（ADR-15 §3.7）。
--
-- 公式：PMT = PV × r × (1+r)^n ÷ ((1+r)^n − 1)
-- 零利率是退化情形（分母为 0），单独走等额平摊分支，否则会除零报错。
-- 嵌套前缀算术一律拆中间变量：内联深嵌套会被 parser 误绑元数。

从 blocks.财务.保留分 导入 圆分。

函数 年供 接收 赵现值 赵期率 赵期数：
  如果 等于 赵期率 0 那么：
    返回 圆分(除 赵现值 赵期数)。
  。
  定义赵底=加 1 赵期率。
  定义赵倍=幂 赵底 赵期数。
  定义赵分子=乘 乘 赵现值 赵期率 赵倍。
  定义赵分母=减 赵倍 1。
  返回 圆分(除 赵分子 赵分母)。
。

导出 年供。
