-- 块 `财务.折旧` 的单元测试。

从 blocks.财务.折旧 导入 折价。

-- 原值 1 万、残值 1000、3 年：3000+3000+3000，合计 9000
打印 折价(10000, 1000, 3)。

-- 不能整除：原值 1 万、残值 0、3 年 → 3333.33 + 3333.33 + 3333.34
打印 折价(10000, 0, 3)。

-- 单年 → 全额折旧
打印 折价(5000, 500, 1)。
