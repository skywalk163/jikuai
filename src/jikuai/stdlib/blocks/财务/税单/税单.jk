-- 极快 v0.13.0 块生态：财务.税单
-- 一级块（层级 1）——月薪个税单：返回 [月收入, 个税, 税后, 税额大写] 四元列表。
--
-- 导出名 `税据` 已验证词法原子（ADR-15 §3.7）。
--
-- 跨领域聚合的典型例子：计算层取 财务.个税，表现层取 中文.金额雅写。
-- 这正是把「财务」与「中文」拆成两个领域的收益——各自单一职责，组合出报表。

从 blocks.财务.个税 导入 缴税。
从 blocks.财务.保留分 导入 圆分。
从 blocks.中文.金额雅写 导入 银码。

函数 税据 接收 赵月收：
  定义赵税=缴税(赵月收)。
  定义赵税后=圆分(减 赵月收 赵税)。
  返回 列 赵月收 赵税 赵税后 银码(赵税)。
。

导出 税据。
