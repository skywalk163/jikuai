-- 极快 v0.18.0 块生态：财务.贷户档案
-- 三级块（层级 3）——信贷客户一站式单据：个人档案 + 分期还款计划。
--
-- L3 判定（ADR-28 §3.1）：满足**条件 1** —— 直接依赖 **2 个 L2+ 聚合块**：
--   月结单(L2/财务/stable) + 用户档案(L2/中文/stable)
-- 跨域并集 = {财务, 中文}。**这是块库里第一个走条件 1 的 L3**（现有 5 个
-- L3 全走条件 2「1 个 L2+ 且依赖跨 >=2 领域」），把 ADR-28 §3.1 的两条判定
-- 路径都覆盖上，让 check_level_consistency 的条件 1 分支第一次有真实样本。
--
-- 稳定性：experimental。两条直接依赖都是 stable，§3.2 允许标 stable，但本块
-- 首版的「贷户单据该含哪些字段」（要不要含市/区？要不要含总利息？）没经真实
-- 信贷业务打磨，按 DEFAULT_STABILITY 保守起标。
--
-- 【本块是 ADR-30 字面匹配的上限压力样本】v0.18-L3选题.md §二 L3-4 明说：
-- 入参**刻意不做 §3.6 tuple 收敛**，摊成 9 个标量（字符串×3 + 数×6）。
-- 理由：这块的存在意义就是让 ADR-30 字面匹配面对最恶劣的同型入参场景 ——
-- 6 个 `数` 槽（今年/今月/今日/贷款额/期数/年利率）在类型图上完全无从区分，
-- 只有槽名字面能救。收敛成元组反而"作弊"绕过了路线 2 的真实压力。
-- 兄弟块 贷款简报 是它的全异型对照组。
--
-- 流水：
--   1) 档案解析：中文.用户档案 档帖(姓名,证号,址,今年,今月,今日)
--      → [姓, 名, 有效, 省, 市, 区, 周岁]，取 [0] 姓 / [2] 有效 / [3] 省 / [6] 周岁
--   2) 还款计划：财务.月结单 结单(贷款额,期数,年利率)
--      → [月供, 总息, 月供大写, 每期明细]，取 [0] 月供 / [2] 月供大写
--
-- 返回 6 元组：[姓, 省, 周岁, 月供, 月供大写, 证件有效]
--
-- 【为什么周岁与证件有效性一起出】信贷风控的两条硬门槛：年龄区间 + 证件真伪。
-- 周岁在证号非法时 用户档案 会返回哨兵 −1（不是抛异常），配合第 6 位的
-- 证件有效布尔，调用方能区分「26 岁真件」与「−1 未知 + 假件」两种情形，
-- 不需要再猜。
--
-- 【每期明细与总利息不进返回值】沿用 ADR-28 §3.6 在返回侧的同一取舍（见
-- 兄弟块 贷款简报 的同名注记）：[3] 位长度 == 期数，240 期会把单据撑成账本。
--
-- 用户块函数必须括号调用（ADR-11）。
-- 形参 赵姓名/赵证号/赵址/赵今年/赵今月/赵今日/赵贷款额/赵期数/赵年利率 与局部
-- 赵档/赵姓/赵省/赵有效/赵岁/赵结/赵供/赵雅写 均已跑原子性预检。
-- 注意 `赵大写` 不可用（`大写` 是内建 VERB），故月供大写命名为 `赵雅写`。

从 blocks.财务.月结单 导入 结单。
从 blocks.中文.用户档案 导入 档帖。

函数 贷户 接收 赵姓名 赵证号 赵址 赵今年 赵今月 赵今日 赵贷款额 赵期数 赵年利率：
  定义赵档=档帖(赵姓名, 赵证号, 赵址, 赵今年, 赵今月, 赵今日)。
  定义赵姓=取值 赵档 0。
  定义赵有效=取值 赵档 2。
  定义赵省=取值 赵档 3。
  定义赵岁=取值 赵档 6。
  定义赵结=结单(赵贷款额, 赵期数, 赵年利率)。
  定义赵供=取值 赵结 0。
  定义赵雅写=取值 赵结 2。
  返回 列 赵姓 赵省 赵岁 赵供 赵雅写 赵有效。
。

导出 贷户。
