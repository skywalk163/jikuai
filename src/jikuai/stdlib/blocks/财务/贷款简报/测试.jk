-- 块 财务.贷款简报 的单元测试（三级块，聚合 月结单 + 数字中文 + 状态判定）。
-- 全异型入参：贷款参数(元组) / 提示(字符串) / 预算(数)。

从 blocks.财务.贷款简报 导入 贷简。

-- 断言 1（12 万 12 期 6% · 月供 10327.97 > 预算 11000？否，在预算内 · happy path）
--   结单(120000,12,0.06) → 月供 10327.97；10327.97 <= 11000 → 码 200 → 在预算内
--   实测 [10327.97, '壹万零叁佰贰拾柒元玖角柒分', True,
--         '年度经营贷 · 分 十二 期 · 月供 壹万零叁佰贰拾柒元玖角柒分 · 预算核对：成功']
打印 贷简(列 120000 12 0.06, "年度经营贷", 11000)。

-- 断言 2（同贷款但预算压到 1 万 · 超预算边界）：10327.97 > 10000 → 码 400 → 客户端错误
--   实测 [10327.97, '壹万零叁佰贰拾柒元玖角柒分', False, '...预算核对：客户端错误']
打印 贷简(列 120000 12 0.06, "年度经营贷", 10000)。

-- 断言 3（零利率 1 期 · 月供恰好等于预算 · <= 边界守恒）
--   结单(10000,1,0) → 月供 10000.0；10000.0 <= 10000 → 码 200（恰好等于算在预算内）
--   实测 [10000.0, '壹万元整', True, '短期周转 · 分 一 期 · 月供 壹万元整 · 预算核对：成功']
打印 贷简(列 10000 1 0, "短期周转", 10000)。
