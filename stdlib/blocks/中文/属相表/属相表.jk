-- 极快 v0.12.0 块生态：中文.属相表
-- 原子块（层级 0）——把公历年份转成生肖。
-- 导出名 `属相` 已验证词法原子（ADR-15 §3.7）：tokenize('属相。') → [('IDENT','属相')]。
--
-- 【换名记录 1】导出名：`生肖` → `属相`
--   `生肖` 是极快内建动词（keywords.py 第 134 行），作导出名会被切成 VERB。
--   取俗称 `属相` 规避。
--
-- 【换名记录 2】目录名：`生肖属相` → `属相表`
--   目录名会作为点分导入路径的一段出现（`从 blocks.中文.X 导入 …`），
--   parser._read_module_name 要求**每段恰好一个词形 token**。
--   `生肖属相` 被切成 `生肖`(VERB) + `属相`(IDENT) 共两个 token，
--   导致 `从 blocks.中文.生肖属相 导入 属相。` 报「从...期望 导入」语法错误。
--   `属相表` 整体成词为单个 IDENT，故改用它。
--
-- 形参 `赵年份` 已验证原子。
-- 底层复用内建动词 `生肖`（元数 1，返回单字生肖字符串，如 '马'）。

函数 属相 接收 赵年份：
  返回 生肖 赵年份。
。

导出 属相。
