-- 极快 v0.12.0 块生态：中文.干支
-- 原子块（层级 0）——把公历年份转成干支纪年。
-- 导出名 `甲子` 已验证词法原子（ADR-15 §3.7）：tokenize('甲子。') → [('IDENT','甲子')]。
--
-- 为什么不叫 `干支`：`干支纪年` 是内建动词；`干支` 语义上也易与内建混淆。
-- 取 `甲子`（干支之首，代指干支纪年）作为原子导出名。
-- 形参 `赵年份` 已验证原子。
--
-- 底层复用内建动词 `干支纪年`（元数 1，返回两字干支字符串，如 '丙午'）。

函数 甲子 接收 赵年份：
  返回 干支纪年 赵年份。
。

导出 甲子。
