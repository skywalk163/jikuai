-- 极快 v0.12.0 块生态：中文.手机号
-- 原子块（层级 0）——校验手机号；合法则返回运营商名，非法返回空串。
-- 导出名 `核号` 已验证词法原子（ADR-15 §3.7）：tokenize('核号。') → [('IDENT','核号')]。
--
-- 为什么不叫 `校验手机号` / `判断运营商`：两者都是极快内建动词
-- （keywords.py 第 126、127 行）。取 `核号`（核对号码）作为原子导出名。
-- 目录名 `手机号` 本身已是单个 IDENT，可直接作点分路径片段。
-- 形参 `赵号码` 已验证原子。
--
-- 底层复用两个内建动词（均元数 1，走 stdlib 校验模块）：
--   `校验手机号` → 布尔
--   `判断运营商` → 字符串（'移动'/'联通'/'电信'）
-- 用「先校验、后归属」的短路顺序，避免把非法号码喂给运营商判定。

函数 核号 接收 赵号码：
  如果 校验手机号 赵号码 那么：
    返回 判断运营商 赵号码。
  否则：
    返回 ""。
  。
。

导出 核号。
