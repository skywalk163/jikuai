-- 极快 v0.12.0 块生态：中文.排版规整
-- 原子块（层级 0）——中英混排规范化（CJK-Latin 间距 + 标点规范化）。
-- 导出名 `润色` 已验证词法原子（ADR-15 §3.7）：tokenize('润色。') → [('IDENT','润色')]。
--
-- 为什么不叫 `规范化文本` / `插入间距` / `排版`：
--   `规范化文本`、`插入间距`、`规范标点` 是 stdlib 排版模块的导出名；
--   同名会遮蔽。取 `润色`（润饰排版）作为单一入口封装三步规整。
-- 目录名 `排版规整` 已验证是单个 IDENT。
-- 形参 `赵文本` 已验证原子。
--
-- 底层复用 stdlib 混合模块 `排版`（实现在 排版.py）的三个函数：
--   `规范标点` — 半角标点→全角
--   `插入间距` — CJK 与 Latin/数字之间补空格
--   `规范化文本` — 综合规范化（含上述二者加首尾清理）
-- 这里直接用 `规范化文本` 一步到位。

从 排版 导入 规范化文本。

函数 润色 接收 赵文本：
  返回 规范化文本(赵文本)。
。

导出 润色。
