-- 极快 v0.12.0 块生态：中文.阴历日程
-- 一级块（层级 1）——输入公历年月日，返回 [农历日期, 生肖, 干支] 三元列表。
-- 导出名 `历帖` 已验证词法原子（ADR-15 §3.7）：tokenize('历帖。') → [('IDENT','历帖')]。
--
-- 【换名记录】目录名：`农历日程` → `阴历日程`
--   目录名会作为点分导入路径的一段（`从 blocks.中文.X 导入 …`），
--   parser._read_module_name 要求**每段恰好一个词形 token**。
--   `农历日程` 被切成 `农历`(VERB) + `日程`(IDENT) 两个 token，导入语句会报
--   「从...期望 导入」语法错误。`阴历日程` 整体成词为单个 IDENT，故改用它。
--
-- 依赖三个层级 0 原子块，全部用 `从 blocks.中文.<目录名> 导入 <导出名>` 引入：
--   中文.农历    → 阴历(年, 月, 日) → 农历完整日期字符串
--   中文.属相表  → 属相(年)         → 单字生肖
--   中文.干支    → 甲子(年)         → 两字干支
-- 用户块函数是普通用户函数，**必须括号调用**，多参用逗号分隔。
-- 形参 `赵年`/`赵月`/`赵日` 均已验证原子。

从 blocks.中文.农历 导入 阴历。
从 blocks.中文.属相表 导入 属相。
从 blocks.中文.干支 导入 甲子。

函数 历帖 接收 赵年 赵月 赵日：
  返回 列 阴历(赵年, 赵月, 赵日) 属相(赵年) 甲子(赵年)。
。

导出 历帖。
