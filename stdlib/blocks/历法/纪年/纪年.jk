-- 极快 v0.13.0 块生态：历法.纪年
-- 一级块（层级 1）——公历年 → [干支, 生肖] 二元列表。
--
-- 导出名换名记录：原取 `岁次` 不可用 —— `次` 是 KEYWORD（`重复 N 次`），
-- `岁次` 被切成 `岁`(IDENT)+`次`(KEYWORD) 两个 token。改用 `支肖`
-- （干支 + 生肖），已验证是单个 IDENT。
--
-- 跨领域聚合：历法域负责组合，中文域提供干支/生肖的表现层能力。
-- 依赖块用叶名（`干支` / `属相表`），G11 门禁校验双向一致。

从 blocks.中文.干支 导入 甲子。
从 blocks.中文.属相表 导入 属相。

函数 支肖 接收 赵年：
  返回 列 甲子(赵年) 属相(赵年)。
。

导出 支肖。
