-- 单元测试：`工具.日志.志记`。
-- 3 条断言性打印：分别覆盖信息 / 警告 / 错误三个常见级别，
-- 并验证 `志记` 会把「原消息」按值回传（可拼进管道）。

从 blocks.工具.日志 导入 志记。

打印 志记("信息", "启动完成")。
打印 志记("警告", "配置项缺失")。

定义 赵回传 = 志记("错误", "hello")。
打印 拼接 "回传：" 赵回传。
