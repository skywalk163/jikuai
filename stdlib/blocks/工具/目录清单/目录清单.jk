-- 极快 v0.12.0 块生态：工具.目录清单
-- 原子块（层级 0）——列出目录下的条目名。
-- 依赖 蟒:os（os.listdir），故 稳定性=experimental。
--
-- 命名（ADR-15 §3.7）：
--   建议名 `罗列` **不可用**——lexer 把它切成 `罗`(IDENT)+`列`(VERB)，
--   调用方会拿不到符号。改用 `名录`，实测单个 IDENT ✓
--
-- 语义与 os.listdir 一致：既含文件也含子目录，不做过滤（过滤要拼全路径
-- 再 isfile，属于调用方的策略，不该由原子块替它决定）。

导入 蟒:os。

函数 名录 接收 赵目录：
  返回 os.listdir(赵目录)。
。

导出 名录。
