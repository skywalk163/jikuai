-- 极快 v0.12.0 块生态：工具.配置集
-- 一级块（层级 1）——读取多个环境变量，缺失的记日志，返回值列表。
-- 依赖原子块：环境值（环境）、日志（志记）。
--
-- 导出名 `载配` 词法原子 ✓

从 blocks.工具.环境值 导入 环境。
从 blocks.工具.日志 导入 志记。

函数 载配 接收 赵键表：
  定义 赵结果 = 列。
  遍历 赵键 于 赵键表：
    定义 赵值 = 环境(赵键, "")。
    如果 等于 赵值 "" 那么：
      志记("警告", 拼接 "环境变量缺失：" 赵键)。
    。
    赋值 赵结果 = 追加 赵结果 赵值。
  。
  返回 赵结果。
。

导出 载配。
