-- 极快 v0.13.0 块生态：数据.分组
-- 原子块（层级 0）——把列表按固定大小切成若干组，末组允许不足。
--
-- 导出名换名记录：原取 `归组` 不可用 —— `归` 是 ADVERB（reduce 副词），
-- `归组` 被切成 `归`(ADVERB)+`组`(IDENT)。改用 `聚簇`，已验证单 IDENT。
--
-- 典型用途：批量接口按 100 条一批提交、分页渲染、并发切片。

函数 聚簇 接收 赵项表 赵每组：
  定义赵集=列。
  定义赵组=列。
  遍历 赵项 于 赵项表：
    赋值赵组=追加 赵组 赵项。
    定义赵长=长度 赵组。
    如果 等于 赵长 赵每组 那么：
      赋值赵集=追加 赵集 赵组。
      赋值赵组=列。
    。
  。
  定义赵余=长度 赵组。
  如果 大于 赵余 0 那么：
    赋值赵集=追加 赵集 赵组。
  。
  返回 赵集。
。

导出 聚簇。
