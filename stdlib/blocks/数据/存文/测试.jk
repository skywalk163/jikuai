-- 块 `数据.存文` 的单元测试。
-- 临时文件统一写到本块目录下的 `临时_测试*.txt`，测试结束后由外部清理。
从 blocks.数据.存文 导入 落盘。

-- 测试1：普通字符串
定义赵果=落盘("stdlib/blocks/数据/存文/临时_测试.txt" "测试内容")。
打印 赵果。

-- 测试2：空字符串（边界）
定义赵空=落盘("stdlib/blocks/数据/存文/临时_测试2.txt" "")。
打印 赵空。

-- 测试3：多行文本（特殊情况：含换行转义）
定义赵多=落盘("stdlib/blocks/数据/存文/临时_测试3.txt" "行1\n行2\n行3")。
打印 赵多。
