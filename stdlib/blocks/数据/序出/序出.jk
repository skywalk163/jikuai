-- 极快 v0.12.0 块生态：数据.序出
-- 一级块（层级 1）——输入列表与文件路径，升序后写入文件，返回写入行数。
-- 导出名 `存档` 已验证词法原子（ADR-15 §3.7）。
--
-- 换名记录（两处）：
-- 1) 导出名：原建议 `归档` 不原子——分词为 `归`(ADVERB) + `档`(IDENT)，
--    因为 `归` 是归约副词。改用 `存档`（单个 IDENT）。
-- 2) 目录名：原计划 `排序导出` 不可用——分词为 `排序`(VERB) + `导出`(KEYWORD)
--    两个 token，而点分模块路径每段只能是一个 token。改用 `序出`。
--
-- 依赖块：升序、文本合成、存文

从 blocks.数据.升序 导入 顺排。
从 blocks.数据.文本合成 导入 缝合。
从 blocks.数据.存文 导入 落盘。

函数 存档 接收 赵数值 赵路径：
  定义赵序=顺排(赵数值)。
  定义赵文=缝合(赵序 "\n")。
  落盘(赵路径 赵文)。
  返回 长度 赵序。
。

导出 存档。
