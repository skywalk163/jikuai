-- 极快 v0.13.0 块生态：数据.扁平
-- 原子块（层级 0）——把嵌套一层的列表摊平成单层。
--
-- 导出名 `摊平` 已验证词法原子（ADR-15 §3.7）。
-- 只摊一层（与 `分组` 互为逆操作），不做递归深摊——递归深度不可控，
-- 需要深摊的场景自己套两次更明确。

函数 摊平 接收 赵项表：
  定义赵集=列。
  遍历 赵组 于 赵项表：
    遍历 赵项 于 赵组：
      赋值赵集=追加 赵集 赵项。
    。
  。
  返回 赵集。
。

导出 摊平。
