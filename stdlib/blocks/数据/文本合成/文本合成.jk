-- 极快 v0.12.0 块生态：数据.文本合成
-- 原子块（层级 0）——用分隔符把列表合成字符串。
-- 导出名 `缝合` 已验证词法原子（ADR-15 §3.7）。
--
-- 实现换路记录：原设计想直接 `拼接 赵数值 赵分隔`，但内建 `拼接` 是**变参
-- 字符串连接**（把每个实参 str() 后首尾相接），不是「按分隔符 join 列表」：
--   拼接 列("甲" "乙") ","   →  "['甲', '乙'],"     ✗
-- 极快没有内建 join 动词，故用 `遍历` + 计数器手写 join。

函数 缝合 接收 赵数值 赵分隔：
  定义赵果=""。
  定义赵计=0。
  遍历赵项于赵数值：
    如果 赵计 大于 0 那么：
      赋值赵果=拼接 赵果 赵分隔 赵项。
    否则：
      赋值赵果=拼接 赵果 赵项。
    。
    赋值赵计=赵计 加 1。
  。
  返回 赵果。
。

导出 缝合。
