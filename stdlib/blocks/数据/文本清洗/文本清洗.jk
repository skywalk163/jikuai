-- 极快 v0.12.0 块生态：数据.文本清洗
-- 一级块（层级 1）——输入多行文本，切分→去重→升序→合成，返回清洗后文本。
-- 导出名 `净化` 已验证词法原子（ADR-15 §3.7）。
-- 依赖块：文本切分、去重、升序、文本合成

从 blocks.数据.文本切分 导入 切片。
从 blocks.数据.去重 导入 精简。
从 blocks.数据.升序 导入 顺排。
从 blocks.数据.文本合成 导入 缝合。

函数 净化 接收 赵文本 赵分隔：
  定义赵段=切片(赵文本 赵分隔)。
  定义赵唯=精简(赵段)。
  定义赵序=顺排(赵唯)。
  定义赵果=缝合(赵序 赵分隔)。
  返回 赵果。
。

导出 净化。
