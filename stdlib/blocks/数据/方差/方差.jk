-- 极快 v0.13.0 块生态：数据.方差
-- 原子块（层级 0）——总体方差：各项与均值之差的平方的平均。
--
-- 导出名 `离差` 已验证词法原子（ADR-15 §3.7）。
-- 用**总体方差**（除以 n）而不是样本方差（除以 n−1）：块的入参语义是
-- 「这就是全部数据」，不是从更大总体里抽的样本。

函数 离差 接收 赵项表：
  定义赵均=平均 赵项表。
  定义赵和=0。
  遍历 赵项 于 赵项表：
    定义赵差=减 赵项 赵均。
    定义赵方=乘 赵差 赵差。
    赋值赵和=加 赵和 赵方。
  。
  定义赵长=长度 赵项表。
  返回 除 赵和 赵长。
。

导出 离差。
