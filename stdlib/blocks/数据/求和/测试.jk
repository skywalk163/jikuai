-- 块 `数据.求和` 的单元测试。
-- 注意导入形式：必须用 `从 ... 导入 <导出名>`，把导出名直接绑进当前作用域。
-- 不能写 `导入 blocks.数据.求和。` 后调用 `汇总(...)`——那样 `汇总` 只挂在
-- 模块对象上，当前作用域拿不到（ADR-15 §3.7）。

从 blocks.数据.求和 导入 汇总。

定义赵结果=汇总(列 1 2 3 4 5)。
打印 赵结果。

定义赵空=汇总(列)。
打印 赵空。
