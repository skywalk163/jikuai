-- 块 `数据.载入` 的单元测试。
-- 用兄弟块 `存文.落盘` 先造样本再读回来对照（内建 `写入` 未实现）。
-- 临时文件写在本块目录下（`临时_测试*.txt`），测试结束后由外部清理。
从 blocks.数据.载入 导入 装载。
从 blocks.数据.存文 导入 落盘。

-- 测试1：普通中文文本
落盘("stdlib/blocks/数据/载入/临时_测试.txt" "你好极快")。
定义赵果=装载("stdlib/blocks/数据/载入/临时_测试.txt")。
打印 赵果。

-- 测试2：空文件（边界）
落盘("stdlib/blocks/数据/载入/临时_测试2.txt" "")。
定义赵空=装载("stdlib/blocks/数据/载入/临时_测试2.txt")。
打印 赵空。

-- 测试3：多行文本（特殊情况：换行原样保留）
落盘("stdlib/blocks/数据/载入/临时_测试3.txt" "甲\n乙\n丙")。
定义赵多=装载("stdlib/blocks/数据/载入/临时_测试3.txt")。
打印 赵多。
