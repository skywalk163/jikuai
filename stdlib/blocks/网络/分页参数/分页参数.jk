-- 极快 v0.12.0 块生态：网络.分页参数
-- 原子块（层级 0）——总数 + 每页数 → 各页 offset 列表（如 25,10 → [0,10,20]）。
--
-- 命名约束（ADR-15 §3.7）：`翻页` 是原子 IDENT。
--
-- 设计取舍：
-- 1. **纯极快实现，不碰 Python 桥** ⇒ 稳定性 `stable`。
-- 2. 用 `当`（while）从 0 起按 `每页` 步进累加，直到 >= 总数。offset 语义与
--    多数 REST/SQL 分页一致（`?offset=N&limit=每页`）。
-- 3. 总数<=0 → 空列表（循环不进入）。假定每页>0（每页<=0 无意义，调用方保证）。

函数 翻页 接收 赵总 赵每页：
  定义赵集=列。
  定义赵偏=0。
  当 小于 赵偏 赵总：
    赋值赵集=追加 赵集 赵偏。
    赋值赵偏=加 赵偏 赵每页。
  。
  返回 赵集。
。

导出 翻页。
