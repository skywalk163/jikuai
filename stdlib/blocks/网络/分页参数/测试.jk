-- 块 `网络.分页参数` 的单元测试。纯数值计算，不触网络。

从 blocks.网络.分页参数 导入 翻页。

-- 断言 1：25 条每页 10 → [0, 10, 20]
打印 翻页(25 10)。

-- 断言 2：整除边界，20 条每页 10 → [0, 10]
打印 翻页(20 10)。

-- 断言 3：不足一页，5 条每页 10 → [0]
打印 翻页(5 10)。

-- 断言 4：0 条 → [] （无页）
打印 翻页(0 10)。

-- 断言 5：页数可数（配合 长度 动词）。100 条每页 30 → 4 页
打印 长度 翻页(100 30)。
