-- 极快 v0.13.0 块生态：网络.域名剖解
-- 原子块（层级 0）——把域名按点分拆成子域列表（"api.example.com" → ["api","example","com"]）。
--
-- 命名（ADR-15 §3.7）：导出 `拆域`、目录 `域名剖解`、形参 `赵域` 均已跑原子性预检。
-- 与 `网络.网址剖解` 区别：本块只处理裸域名字符串，不做协议/路径拆分。
-- 实现：纯极快，内建 `分割 赵域 "."`。

函数 拆域 接收 赵域:
  返回 分割 赵域 "."。
。

导出 拆域。
