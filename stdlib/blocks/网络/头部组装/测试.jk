-- 块 `网络.头部组装` 的单元测试。纯字符串拼接，不触网络。

从 blocks.网络.头部组装 导入 造头。

-- 断言 1：单键。期望 ['Accept: application/json']
打印 造头({"Accept": "application/json"})。

-- 断言 2：多键按字典序（大写字母序在小写之前）。
-- 期望 ['Accept: */*', 'X-Trace: abc']
打印 造头({"X-Trace": "abc", "Accept": "*/*"})。

-- 断言 3：空字典 → 空列表。期望 []
打印 造头({})。

-- 断言 4：数值型值经 拼接 归一为字符串。期望 ['Content-Length: 128']
打印 造头({"Content-Length": 128})。

-- 断言 5：长度可数（配合 长度 动词做条件分支）。期望 2
打印 长度 造头({"A": "1", "B": "2"})。
