-- 块 `网络.记法织串` 的单元测试。纯序列化，不触网络。

从 blocks.网络.记法织串 导入 织串。

-- 断言 1：列表 → JSON 数组。期望 [1, 2, 3]
打印 织串(列 1 2 3)。

-- 断言 2：字典 → JSON 对象（键排序由 dumps 决定，这里单键确定）。期望 {"a": 1}
打印 织串({"a": 1})。

-- 断言 3：字符串标量 → 带引号 JSON。期望 "hi"
打印 织串("hi")。

-- 断言 4：整数标量。期望 42
打印 织串(42)。

-- 断言 5：嵌套列表。期望 [[1, 2], [3, 4]]
打印 织串(列 (列 1 2) (列 3 4))。
