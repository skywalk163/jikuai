-- 块 `网络.记法解析` 的单元测试。纯字符串→结构解析，不触网络。

从 blocks.网络.记法解析 导入 解构。

-- 断言 1：数组 → 列表。期望 [1, 2, 3]
打印 解构("[1,2,3]")。

-- 断言 2：对象 → 字典（键值可读回）。期望 1，然后 hello
定义赵d=解构("{\"a\":1,\"b\":\"hello\"}")。
打印 赵d.a。
打印 赵d.b。

-- 断言 3：嵌套结构。期望 [1, 2]
定义赵n=解构("{\"k\":[1,2]}")。
打印 赵n.k。

-- 断言 4：标量。期望 42
打印 解构("42")。

-- 断言 5：字符串标量。期望 hi
打印 解构("\"hi\"")。
