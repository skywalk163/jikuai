-- 极快 v0.13.0 块生态：网络.超时策略
-- 原子块（层级 0）——按尝试轮次计算封顶指数退避时长（毫秒）。
--
-- 命名（ADR-15 §3.7）：导出 `候时`、目录 `超时策略`、形参 `赵轮`/`赵基数`/`赵上限` 均已跑原子性预检。
-- 与 `网络.重试策略` 区别：`重试策略` 返回整个退避序列；本块只给单轮的等待值。
-- 算法：等待 = min(base * 2^attempt, cap)；attempt<0 直接返回 0。
-- 坑 #1：嵌套动词做实参必须括号。

函数 候时 接收 赵轮 赵基数 赵上限:
  如果 小于 赵轮 0 那么:
    返回 0。
  。
  定义赵倍=幂 2 赵轮。
  定义赵毫=乘 赵基数 赵倍。
  如果 大于 赵毫 赵上限 那么:
    返回 赵上限。
  。
  返回 赵毫。
。

导出 候时。
