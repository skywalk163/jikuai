-- 极快 v0.13.0 块生态：网络.跳转链
-- 原子块（层级 0）——从跳转链列表取最终 URL（末个）。空表返回空串。
--
-- 命名（ADR-15 §3.7）：导出 `追踪`、目录 `跳转链`、形参 `赵链` 均已跑原子性预检。
-- 场景：调用方拿到 3xx 重定向历史（起点 + 每次 Location），本块给出最终目标。
-- 语义：只取尾部；不做去重/环检测（那属于 L1 聚合块的职责）。

函数 追踪 接收 赵链:
  如果 等于 (长度 赵链) 0 那么:
    返回 ""。
  。
  返回 末个 赵链。
。

导出 追踪。
