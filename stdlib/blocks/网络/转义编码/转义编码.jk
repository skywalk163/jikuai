-- 极快 v0.12.0 块生态：网络.转义编码
-- 原子块（层级 0）——字符串百分号编码（RFC 3986）。
--
-- 命名约束（ADR-15 §3.7）：`转义` 是原子 IDENT。
-- 目录名原要求 `百分编码`，但 `百` 被 lexer 识别为中文数字 100，不原子。
-- 改用 `转义编码`（原子）作为目录名。
--
-- 实现：`urllib.parse.quote(s, safe="")`，safe="" 使 `/` 也转义。
-- 稳定性：`experimental`（依赖 pybridge）。`urllib.parse` 不发网络。

导入 蟒:urllib.parse。

函数 转义 接收 赵文本：
  返回 urllib.parse.quote(赵文本 "")。
。

导出 转义。
