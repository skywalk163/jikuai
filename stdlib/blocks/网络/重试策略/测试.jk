-- 块 `网络.重试策略` 的单元测试。纯数值计算，不触网络。

从 blocks.网络.重试策略 导入 退避。

-- 断言 1：3 次 → [100, 200, 400]
打印 退避(3)。

-- 断言 2：1 次 → [100]
打印 退避(1)。

-- 断言 3：0 次 → [] （不重试）
打印 退避(0)。

-- 断言 4：5 次 → [100, 200, 400, 800, 1600]
打印 退避(5)。

-- 断言 5：末档时长可取（配合 末个 动词）。期望 400
打印 末个 退避(3)。
