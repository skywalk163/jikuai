-- 极快 v0.13.0 块生态：财务.分期
-- 原子块（层级 0）——把总额等额分成 N 期，余数落在最后一期。
--
-- 导出名 `期供` 已验证词法原子（ADR-15 §3.7）。
--
-- 为什么余数要落最后一期：`10 元分 3 期` 平摊得 3.33，三期合计 9.99，
-- 差 1 分。财务场景不允许总额对不上，所以前 N−1 期取分位平摊值，
-- 最后一期用「总额 − 已付」兜住差额。

从 blocks.财务.保留分 导入 圆分。

函数 期供 接收 赵总额 赵期数：
  定义赵每期=圆分(除 赵总额 赵期数)。
  定义赵集=列。
  定义赵计=0。
  定义赵已付=0。
  定义赵末期=减 赵期数 1。
  当 小于 赵计 赵末期：
    赋值赵集=追加 赵集 赵每期。
    赋值赵已付=加 赵已付 赵每期。
    赋值赵计=加 赵计 1。
  。
  定义赵尾款=圆分(减 赵总额 赵已付)。
  赋值赵集=追加 赵集 赵尾款。
  返回 赵集。
。

导出 期供。
