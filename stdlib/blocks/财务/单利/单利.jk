-- 极快 v0.13.0 块生态：财务.单利
-- 原子块（层级 0）——单利计息：本金 × 年利率 × 年数。
--
-- 导出名 `息金` 已验证词法原子（ADR-15 §3.7）。
-- 形参用 `赵本金` / `赵年率` / `赵年数`：首字必须是百家姓姓氏，且整体词法原子。
--
-- 极快没有中缀运算符，`本金 × 利率 × 年数` 写作前缀嵌套 `乘 乘 a b c`。
-- 结果过 `圆分` 收口到分位——财务领域的统一约定，别让无穷小数漏到调用方。

从 blocks.财务.保留分 导入 圆分。

函数 息金 接收 赵本金 赵年率 赵年数：
  返回 圆分(乘 乘 赵本金 赵年率 赵年数)。
。

导出 息金。
