-- 极快 v0.18.0 块生态：财务.员工薪历
-- 三级块（层级 3）——单员工月薪一次算出税后 + 周岁 + 月薪中文大写 + 核算当月日历。
--
-- L3 判定（ADR-28 §3.1）：满足条件 2 —— 直接 L2+ 聚合依赖 1 个（工资条/L2），
-- 依赖跨 3 个领域：财务(工资条) / 中文(金额报表) / 历法(历表)。
--
-- 稳定性：experimental。三条直接依赖都是 stable，§3.2 允许标 stable，但本块
-- 首版把「核算日历」定死取核算月（今日期的年月），没给「上一发薪月」等选项，
-- 首版语义边界未打磨，按 DEFAULT_STABILITY 保守起标。
--
-- 【本块的 ADR-30 测试角色】v0.18-L3选题.md §二 L3-6：入参把两个日期各自
-- 打包成元组（§3.6 部分收敛），但生辰 与 今日期 **同为 元组[数,数,数]**，
-- 是给 ADR-30 字面匹配的中等强度样本 —— 考「生」/「今」这两个字面前缀能不能
-- 把两个同型元组分别喂到正确的位置。全异型的对照组见兄弟块 贷款简报。
--
-- 流水：
--   1) 解包：取值 赵生辰 0/1/2 → 生年/生月/生日；取值 赵今日期 0/1/2 → 今年/今月/今日
--   2) 工资条：财务.工资条 薪单(月薪, 生年,生月,生日, 今年,今月,今日)
--      → [税前, 个税, 税后, 大写, 岁]，取 [2] 税后、[4] 周岁
--   3) 月薪大写：中文.金额报表 账单(月薪) → [数, 大写, 汉数]，取 [1] 大写
--   4) 当月日历：历法.历表 月历(今年, 今月) → [[日, 星期名], ...]
--
-- 返回 4 元组：[税后, 周岁, 月薪大写, 当月日历]
--
-- 【为什么大写取自 账单 而不是 薪单[3]】薪单[3] 是「税后」的大写，本块要的是
-- 「月薪」本身的大写呈现（工资单抬头写的是月薪不是税后），故单独走 账单(月薪)。
--   两者语义不同，不能图省事复用。
--
-- 用户块函数必须括号调用（ADR-11）。
-- 形参 赵月薪/赵生辰/赵今日期 与局部 赵生年/赵生月/赵生日/赵今年/赵今月/赵今日/
-- 赵薪/赵税后/赵岁/赵单/赵雅写/赵历 均已跑原子性预检。
-- 注意 `赵大写` 不可用（`大写` 是内建 VERB），故月薪大写命名为 `赵雅写`。

从 blocks.财务.工资条 导入 薪单。
从 blocks.中文.金额报表 导入 账单。
从 blocks.历法.历表 导入 月历。

函数 薪历 接收 赵月薪 赵生辰 赵今日期：
  定义赵生年=取值 赵生辰 0。
  定义赵生月=取值 赵生辰 1。
  定义赵生日=取值 赵生辰 2。
  定义赵今年=取值 赵今日期 0。
  定义赵今月=取值 赵今日期 1。
  定义赵今日=取值 赵今日期 2。
  定义赵薪=薪单(赵月薪, 赵生年, 赵生月, 赵生日, 赵今年, 赵今月, 赵今日)。
  定义赵税后=取值 赵薪 2。
  定义赵岁=取值 赵薪 4。
  定义赵单=账单(赵月薪)。
  定义赵雅写=取值 赵单 1。
  定义赵历=月历(赵今年, 赵今月)。
  返回 列 赵税后 赵岁 赵雅写 赵历。
。

导出 薪历。
