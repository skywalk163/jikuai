-- 极快 v0.13.0 块生态：财务.折旧
-- 原子块（层级 0）——直线法折旧：逐年折旧额列表。
--
-- 导出名 `折价` 已验证词法原子（ADR-15 §3.7）。
--
-- 直线法：年折旧额 = (原值 − 残值) ÷ 年限，每年相同。
-- 与 `分期` 同样的分位处理：前 N−1 年取平摊值，最后一年兜差额，
-- 保证累计折旧正好等于「原值 − 残值」，不因四舍五入漏分。

从 blocks.财务.保留分 导入 圆分。

函数 折价 接收 赵原值 赵残值 赵年限：
  定义赵总额=减 赵原值 赵残值。
  定义赵每年=圆分(除 赵总额 赵年限)。
  定义赵集=列。
  定义赵计=0。
  定义赵已提=0。
  定义赵末年=减 赵年限 1。
  当 小于 赵计 赵末年：
    赋值赵集=追加 赵集 赵每年。
    赋值赵已提=加 赵已提 赵每年。
    赋值赵计=加 赵计 1。
  。
  定义赵尾=圆分(减 赵总额 赵已提)。
  赋值赵集=追加 赵集 赵尾。
  返回 赵集。
。

导出 折价。
