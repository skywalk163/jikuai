-- 极快 v0.14.0 块生态：财务.月结单
-- 二级块（层级 2）——月度还款月结单：
--   返回 [月供, 总利息, 月供中文大写, 每期供额明细] 四元列表。
--
-- 导出名 `结单` 已验证词法原子（ADR-15 §3.7）。
--
-- 跨领域聚合：金额计算取 财务.等额本息/分期/保留分，表现层取 中文.金额雅写。
--
-- 算法（v0.14.0 W12 修正后）：
--   月供,总息,总还 = 房贷(贷款额, 年利率, 期数)   -- 真等额本息（月率=年率÷12 的年金）
--   月供 = 圆分(月供)                              -- 圆到分，避免浮点尾
--   明细 = 期供(总还, 期数)                        -- 等额切片，余数落最后一期
--   大写 = 银码(月供)                              -- 中文大写呈现
--
-- 与旧版（复利滚存÷期数）的差别：旧版把年利率按 (期数÷12) 年复利滚一次
-- 再摊到每期，等价于「本金全期滚利再平均」，与真实等额本息的月供偏差最大到
-- 10% 左右（如 12 万/12 期/6%：旧算 10600，真等额本息 ≈ 10327.03）。W12
-- 发布准入前用户裁决：金融语义必须正确，切到 等额本息.房贷。
--
-- 零利率退化：房贷 内部走年金公式，年率为 0 时年供块自动退化到 P÷N，
-- 总息 = 0（等额本息块已在 W6 保证）。
-- 用户块函数必须括号调用（ADR-11）。

从 blocks.财务.等额本息 导入 房贷。
从 blocks.财务.分期 导入 期供。
从 blocks.财务.保留分 导入 圆分。
从 blocks.中文.金额雅写 导入 银码。

函数 结单 接收 赵贷款额 赵期数 赵年利率：
  定义赵房=房贷(赵贷款额, 赵年利率, 赵期数)。
  定义赵月供=圆分(取值 赵房 0)。
  定义赵总息=取值 赵房 1。
  定义赵总还=取值 赵房 2。
  定义赵明细=期供(赵总还, 赵期数)。
  定义赵大写=银码(赵月供)。
  返回 列 赵月供 赵总息 赵大写 赵明细。
。

导出 结单。
