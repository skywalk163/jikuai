-- 极快 v0.13.0 块生态：财务.贴现
-- 原子块（层级 0）——把终值折现到现值：终值 ÷ (1 + 贴现率) ^ 期数。
--
-- 导出名 `折现` 已验证词法原子（ADR-15 §3.7）。
-- 嵌套前缀算术拆成中间变量：内联的 `幂 加 1 x n` 会被 parser 误绑元数。

从 blocks.财务.保留分 导入 圆分。

函数 折现 接收 赵终值 赵贴率 赵期数：
  定义赵底=加 1 赵贴率。
  定义赵倍=幂 赵底 赵期数。
  返回 圆分(除 赵终值 赵倍)。
。

导出 折现。
