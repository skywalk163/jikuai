-- 极快语言标准库：中文正则（门面）
-- 本模块为混合模块：实现在同名 正则.py 中（ADR-16 §3.3）。
-- 加载器加载本文件时，会把 正则.py 的公共函数注入模块环境，
-- 这里只需用 `导出` 声明对外可见的公共符号。
--
-- 语法子集（详见 docs/标准库.md）：
--   支持：字面量 / 字符类 [] / 量词 * + ? {n,m} / 分组 () / 或 |
--   中文别名：\汉  等价 [一-鿿]（\u4e00-\u9fff）
--   底层引擎为 Python 标准库 re，其余语法由 re 决定。
--
-- 命名说明：替换动作对外叫 `替代` 而不是 `替换`。
-- 原因：`替换` 已是内建动词（元数 3），词法器的最长关键字匹配会把
-- `替换` 及任何以它开头的名字切成 VERB，而 parser 的成员访问要求
-- `.` 之后必须是 IDENT，故 `正则.替换(...)` / `正则.替换文本(...)`
-- 都无法通过语法分析。在不改动 src/ 的前提下，改名是唯一出路。

导出 匹配 搜索 替代 编译。